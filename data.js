var modules = [
{
id: "intro",
title: "一、C# 入门教程",
desc: "从零开始掌握C#基本语法",
badge: "入门",
lessons: [
{
id: "intro-1",
title: "第一个应用程序",
tags: ["Hello World","Console"],
content: "<h4>创建第一个C#程序</h4><p>打开Visual Studio，选择\"新建项目\"，选择\"控制台应用(.NET Framework)\"或\"控制台应用(.NET Core)\"，为项目命名后点击创建。系统自动生成<code>Main</code>方法的<code>Program.cs</code>。</p><h4>Hello World 解析</h4><pre><code><span class=\"kw\">using</span> <span class=\"type\">System</span>;\n\n<span class=\"kw\">namespace</span> <span class=\"type\">HelloWorld</span>\n{\n    <span class=\"kw\">class</span> <span class=\"type\">Program</span>\n    {\n        <span class=\"kw\">static</span> <span class=\"type\">void</span> <span class=\"fn\">Main</span>(<span class=\"type\">string</span>[] args)\n        {\n            <span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"str\">\"Hello, World!\"</span>);\n        }\n    }\n}</code></pre><table><tr><th>关键字</th><th>说明</th></tr><tr><td>using System</td><td>引入System命名空间</td></tr><tr><td>namespace</td><td>定义命名空间</td></tr><tr><td>class Program</td><td>定义Program类</td></tr><tr><td>static void Main</td><td>程序入口点</td></tr><tr><td>Console.WriteLine</td><td>输出一行文本并换行</td></tr></table><h4>Console常用方法</h4><table><tr><th>方法</th><th>功能</th></tr><tr><td>Write()</td><td>输出不换行</td></tr><tr><td>WriteLine()</td><td>输出并换行</td></tr><tr><td>ReadLine()</td><td>读取用户输入</td></tr><tr><td>ReadKey()</td><td>读取单个按键</td></tr></table><div class=\"highlight-box\"><strong>小贴士：</strong>F5调试运行，Ctrl+F5不调试运行。<code>Console.ReadKey()</code>防止窗口闪退。</div>"
},
{
id: "intro-2",
title: "变量",
tags: ["变量","数据类型"],
content: "<h4>什么是变量</h4><p>变量是程序中存储数据的容器。C#是强类型语言，声明时必须指定类型。</p><h4>声明与初始化</h4><pre><code><span class=\"cm\">// 先声明再赋值</span>\n<span class=\"type\">int</span> age;\nage = <span class=\"num\">25</span>;\n\n<span class=\"cm\">// 声明同时初始化（推荐）</span>\n<span class=\"type\">int</span> age = <span class=\"num\">25</span>;\n<span class=\"type\">string</span> name = <span class=\"str\">\"张三\"</span>;\n<span class=\"type\">double</span> salary = <span class=\"num\">5000.50</span>;\n<span class=\"type\">bool</span> isStudent = <span class=\"kw\">true</span>;\n<span class=\"type\">char</span> grade = <span class=\"str\">'A'</span>;</code></pre><h4>常用数据类型</h4><table><tr><th>类型</th><th>说明</th><th>范围</th><th>示例</th></tr><tr><td>int</td><td>32位整数</td><td>-2^31~2^31-1</td><td>int a=100;</td></tr><tr><td>long</td><td>64位整数</td><td>-2^63~2^63-1</td><td>long b=100L;</td></tr><tr><td>float</td><td>32位浮点</td><td>约7位精度</td><td>float f=3.14f;</td></tr><tr><td>double</td><td>64位浮点</td><td>约15位精度</td><td>double d=3.14;</td></tr><tr><td>decimal</td><td>128位高精度</td><td>28位精度</td><td>decimal m=3.14m;</td></tr><tr><td>bool</td><td>布尔值</td><td>true/false</td><td>bool flag=true;</td></tr><tr><td>char</td><td>16位字符</td><td>单个Unicode</td><td>char c='A';</td></tr><tr><td>string</td><td>字符串</td><td>字符序列</td><td>string s=\"Hello\";</td></tr></table><div class=\"highlight-box\"><strong>小贴士：</strong>变量必须先声明后使用。局部变量必须显式初始化才能使用。</div>"
},
{
id: "intro-3",
title: "变量本质",
tags: ["内存","堆栈"],
content: "<h4>变量在内存中的存储</h4><p>变量的本质是内存中的一块空间。声明<code>int age=25;</code>时，系统在栈上分4字节，将值25以二进制存入，用age关联地址。</p><h4>栈与堆</h4><table><tr><th>特征</th><th>栈(Stack)</th><th>堆(Heap)</th></tr><tr><td>存储</td><td>值类型、引用地址</td><td>引用类型对象</td></tr><tr><td>分配</td><td>编译器自动分配释放</td><td>new分配，GC回收</td></tr><tr><td>速度</td><td>快</td><td>较慢</td></tr><tr><td>空间</td><td>较小(1MB)</td><td>较大(受系统内存限制)</td></tr></table><pre><code><span class=\"cm\">// 值类型：复制内容</span>\n<span class=\"type\">int</span> a = <span class=\"num\">10</span>;\n<span class=\"type\">int</span> b = a;\nb = <span class=\"num\">20</span>;\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(a);  <span class=\"cm\">// 10</span>\n\n<span class=\"cm\">// 引用类型：复制地址</span>\n<span class=\"type\">int</span>[] arr1 = { <span class=\"num\">1</span>, <span class=\"num\">2</span>, <span class=\"num\">3</span> };\n<span class=\"type\">int</span>[] arr2 = arr1;\narr2[<span class=\"num\">0</span>] = <span class=\"num\">99</span>;\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(arr1[<span class=\"num\">0</span>]);  <span class=\"cm\">// 99</span></code></pre><div class=\"highlight-box\"><strong>小贴士：</strong>值类型赋值\"复制内容\"，引用类型赋值\"复制地址\"。</div>"
},
{
id: "intro-4",
title: "变量的命名规则",
tags: ["命名规范","CamelCase"],
content: "<h4>C#命名规则</h4><p>良好的命名让代码自解释。</p><h4>强制规则</h4><table><tr><th>规则</th><th>正确</th><th>错误</th></tr><tr><td>字母数字下划线</td><td>player1,_c</td><td>player-1,my v</td></tr><tr><td>不以数字开头</td><td>item2</td><td>2item</td></tr><tr><td>不用关键字</td><td>@class,sName</td><td>class,int</td></tr><tr><td>区分大小写</td><td>age与Age不同</td><td>-</td></tr></table><h4>推荐规范</h4><table><tr><th>规范</th><th>说明</th><th>示例</th></tr><tr><td>camelCase</td><td>首字母小写</td><td>firstName</td></tr><tr><td>PascalCase</td><td>单词首字母大写</td><td>StudentManager</td></tr><tr><td>有意义</td><td>描述性，不用缩写</td><td>customerAge</td></tr></table><pre><code><span class=\"cm\">// 推荐</span>\n<span class=\"type\">string</span> firstName = <span class=\"str\">\"三\"</span>;\n<span class=\"type\">int</span> studentCount = <span class=\"num\">30</span>;\n<span class=\"cm\">// 不推荐</span>\n<span class=\"type\">string</span> fn = <span class=\"str\">\"三\"</span>;</code></pre><div class=\"highlight-box\"><strong>小贴士：</strong>关键字前加@可用：<code>int @class=1;</code></div>"
},
{
id: "intro-5",
title: "常量",
tags: ["const","不可变"],
content: "<h4>什么是常量</h4><p>运行过程中值不变的量。用<code>const</code>声明，必须声明时初始化。</p><pre><code><span class=\"cm\">// 常量声明</span>\n<span class=\"kw\">const</span> <span class=\"type\">double</span> PI = <span class=\"num\">3.1415926</span>;\n<span class=\"kw\">const</span> <span class=\"type\">int</span> MAX_SIZE = <span class=\"num\">100</span>;\n<span class=\"kw\">const</span> <span class=\"type\">string</span> APP_NAME = <span class=\"str\">\"C#学习系统\"</span>;\n\n<span class=\"cm\">// PI = 3.14;  // 编译错误！</span></code></pre><h4>const vs static readonly</h4><table><tr><th>特性</th><th>const</th><th>static readonly</th></tr><tr><td>初始化</td><td>编译时</td><td>运行时</td></tr><tr><td>类型</td><td>基本类型+string</td><td>任意类型</td></tr><tr><td>赋值</td><td>声明时</td><td>声明时或静态构造器</td></tr></table><pre><code><span class=\"kw\">const</span> <span class=\"type\">int</span> A = <span class=\"num\">10</span>;\n<span class=\"kw\">static</span> <span class=\"kw\">readonly</span> <span class=\"type\">DateTime</span> Now = <span class=\"type\">DateTime</span>.Now;</code></pre><div class=\"highlight-box\"><strong>小贴士：</strong>常量全大写+下划线：<code>MAX_VALUE</code>。避免\"魔法数字\"。</div>"
},
{
id: "intro-6",
title: "转义字符",
tags: ["转义","字符串"],
content: "<h4>什么是转义字符</h4><p>以反斜杠<code>\\</code>开头的特殊字符序列，表示特殊含义或无法直接输入的字符。</p><h4>常用转义字符</h4><table><tr><th>序列</th><th>含义</th><th>Unicode</th></tr><tr><td>\\n</td><td>换行</td><td>U+000A</td></tr><tr><td>\\r</td><td>回车</td><td>U+000D</td></tr><tr><td>\\t</td><td>水平制表</td><td>U+0009</td></tr><tr><td>\\\\</td><td>反斜杠</td><td>U+005C</td></tr><tr><td>\\'</td><td>单引号</td><td>U+0027</td></tr><tr><td>\\\"</td><td>双引号</td><td>U+0022</td></tr></table><pre><code><span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"str\">\"第一行\\n第二行\"</span>);\n\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"str\">\"姓名\\t年龄\"</span>);\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"str\">\"他说：\\\"你好！\\\"\"</span>);\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"str\">\"路径：C:\\\\Users\\\\Admin\"</span>);</code></pre><h4>@逐字字符串</h4><pre><code><span class=\"cm\">// @字符串中转义不生效</span>\n<span class=\"type\">string</span> path = <span class=\"str\">@\"C:\\Users\\Admin\\Desktop\"</span>;\n<span class=\"cm\">// 双引号用两个双引号表示</span>\n<span class=\"type\">string</span> q = <span class=\"str\">@\"他说：\"\"你好！\"\"\"</span>;</code></pre><div class=\"highlight-box\"><strong>小贴士：</strong>文件路径用@字符串最方便。多行SQL/JSON用@字符串也很合适。</div>"
},
{
id: "intro-7",
title: "类型转换—隐式转换",
tags: ["隐式转换","自动转换"],
content: "<h4>什么是隐式转换</h4><p>编译器自动完成的类型转换，不需要额外代码，保证数据不丢失精度。小范围类型可隐式转为大范围类型。</p><h4>转换规则</h4><pre><code><span class=\"type\">byte</span> -> <span class=\"type\">short</span> -> <span class=\"type\">int</span> -> <span class=\"type\">long</span> -> <span class=\"type\">float</span> -> <span class=\"type\">double</span>\n<span class=\"type\">char</span> -> <span class=\"type\">int</span> -> <span class=\"type\">long</span> -> <span class=\"type\">float</span> -> <span class=\"type\">double</span></code></pre><pre><code><span class=\"type\">int</span> a = <span class=\"num\">100</span>;\n<span class=\"type\">long</span> b = a;      <span class=\"cm\">// int->long</span>\n\n<span class=\"type\">float</span> f = <span class=\"num\">3.14f</span>;\n<span class=\"type\">double</span> d = f;    <span class=\"cm\">// float->double</span>\n\n<span class=\"type\">char</span> c = <span class=\"str\">'A'</span>;\n<span class=\"type\">int</span> code = c;    <span class=\"cm\">// char->int (code=65)</span>\n\n<span class=\"type\">byte</span> b1 = <span class=\"num\">10</span>;\n<span class=\"type\">int</span> result = b1 + <span class=\"num\">20</span>;  <span class=\"cm\">// byte提升为int</span></code></pre><h4>安全条件</h4><table><tr><th>源类型</th><th>可隐式转换为</th><th>原因</th></tr><tr><td>byte/short</td><td>int,long,float,double</td><td>范围全包含</td></tr><tr><td>int</td><td>long,float,double</td><td>范围全包含</td></tr><tr><td>float</td><td>double</td><td>精度更高</td></tr><tr><td>char</td><td>int,long,float,double</td><td>编码是整数</td></tr></table><div class=\"highlight-box\"><strong>小贴士：</strong>隐式转换只能从小到大。编译器：安全自动做，风险需显式。</div>"
},
{
id: "intro-8",
title: "类型转换—显式转换",
tags: ["显式转换","强制转换","Parse"],
content: "<h4>什么是显式转换</h4><p>需要程序员手动编写代码的转换。大转小可能丢失数据，必须显式指定。</p><h4>三种方式</h4><pre><code><span class=\"cm\">// 1. 强制转换 (类型)值</span>\n<span class=\"type\">double</span> d = <span class=\"num\">3.99</span>;\n<span class=\"type\">int</span> a = (<span class=\"type\">int</span>)d;  <span class=\"cm\">// a=3（截断）</span>\n\n<span class=\"cm\">// 2. Convert类（四舍五入）</span>\n<span class=\"type\">int</span> b = <span class=\"type\">Convert</span>.<span class=\"fn\">ToInt32</span>(<span class=\"num\">3.99</span>);  <span class=\"cm\">// b=4</span>\n\n<span class=\"cm\">// 3. Parse</span>\n<span class=\"type\">int</span> num = <span class=\"type\">int</span>.<span class=\"fn\">Parse</span>(<span class=\"str\">\"123\"</span>);  <span class=\"cm\">// num=123</span></code></pre><h4>对比</h4><table><tr><th>方式</th><th>浮点转整</th><th>字符串转数值</th></tr><tr><td>(int)</td><td>截断->3</td><td>不支持</td></tr><tr><td>Convert</td><td>四舍五入->4</td><td>支持</td></tr><tr><td>Parse</td><td>不支持</td><td>推荐</td></tr></table><pre><code><span class=\"cm\">// TryParse：安全转换</span>\n<span class=\"kw\">if</span> (<span class=\"type\">int</span>.<span class=\"fn\">TryParse</span>(input, <span class=\"kw\">out</span> <span class=\"type\">int</span> r))\n    <span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"str\">$\"成功：{r}\"</span>);\n<span class=\"kw\">else</span>\n    <span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"str\">\"失败\"</span>);</code></pre><div class=\"highlight-box\"><strong>小贴士：</strong>处理用户输入始终用<code>TryParse</code>避免崩溃。</div>"
},
{
id: "intro-9",
title: "异常捕获",
tags: ["try-catch","异常处理","finally"],
content: "<h4>什么是异常</h4><p>程序运行时发生的错误，不处理会导致崩溃。C#用try-catch-finally捕获处理。</p><h4>基本结构</h4><pre><code><span class=\"kw\">try</span>\n{\n    <span class=\"type\">int</span> a = <span class=\"num\">10</span>, b = <span class=\"num\">0</span>;\n    <span class=\"type\">int</span> r = a / b;  <span class=\"cm\">// 除零异常！</span>\n}\n<span class=\"kw\">catch</span> (<span class=\"type\">DivideByZeroException</span> ex)\n{\n    <span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"str\">\"除数不能为零\"</span>);\n}\n<span class=\"kw\">catch</span> (<span class=\"type\">Exception</span> ex)\n{\n    <span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"str\">$\"异常：{ex.Message}\"</span>);\n}\n<span class=\"kw\">finally</span>\n{\n    <span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"str\">\"finally始终执行\"</span>);\n}</code></pre><h4>常见异常</h4><table><tr><th>异常类型</th><th>触发场景</th></tr><tr><td>DivideByZeroException</td><td>除数为零</td></tr><tr><td>NullReferenceException</td><td>访问null对象</td></tr><tr><td>IndexOutOfRangeException</td><td>数组索引越界</td></tr><tr><td>FormatException</td><td>格式转换失败</td></tr><tr><td>OverflowException</td><td>数值溢出</td></tr><tr><td>FileNotFoundException</td><td>文件未找到</td></tr></table><div class=\"highlight-box\"><strong>小贴士：</strong>catch从具体到通用排列。finally用于清理资源。using语句可自动释放IDisposable资源。</div>"
},
{
id: "intro-10",
title: "算数运算符",
tags: ["运算符","算术"],
content: "<h4>基本算数运算符</h4><table><tr><th>运算符</th><th>名称</th><th>示例(a=10,b=3)</th></tr><tr><td>+</td><td>加</td><td>a+b=13</td></tr><tr><td>-</td><td>减</td><td>a-b=7</td></tr><tr><td>*</td><td>乘</td><td>a*b=30</td></tr><tr><td>/</td><td>除</td><td>a/b=3(整数截断)</td></tr><tr><td>%</td><td>取模</td><td>a%b=1</td></tr></table><pre><code><span class=\"type\">int</span> a = <span class=\"num\">10</span>, b = <span class=\"num\">3</span>;\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(a + b);  <span class=\"cm\">// 13</span>\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(a / b);  <span class=\"cm\">// 3</span>\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(a % b);  <span class=\"cm\">// 1</span>\n\n<span class=\"cm\">// 浮点除法</span>\n<span class=\"type\">double</span> c = <span class=\"num\">10.0</span> / <span class=\"num\">3</span>;  <span class=\"cm\">// 3.333</span>\n\n<span class=\"cm\">// 取模技巧</span>\n<span class=\"type\">int</span> d = <span class=\"num\">12345</span> % <span class=\"num\">10</span>;  <span class=\"cm\">// 个位5</span>\n<span class=\"type\">bool</span> isEven = n % <span class=\"num\">2</span> == <span class=\"num\">0</span>;  <span class=\"cm\">// 判偶数</span></code></pre><h4>自增自减</h4><table><tr><th>运算符</th><th>名称</th><th>说明</th></tr><tr><td>++</td><td>自增</td><td>变量加1</td></tr><tr><td>--</td><td>自减</td><td>变量减1</td></tr></table><pre><code><span class=\"type\">int</span> x = <span class=\"num\">5</span>;\nx++;  <span class=\"cm\">// 后置，x=6</span>\n++x;  <span class=\"cm\">// 前置，x=7</span>\n\n<span class=\"type\">int</span> m = <span class=\"num\">10</span>;\n<span class=\"type\">int</span> n = m++;  <span class=\"cm\">// n=10,m=11（先赋值）</span>\n<span class=\"type\">int</span> q = ++m;  <span class=\"cm\">// q=12,m=12（先自增）</span></code></pre><div class=\"highlight-box\"><strong>小贴士：</strong>整数除截断非四舍五入。浮点除确保至少一个操作数是浮点类型。</div>"
},
{
id: "intro-11",
title: "字符串拼接",
tags: ["字符串","拼接","StringBuilder"],
content: "<h4>字符串拼接的多种方式</h4><p>字符串是C#中使用最频繁的类型之一。</p><h4>方式1: +运算符</h4><pre><code><span class=\"type\">string</span> s1 = <span class=\"str\">\"Hello\"</span>;\n<span class=\"type\">string</span> s2 = <span class=\"str\">\"World\"</span>;\n<span class=\"type\">string</span> r = s1 + <span class=\"str\">\" \"</span> + s2;  <span class=\"cm\">// \"Hello World\"</span>\n\n<span class=\"cm\">// 注意：+号从左到右结合</span>\n<span class=\"type\">string</span> s = <span class=\"str\">\"结果\"</span> + <span class=\"num\">10</span> + <span class=\"num\">20</span>;   <span class=\"cm\">// \"结果1020\"</span>\n<span class=\"type\">string</span> s = <span class=\"str\">\"结果\"</span> + (<span class=\"num\">10</span> + <span class=\"num\">20</span>); <span class=\"cm\">// \"结果30\"</span></code></pre><h4>方式2: $字符串插值</h4><pre><code><span class=\"type\">string</span> name = <span class=\"str\">\"张三\"</span>;\n<span class=\"type\">int</span> age = <span class=\"num\">25</span>;\n<span class=\"type\">string</span> info = <span class=\"str\">$\"姓名：{name}，年龄：{age}\"</span>;\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"str\">$\"明年{name}就{age+1}岁了\"</span>);</code></pre><h4>方式3: StringBuilder</h4><pre><code><span class=\"type\">StringBuilder</span> sb = <span class=\"kw\">new</span> <span class=\"type\">StringBuilder</span>();\n<span class=\"kw\">for</span> (<span class=\"type\">int</span> i = <span class=\"num\">0</span>; i < <span class=\"num\">5</span>; i++)\n    sb.<span class=\"fn\">Append</span>(<span class=\"str\">$\"第{i}行\\n\"</span>);\n<span class=\"type\">string</span> result = sb.<span class=\"fn\">ToString</span>();</code></pre><h4>性能对比</h4><table><tr><th>方式</th><th>适用</th><th>性能</th></tr><tr><td>+</td><td>少量(<5次)</td><td>一般</td></tr><tr><td>$插值</td><td>含变量</td><td>好</td></tr><tr><td>StringBuilder</td><td>大量循环</td><td>优秀</td></tr></table><div class=\"highlight-box\"><strong>小贴士：</strong>循环拼接用StringBuilder（string不可变，+每次新建对象）。日常用$插值最方便。</div>"
},
{
id: "intro-12",
title: "条件运算符",
tags: ["条件运算符","比较"],
content: "<h4>条件运算符（比较运算符）</h4><p>用于比较两个值的大小或相等关系，结果为bool类型。</p><h4>六种条件运算符</h4><table><tr><th>运算符</th><th>含义</th><th>示例</th></tr><tr><td>==</td><td>等于</td><td>5==5 -> true</td></tr><tr><td>!=</td><td>不等于</td><td>5!=3 -> true</td></tr><tr><td>></td><td>大于</td><td>5>3 -> true</td></tr><tr><td><</td><td>小于</td><td>5<3 -> false</td></tr><tr><td>>=</td><td>大于等于</td><td>5>=5 -> true</td></tr><tr><td><=</td><td>小于等于</td><td>5<=3 -> false</td></tr></table><pre><code><span class=\"type\">int</span> a = <span class=\"num\">10</span>, b = <span class=\"num\">20</span>;\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(a == b);  <span class=\"cm\">// False</span>\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(a != b);  <span class=\"cm\">// True</span>\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(a > b);   <span class=\"cm\">// False</span>\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(a < b);   <span class=\"cm\">// True</span>\n\n<span class=\"cm\">// string比较区分大小写</span>\n<span class=\"type\">string</span> s1 = <span class=\"str\">\"abc\"</span>, s2 = <span class=\"str\">\"ABC\"</span>;\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(s1 == s2);  <span class=\"cm\">// False</span></code></pre><h4>== 和 = 的区别</h4><table><tr><th>符号</th><th>含义</th><th>用途</th></tr><tr><td>=</td><td>赋值</td><td>右边值赋给左边变量</td></tr><tr><td>==</td><td>等于比较</td><td>判断两边值是否相等</td></tr></table><div class=\"highlight-box\"><strong>小贴士：</strong>比较浮点数用<code>Math.Abs(a-b)<0.000001</code>代替==，避免精度误差。</div>"
},
{
id: "intro-13",
title: "逻辑运算符",
tags: ["逻辑","与或非"],
content: "<h4>逻辑运算符</h4><p>用于连接多个条件表达式，结果为bool。</p><h4>三种逻辑运算符</h4><table><tr><th>运算符</th><th>名称</th><th>规则</th></tr><tr><td>&&</td><td>逻辑与</td><td>两边都true才true</td></tr><tr><td>||</td><td>逻辑或</td><td>任一边true就true</td></tr><tr><td>!</td><td>逻辑非</td><td>取反</td></tr></table><pre><code><span class=\"type\">int</span> age = <span class=\"num\">20</span>;\n<span class=\"type\">bool</span> hasLicense = <span class=\"kw\">true</span>;\n\n<span class=\"kw\">if</span> (age >= <span class=\"num\">18</span> && hasLicense)\n    <span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"str\">\"可以开车\"</span>);\n\n<span class=\"type\">int</span> score = <span class=\"num\">45</span>;\n<span class=\"kw\">if</span> (score < <span class=\"num\">0</span> || score > <span class=\"num\">100</span>)\n    <span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"str\">\"成绩无效\"</span>);\n\n<span class=\"kw\">if</span> (!isRaining)\n    <span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"str\">\"不下雨\"</span>);</code></pre><h4>短路运算</h4><pre><code><span class=\"cm\">// &&短路：左边false，右边不执行</span>\n<span class=\"kw\">if</span> (<span class=\"kw\">false</span> && a++ > <span class=\"num\">0</span>)  <span class=\"cm\">// a++不会执行</span>\n\n<span class=\"cm\">// ||短路：左边true，右边不执行</span>\n<span class=\"kw\">if</span> (<span class=\"kw\">true</span> || b++ > <span class=\"num\">0</span>)  <span class=\"cm\">// b++不会执行</span></code></pre><h4>真值表</h4><table><tr><th>A</th><th>B</th><th>A&&B</th><th>A||B</th><th>!A</th></tr><tr><td>T</td><td>T</td><td>T</td><td>T</td><td>F</td></tr><tr><td>T</td><td>F</td><td>F</td><td>T</td><td>F</td></tr><tr><td>F</td><td>T</td><td>F</td><td>T</td><td>T</td></tr><tr><td>F</td><td>F</td><td>F</td><td>F</td><td>T</td></tr></table><div class=\"highlight-box\"><strong>小贴士：</strong><code>obj != null && obj.Value > 0</code>利用短路避免空引用异常。</div>"
},
{
id: "intro-14",
title: "位运算符",
tags: ["位运算","二进制"],
content: "<h4>位运算符</h4><p>直接对整数的二进制位操作，速度快，用于底层开发和权限管理。</p><h4>六种位运算符</h4><table><tr><th>运算符</th><th>名称</th><th>规则</th></tr><tr><td>&</td><td>按位与</td><td>都为1则为1</td></tr><tr><td>|</td><td>按位或</td><td>有1则为1</td></tr><tr><td>^</td><td>按位异或</td><td>不同为1</td></tr><tr><td>~</td><td>按位取反</td><td>0变1，1变0</td></tr><tr><td><<</td><td>左移</td><td>左边丢弃，右边补0</td></tr><tr><td>>></td><td>右移</td><td>右边丢弃，左边补符号位</td></tr></table><pre><code><span class=\"type\">int</span> a = <span class=\"num\">6</span>;  <span class=\"cm\">// 0110</span>\n<span class=\"type\">int</span> b = <span class=\"num\">3</span>;  <span class=\"cm\">// 0011</span>\n\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(a & b);   <span class=\"cm\">// 2  (0010)</span>\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(a | b);   <span class=\"cm\">// 7  (0111)</span>\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(a ^ b);   <span class=\"cm\">// 5  (0101)</span>\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(~a);      <span class=\"cm\">// -7</span>\n\n<span class=\"cm\">// 位移</span>\n<span class=\"type\">int</span> c = <span class=\"num\">8</span>;  <span class=\"cm\">// 1000</span>\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(c << <span class=\"num\">1</span>);  <span class=\"cm\">// 16 (乘2)</span>\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(c >> <span class=\"num\">2</span>);  <span class=\"cm\">// 2  (除4)</span></code></pre><h4>实用技巧</h4><pre><code><span class=\"cm\">// 判断奇偶</span>\n<span class=\"kw\">if</span> ((n & <span class=\"num\">1</span>) == <span class=\"num\">0</span>)  <span class=\"cm\">// 偶数</span>\n\n<span class=\"cm\">// 交换两数</span>\nx = x ^ y; y = x ^ y; x = x ^ y;\n\n<span class=\"cm\">// 权限管理</span>\n<span class=\"type\">int</span> READ = <span class=\"num\">1</span>, WRITE = <span class=\"num\">2</span>, EXE = <span class=\"num\">4</span>;\n<span class=\"type\">int</span> p = READ | WRITE;  <span class=\"cm\">// 设置</span>\n<span class=\"type\">bool</span> canRead = (p & READ) != <span class=\"num\">0</span>;  <span class=\"cm\">// 检查</span></code></pre><div class=\"highlight-box\"><strong>小贴士：</strong>区分位运算符（&,|）和逻辑运算符（&&,||）。位运算常用于图形处理、加密、权限系统。</div>"
},
{
id: "intro-15",
title: "三目运算符",
tags: ["三目","条件表达式"],
content: "<h4>三目运算符 ?:</h4><p>C#中唯一需要三个操作数的运算符，简洁替代简单的if-else。</p><h4>语法</h4><pre><code><span class=\"cm\">// 条件 ? 表达式1 : 表达式2</span>\n<span class=\"cm\">// true -> 表达式1, false -> 表达式2</span>\n\n<span class=\"type\">int</span> a = <span class=\"num\">10</span>, b = <span class=\"num\">20</span>;\n<span class=\"type\">int</span> max = a > b ? a : b;  <span class=\"cm\">// max=20</span>\n\n<span class=\"cm\">// 等价于：</span>\n<span class=\"type\">int</span> max2;\n<span class=\"kw\">if</span> (a > b) max2 = a; <span class=\"kw\">else</span> max2 = b;</code></pre><h4>常用场景</h4><pre><code><span class=\"cm\">// 绝对值</span>\n<span class=\"type\">int</span> abs = num >= <span class=\"num\">0</span> ? num : -num;\n\n<span class=\"cm\">// 默认值</span>\n<span class=\"type\">string</span> name = input != <span class=\"str\">\"\"</span> ? input : <span class=\"str\">\"匿名\"</span>;\n\n<span class=\"cm\">// 成绩判断</span>\n<span class=\"type\">string</span> level = score >= <span class=\"num\">60</span> ? <span class=\"str\">\"及格\"</span> : <span class=\"str\">\"不及格\"</span>;\n\n<span class=\"cm\">// 嵌套（谨慎使用）</span>\n<span class=\"type\">string</span> g = s >= <span class=\"num\">90</span> ? <span class=\"str\">\"A\"</span> : s >= <span class=\"num\">60</span> ? <span class=\"str\">\"C\"</span> : <span class=\"str\">\"D\"</span>;</code></pre><h4>三目 vs if-else</h4><table><tr><th>对比</th><th>三目</th><th>if-else</th></tr><tr><td>返回值</td><td>返回一个值</td><td>不返回值</td></tr><tr><td>位置</td><td>可嵌表达式中</td><td>必须独立成句</td></tr><tr><td>适用</td><td>简单二选一</td><td>复杂逻辑</td></tr></table><div class=\"highlight-box\"><strong>小贴士：</strong>三目简洁但别过度嵌套。超两层用if-else或switch更清晰。表达式1和2的类型必须兼容。</div>"
},
{
id: "intro-16",
title: "if语句",
tags: ["if","条件判断","分支"],
content: "<h4>if语句概述</h4><p>程序中最基本的分支控制结构，根据条件真假决定执行哪段代码。</p><h4>四种形式</h4><pre><code><span class=\"cm\">// 1.单分支</span>\n<span class=\"kw\">if</span> (条件) { <span class=\"cm\">/* true时执行 */</span> }\n\n<span class=\"cm\">// 2.双分支</span>\n<span class=\"kw\">if</span> (条件) { <span class=\"cm\">/* true */</span> } <span class=\"kw\">else</span> { <span class=\"cm\">/* false */</span> }\n\n<span class=\"cm\">// 3.多分支</span>\n<span class=\"kw\">if</span> (条件1) { }\n<span class=\"kw\">else</span> <span class=\"kw\">if</span> (条件2) { }\n<span class=\"kw\">else</span> { }\n\n<span class=\"cm\">// 4.嵌套</span>\n<span class=\"kw\">if</span> (条件1) { <span class=\"kw\">if</span> (条件2) { } }</code></pre><h4>完整示例：成绩等级</h4><pre><code><span class=\"type\">Console</span>.<span class=\"fn\">Write</span>(<span class=\"str\">\"请输入成绩：\"</span>);\n<span class=\"type\">int</span> score = <span class=\"type\">int</span>.<span class=\"fn\">Parse</span>(<span class=\"type\">Console</span>.<span class=\"fn\">ReadLine</span>());\n\n<span class=\"kw\">if</span> (score >= <span class=\"num\">90</span>)\n    <span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"str\">\"优秀\"</span>);\n<span class=\"kw\">else</span> <span class=\"kw\">if</span> (score >= <span class=\"num\">80</span>)\n    <span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"str\">\"良好\"</span>);\n<span class=\"kw\">else</span> <span class=\"kw\">if</span> (score >= <span class=\"num\">60</span>)\n    <span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"str\">\"及格\"</span>);\n<span class=\"kw\">else</span>\n    <span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"str\">\"不及格\"</span>);</code></pre><h4>注意事项</h4><table><tr><th>要点</th><th>说明</th></tr><tr><td>条件必须是bool</td><td>不能像C/C++用数值</td></tr><tr><td>建议用大括号</td><td>即使一行也加上</td></tr><tr><td>从上到下判断</td><td>匹配第一个true后停止</td></tr><tr><td>else配对</td><td>else总与最近的if配对</td></tr></table><div class=\"highlight-box\"><strong>小贴士：</strong>条件越严格的越放前面。多条件互斥且数量多时考虑用switch。</div>"
},
{
id: "intro-17",
title: "switch语句",
tags: ["switch","多分支","case"],
content: "<h4>switch语句概述</h4><p>多分支选择结构，根据表达式值选择执行不同代码块，比多层if-else更清晰。</p><h4>基本语法</h4><pre><code><span class=\"kw\">switch</span> (表达式)\n{\n    <span class=\"kw\">case</span> 值1:\n        <span class=\"cm\">// 代码块1</span>\n        <span class=\"kw\">break</span>;\n    <span class=\"kw\">case</span> 值2:\n        <span class=\"cm\">// 代码块2</span>\n        <span class=\"kw\">break</span>;\n    <span class=\"kw\">default</span>:\n        <span class=\"cm\">// 都不匹配时执行</span>\n        <span class=\"kw\">break</span>;\n}</code></pre><h4>完整示例：星期判断</h4><pre><code><span class=\"type\">Console</span>.<span class=\"fn\">Write</span>(<span class=\"str\">\"输入星期几(1-7)：\"</span>);\n<span class=\"type\">int</span> day = <span class=\"type\">int</span>.<span class=\"fn\">Parse</span>(<span class=\"type\">Console</span>.<span class=\"fn\">ReadLine</span>());\n\n<span class=\"kw\">switch</span> (day)\n{\n    <span class=\"kw\">case</span> <span class=\"num\">1</span>:\n        <span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"str\">\"星期一\"</span>); <span class=\"kw\">break</span>;\n    <span class=\"kw\">case</span> <span class=\"num\">2</span>:\n    <span class=\"kw\">case</span> <span class=\"num\">3</span>:\n    <span class=\"kw\">case</span> <span class=\"num\">4</span>:\n    <span class=\"kw\">case</span> <span class=\"num\">5</span>:\n        <span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"str\">\"工作日\"</span>); <span class=\"kw\">break</span>;\n    <span class=\"kw\">case</span> <span class=\"num\">6</span>:\n    <span class=\"kw\">case</span> <span class=\"num\">7</span>:\n        <span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"str\">\"周末\"</span>); <span class=\"kw\">break</span>;\n    <span class=\"kw\">default</span>:\n        <span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"str\">\"输入无效\"</span>); <span class=\"kw\">break</span>;\n}</code></pre><h4>重要规则</h4><table><tr><th>规则</th><th>说明</th></tr><tr><td>break不可省略</td><td>非空case必须有break</td></tr><tr><td>case可合并</td><td>多case共享代码块</td></tr><tr><td>default可选</td><td>建议保留处理意外值</td></tr><tr><td>C#7.0+</td><td>支持when条件和类型模式</td></tr></table><div class=\"highlight-box\"><strong>小贴士：</strong>判断同一变量的多个固定值用switch；判断范围或复杂条件用if-else。case值必须是编译时常量。</div>"
},
{
id: "intro-18",
title: "while循环",
tags: ["while","循环"],
content: "<h4>while循环概述</h4><p>最基础的循环结构，条件为true时重复执行代码块。适用于\"不知道循环次数但知道终止条件\"的场景。</p><h4>语法与执行流程</h4><pre><code><span class=\"cm\">// 语法：先判断，再执行</span>\n<span class=\"kw\">while</span> (条件)\n{\n    <span class=\"cm\">// 循环体</span>\n}\n\n<span class=\"cm\">// 流程：检查条件->true执行循环体->回到检查</span>\n<span class=\"cm\">// 条件一开始为false，循环体一次也不执行</span></code></pre><h4>应用示例</h4><pre><code><span class=\"cm\">// 输出1到5</span>\n<span class=\"type\">int</span> i = <span class=\"num\">1</span>;\n<span class=\"kw\">while</span> (i <= <span class=\"num\">5</span>)\n{\n    <span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"str\">$\"第{i}次\"</span>);\n    i++;  <span class=\"cm\">// 必须改变条件，否则死循环</span>\n}\n\n<span class=\"cm\">// 累加求和</span>\n<span class=\"type\">int</span> sum = <span class=\"num\">0</span>, n = <span class=\"num\">1</span>;\n<span class=\"kw\">while</span> (n <= <span class=\"num\">100</span>) { sum += n; n++; }\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(sum);  <span class=\"cm\">// 5050</span>\n\n<span class=\"cm\">// break和continue</span>\n<span class=\"type\">int</span> j = <span class=\"num\">0</span>;\n<span class=\"kw\">while</span> (<span class=\"kw\">true</span>)\n{\n    j++;\n    <span class=\"kw\">if</span> (j == <span class=\"num\">5</span>) <span class=\"kw\">break</span>;     <span class=\"cm\">// 跳出</span>\n    <span class=\"kw\">if</span> (j % <span class=\"num\">2</span> == <span class=\"num\">0</span>) <span class=\"kw\">continue</span>; <span class=\"cm\">// 跳过本次</span>\n    <span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(j);\n}</code></pre><div class=\"highlight-box\"><strong>小贴士：</strong>while三要素：初始化（循环外）、条件（while括号）、更新（循环内）。缺一可能死循环。已知次数用for更清晰。</div>"
},
{
id: "intro-19",
title: "do-while循环",
tags: ["do-while","循环"],
content: "<h4>do-while循环概述</h4><p>与while类似，但<strong>先执行一次循环体，再判断条件</strong>。无论条件是否为true，循环体至少执行一次。</p><h4>语法对比</h4><pre><code><span class=\"cm\">// while：先判断后执行</span>\n<span class=\"kw\">while</span> (条件) { <span class=\"cm\">// 可能一次也不执行</span> }\n\n<span class=\"cm\">// do-while：先执行后判断</span>\n<span class=\"kw\">do</span> { <span class=\"cm\">// 至少执行一次</span> } <span class=\"kw\">while</span> (条件);</code></pre><h4>对比示例</h4><pre><code><span class=\"cm\">// 条件一开始为false</span>\n<span class=\"type\">int</span> a = <span class=\"num\">10</span>;\n<span class=\"kw\">while</span> (a < <span class=\"num\">5</span>) { <span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"str\">\"while\"</span>); }  <span class=\"cm\">// 不执行</span>\n\n<span class=\"type\">int</span> b = <span class=\"num\">10</span>;\n<span class=\"kw\">do</span> { <span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"str\">\"do-while\"</span>); } <span class=\"kw\">while</span> (b < <span class=\"num\">5</span>);  <span class=\"cm\">// 执行一次！</span></code></pre><h4>典型应用</h4><pre><code><span class=\"cm\">// 用户输入验证</span>\n<span class=\"type\">string</span> password;\n<span class=\"kw\">do</span>\n{\n    <span class=\"type\">Console</span>.<span class=\"fn\">Write</span>(<span class=\"str\">\"请输入密码：\"</span>);\n    password = <span class=\"type\">Console</span>.<span class=\"fn\">ReadLine</span>();\n} <span class=\"kw\">while</span> (password != <span class=\"str\">\"123456\"</span>);\n\n<span class=\"cm\">// 游戏循环</span>\n<span class=\"type\">string</span> choice;\n<span class=\"kw\">do</span>\n{\n    <span class=\"cm\">// ... 游戏逻辑 ...</span>\n    <span class=\"type\">Console</span>.<span class=\"fn\">Write</span>(<span class=\"str\">\"继续？(y/n)：\"</span>);\n    choice = <span class=\"type\">Console</span>.<span class=\"fn\">ReadLine</span>();\n} <span class=\"kw\">while</span> (choice == <span class=\"str\">\"y\"</span>);</code></pre><div class=\"highlight-box\"><strong>小贴士：</strong>do-while的while后面<strong>必须有分号</strong>。适用于菜单驱动、输入验证等\"至少一次\"的场景。</div>"
},
{
id: "intro-20",
title: "for循环",
tags: ["for","循环","迭代"],
content: "<h4>for循环概述</h4><p>功能最强的循环结构，将初始化、判断、更新集中在一行，适合已知次数的循环。</p><h4>语法结构</h4><pre><code><span class=\"cm\">// for(初始化; 条件; 迭代)</span>\n<span class=\"kw\">for</span> (<span class=\"type\">int</span> i = <span class=\"num\">0</span>; i < <span class=\"num\">10</span>; i++)\n{\n    <span class=\"cm\">// 循环体</span>\n}\n<span class=\"cm\">// 流程：初始化->判断->执行体->迭代->判断...</span></code></pre><h4>基础示例</h4><pre><code><span class=\"cm\">// 输出0到9</span>\n<span class=\"kw\">for</span> (<span class=\"type\">int</span> i = <span class=\"num\">0</span>; i < <span class=\"num\">10</span>; i++)\n    <span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"str\">$\"数字：{i}\"</span>);\n\n<span class=\"cm\">// 计算1+2+...+100</span>\n<span class=\"type\">int</span> sum = <span class=\"num\">0</span>;\n<span class=\"kw\">for</span> (<span class=\"type\">int</span> i = <span class=\"num\">1</span>; i <= <span class=\"num\">100</span>; i++)\n    sum += i;\n\n<span class=\"cm\">// 99乘法表</span>\n<span class=\"kw\">for</span> (<span class=\"type\">int</span> i = <span class=\"num\">1</span>; i <= <span class=\"num\">9</span>; i++)\n{\n    <span class=\"kw\">for</span> (<span class=\"type\">int</span> j = <span class=\"num\">1</span>; j <= i; j++)\n        <span class=\"type\">Console</span>.<span class=\"fn\">Write</span>(<span class=\"str\">$\"{j}x{i}={i*j}\\t\"</span>);\n    <span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>();\n}</code></pre><h4>灵活用法</h4><pre><code><span class=\"cm\">// 递减</span>\n<span class=\"kw\">for</span> (<span class=\"type\">int</span> i = <span class=\"num\">10</span>; i >= <span class=\"num\">1</span>; i--)\n\n<span class=\"cm\">// 多变量</span>\n<span class=\"kw\">for</span> (<span class=\"type\">int</span> i = <span class=\"num\">0</span>, j = <span class=\"num\">10</span>; i < j; i++, j--)\n\n<span class=\"cm\">// 遍历数组</span>\n<span class=\"kw\">for</span> (<span class=\"type\">int</span> i = <span class=\"num\">0</span>; i < arr.Length; i++)</code></pre><h4>三种循环对比</h4><table><tr><th>循环</th><th>适用</th><th>最小次数</th></tr><tr><td>for</td><td>已知次数</td><td>0</td></tr><tr><td>while</td><td>未知次数，知终止条件</td><td>0</td></tr><tr><td>do-while</td><td>至少执行一次</td><td>1</td></tr></table><div class=\"highlight-box\"><strong>小贴士：</strong>for三个表达式都可省略：<code>for(;;)</code>是无限循环。break提前跳出，continue跳过当次。</div>"
},
{
id: "intro-21",
title: "综合练习",
tags: ["综合","练习","复习"],
content: "<h4>综合练习——巩固入门知识</h4><p>以下练习涵盖模块一的核心知识点，建议动手实践。</p><h4>练习1：计算器</h4><pre><code><span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"str\">\"=====简易计算器=====\"</span>);\n<span class=\"type\">Console</span>.<span class=\"fn\">Write</span>(<span class=\"str\">\"输入第一个数字：\"</span>);\n<span class=\"type\">double</span> n1 = <span class=\"type\">double</span>.<span class=\"fn\">Parse</span>(<span class=\"type\">Console</span>.<span class=\"fn\">ReadLine</span>());\n<span class=\"type\">Console</span>.<span class=\"fn\">Write</span>(<span class=\"str\">\"输入运算符(+-*/)：\"</span>);\n<span class=\"type\">string</span> op = <span class=\"type\">Console</span>.<span class=\"fn\">ReadLine</span>();\n<span class=\"type\">Console</span>.<span class=\"fn\">Write</span>(<span class=\"str\">\"输入第二个数字：\"</span>);\n<span class=\"type\">double</span> n2 = <span class=\"type\">double</span>.<span class=\"fn\">Parse</span>(<span class=\"type\">Console</span>.<span class=\"fn\">ReadLine</span>());\n<span class=\"type\">double</span> r = <span class=\"num\">0</span>;\n<span class=\"kw\">switch</span> (op)\n{\n    <span class=\"kw\">case</span> <span class=\"str\">\"+\"</span>: r = n1 + n2; <span class=\"kw\">break</span>;\n    <span class=\"kw\">case</span> <span class=\"str\">\"-\"</span>: r = n1 - n2; <span class=\"kw\">break</span>;\n    <span class=\"kw\">case</span> <span class=\"str\">\"*\"</span>: r = n1 * n2; <span class=\"kw\">break</span>;\n    <span class=\"kw\">case</span> <span class=\"str\">\"/\"</span>: <span class=\"kw\">if</span> (n2 != <span class=\"num\">0</span>) r = n1 / n2; <span class=\"kw\">else</span> <span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"str\">\"除数不能为0\"</span>); <span class=\"kw\">break</span>;\n}\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"str\">$\"结果：{n1}{op}{n2}={r}\"</span>);</code></pre><h4>练习2：猜数字游戏</h4><pre><code><span class=\"type\">Random</span> random = <span class=\"kw\">new</span> <span class=\"type\">Random</span>();\n<span class=\"type\">int</span> target = random.<span class=\"fn\">Next</span>(<span class=\"num\">1</span>, <span class=\"num\">101</span>);\n<span class=\"type\">int</span> guess, count = <span class=\"num\">0</span>;\n<span class=\"kw\">do</span>\n{\n    <span class=\"type\">Console</span>.<span class=\"fn\">Write</span>(<span class=\"str\">\"猜1-100的数字：\"</span>);\n    guess = <span class=\"type\">int</span>.<span class=\"fn\">Parse</span>(<span class=\"type\">Console</span>.<span class=\"fn\">ReadLine</span>());\n    count++;\n    <span class=\"kw\">if</span> (guess > target) <span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"str\">\"太大了\"</span>);\n    <span class=\"kw\">else</span> <span class=\"kw\">if</span> (guess < target) <span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"str\">\"太小了\"</span>);\n} <span class=\"kw\">while</span> (guess != target);\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"str\">$\"恭喜！猜了{count}次\"</span>);</code></pre><h4>练习3：打印菱形</h4><pre><code><span class=\"type\">int</span> n = <span class=\"num\">5</span>;\n<span class=\"kw\">for</span> (<span class=\"type\">int</span> i = <span class=\"num\">1</span>; i <= n; i++)\n{\n    <span class=\"kw\">for</span> (<span class=\"type\">int</span> j = <span class=\"num\">0</span>; j < n-i; j++) <span class=\"type\">Console</span>.<span class=\"fn\">Write</span>(<span class=\"str\">\" \"</span>);\n    <span class=\"kw\">for</span> (<span class=\"type\">int</span> j = <span class=\"num\">0</span>; j < <span class=\"num\">2</span>*i-<span class=\"num\">1</span>; j++) <span class=\"type\">Console</span>.<span class=\"fn\">Write</span>(<span class=\"str\">\"*\"</span>);\n    <span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>();\n}\n<span class=\"kw\">for</span> (<span class=\"type\">int</span> i = n-<span class=\"num\">1</span>; i >= <span class=\"num\">1</span>; i--)\n{\n    <span class=\"kw\">for</span> (<span class=\"type\">int</span> j = <span class=\"num\">0</span>; j < n-i; j++) <span class=\"type\">Console</span>.<span class=\"fn\">Write</span>(<span class=\"str\">\" \"</span>);\n    <span class=\"kw\">for</span> (<span class=\"type\">int</span> j = <span class=\"num\">0</span>; j < <span class=\"num\">2</span>*i-<span class=\"num\">1</span>; j++) <span class=\"type\">Console</span>.<span class=\"fn\">Write</span>(<span class=\"str\">\"*\"</span>);\n    <span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>();\n}</code></pre><h4>练习4：水仙花数</h4><pre><code><span class=\"cm\">// 找出100-999中各位数字立方和等于该数的数</span>\n<span class=\"kw\">for</span> (<span class=\"type\">int</span> i = <span class=\"num\">100</span>; i <= <span class=\"num\">999</span>; i++)\n{\n    <span class=\"type\">int</span> a = i / <span class=\"num\">100</span>;       <span class=\"cm\">// 百位</span>\n    <span class=\"type\">int</span> b = i / <span class=\"num\">10</span> % <span class=\"num\">10</span>;   <span class=\"cm\">// 十位</span>\n    <span class=\"type\">int</span> c = i % <span class=\"num\">10</span>;        <span class=\"cm\">// 个位</span>\n    <span class=\"kw\">if</span> (a*a*a + b*b*b + c*c*c == i)\n        <span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(i);  <span class=\"cm\">// 153,370,371,407</span>\n}</code></pre><div class=\"highlight-box\"><strong>小贴士：</strong>善用try-catch防止输入崩溃，Console.Clear()清屏提升体验。断点调试(F9设断点,F5启动,F10逐过程,F11逐语句)是最重要的调试技能。</div>"
}
]
},
{
id: "basic",
title: "二、C# 基础教学",
desc: "深入理解C#核心概念",
badge: "基础",
lessons: [
{
id: "basic-1",
title: "枚举",
tags: ["enum","枚举"],
content: "<h4>什么是枚举</h4><p>枚举（enum）是一组命名整型常量的集合，用于定义一组有意义的可取值，让代码更具可读性。</p><h4>定义与使用</h4><pre><code><span class=\"cm\">// 定义枚举（通常放在namespace下）</span>\n<span class=\"kw\">enum</span> <span class=\"type\">Season</span>\n{\n    Spring,    <span class=\"cm\">// 默认值0</span>\n    Summer,    <span class=\"cm\">// 1</span>\n    Autumn,    <span class=\"cm\">// 2</span>\n    Winter     <span class=\"cm\">// 3</span>\n}\n\n<span class=\"cm\">// 使用枚举</span>\n<span class=\"type\">Season</span> current = <span class=\"type\">Season</span>.Summer;\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(current);  <span class=\"cm\">// 输出：Summer</span>\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>((<span class=\"type\">int</span>)current);  <span class=\"cm\">// 输出：1</span></code></pre><h4>自定义枚举值</h4><pre><code><span class=\"kw\">enum</span> <span class=\"type\">HttpStatus</span>\n{\n    OK = <span class=\"num\">200</span>,\n    NotFound = <span class=\"num\">404</span>,\n    ServerError = <span class=\"num\">500</span>\n}\n\n<span class=\"kw\">enum</span> <span class=\"type\">Days</span>\n{\n    Monday = <span class=\"num\">1</span>,\n    Tuesday,     <span class=\"cm\">// 自动为2</span>\n    Wednesday,   <span class=\"cm\">// 3</span>\n    Sunday = <span class=\"num\">7</span>\n}</code></pre><h4>枚举转换</h4><pre><code><span class=\"cm\">// string转enum</span>\n<span class=\"type\">Season</span> s = (<span class=\"type\">Season</span>)<span class=\"type\">Enum</span>.<span class=\"fn\">Parse</span>(<span class=\"kw\">typeof</span>(<span class=\"type\">Season</span>), <span class=\"str\">\"Summer\"</span>);\n\n<span class=\"cm\">// int转enum</span>\n<span class=\"type\">Season</span> s2 = (<span class=\"type\">Season</span>)<span class=\"num\">2</span>;  <span class=\"cm\">// Autumn</span>\n\n<span class=\"cm\">// 获取所有枚举名</span>\n<span class=\"type\">string</span>[] names = <span class=\"type\">Enum</span>.<span class=\"fn\">GetNames</span>(<span class=\"kw\">typeof</span>(<span class=\"type\">Season</span>));</code></pre><div class=\"highlight-box\"><strong>小贴士：</strong>枚举底层类型默认为int，可改为byte等：<code>enum Days : byte { ... }</code>。枚举让代码避免\"魔法数字\"，提高可读性和类型安全。</div>"
},
{
id: "basic-2",
title: "一维数组",
tags: ["数组","Array"],
content: "<h4>什么是数组</h4><p>数组是存储固定数量、相同类型元素的集合，通过索引访问。C#中数组是引用类型，存储在堆上。</p><h4>数组的四种声明方式</h4><pre><code><span class=\"cm\">// 方式1：先声明后赋值</span>\n<span class=\"type\">int</span>[] arr1 = <span class=\"kw\">new</span> <span class=\"type\">int</span>[<span class=\"num\">5</span>];\narr1[<span class=\"num\">0</span>] = <span class=\"num\">10</span>; arr1[<span class=\"num\">1</span>] = <span class=\"num\">20</span>;\n\n<span class=\"cm\">// 方式2：声明时初始化</span>\n<span class=\"type\">int</span>[] arr2 = <span class=\"kw\">new</span> <span class=\"type\">int</span>[<span class=\"num\">5</span>] { <span class=\"num\">1</span>, <span class=\"num\">2</span>, <span class=\"num\">3</span>, <span class=\"num\">4</span>, <span class=\"num\">5</span> };\n\n<span class=\"cm\">// 方式3：简写（省略new和大小）</span>\n<span class=\"type\">int</span>[] arr3 = { <span class=\"num\">1</span>, <span class=\"num\">2</span>, <span class=\"num\">3</span>, <span class=\"num\">4</span>, <span class=\"num\">5</span> };\n\n<span class=\"cm\">// 方式4：var推断</span>\n<span class=\"kw\">var</span> arr4 = <span class=\"kw\">new</span>[] { <span class=\"num\">1</span>, <span class=\"num\">2</span>, <span class=\"num\">3</span> };</code></pre><h4>数组的遍历</h4><pre><code><span class=\"type\">int</span>[] nums = { <span class=\"num\">10</span>, <span class=\"num\">20</span>, <span class=\"num\">30</span>, <span class=\"num\">40</span>, <span class=\"num\">50</span> };\n\n<span class=\"cm\">// for循环</span>\n<span class=\"kw\">for</span> (<span class=\"type\">int</span> i = <span class=\"num\">0</span>; i < nums.Length; i++)\n    <span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(nums[i]);\n\n<span class=\"cm\">// foreach循环（推荐）</span>\n<span class=\"kw\">foreach</span> (<span class=\"type\">int</span> n <span class=\"kw\">in</span> nums)\n    <span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(n);</code></pre><h4>数组常用操作</h4><pre><code><span class=\"type\">int</span>[] arr = { <span class=\"num\">3</span>, <span class=\"num\">1</span>, <span class=\"num\">4</span>, <span class=\"num\">2</span>, <span class=\"num\">5</span> };\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(arr.Length);   <span class=\"cm\">// 长度：5</span>\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(arr.Max());    <span class=\"cm\">// 最大值：5</span>\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(arr.Min());    <span class=\"cm\">// 最小值：1</span>\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(arr.Sum());    <span class=\"cm\">// 求和：15</span>\n<span class=\"type\">Array</span>.<span class=\"fn\">Sort</span>(arr);                 <span class=\"cm\">// 排序</span>\n<span class=\"type\">Array</span>.<span class=\"fn\">Reverse</span>(arr);              <span class=\"cm\">// 反转</span></code></pre><div class=\"highlight-box\"><strong>小贴士：</strong>数组长度固定，声明后不能改变。需要动态大小用<code>List&lt;T&gt;</code>。访问越界会抛<code>IndexOutOfRangeException</code>。</div>"
},
{
id: "basic-3",
title: "二维数组",
tags: ["二维数组","多维数组"],
content: "<h4>什么是二维数组</h4><p>二维数组可以理解为\"数组的数组\"，用两个索引访问元素，常用于存储矩阵、表格等数据。</p><h4>声明与初始化</h4><pre><code><span class=\"cm\">// 声明3行2列的二维数组</span>\n<span class=\"type\">int</span>[,] matrix = <span class=\"kw\">new</span> <span class=\"type\">int</span>[<span class=\"num\">3</span>, <span class=\"num\">2</span>];\nmatrix[<span class=\"num\">0</span>, <span class=\"num\">0</span>] = <span class=\"num\">1</span>; matrix[<span class=\"num\">0</span>, <span class=\"num\">1</span>] = <span class=\"num\">2</span>;\nmatrix[<span class=\"num\">1</span>, <span class=\"num\">0</span>] = <span class=\"num\">3</span>; matrix[<span class=\"num\">1</span>, <span class=\"num\">1</span>] = <span class=\"num\">4</span>;\nmatrix[<span class=\"num\">2</span>, <span class=\"num\">0</span>] = <span class=\"num\">5</span>; matrix[<span class=\"num\">2</span>, <span class=\"num\">1</span>] = <span class=\"num\">6</span>;\n\n<span class=\"cm\">// 声明时直接初始化</span>\n<span class=\"type\">int</span>[,] matrix2 = <span class=\"kw\">new</span> <span class=\"type\">int</span>[<span class=\"num\">3</span>, <span class=\"num\">2</span>] { { <span class=\"num\">1</span>, <span class=\"num\">2</span> }, { <span class=\"num\">3</span>, <span class=\"num\">4</span> }, { <span class=\"num\">5</span>, <span class=\"num\">6</span> } };\n\n<span class=\"cm\">// 简写形式</span>\n<span class=\"type\">int</span>[,] matrix3 = { { <span class=\"num\">1</span>, <span class=\"num\">2</span> }, { <span class=\"num\">3</span>, <span class=\"num\">4</span> }, { <span class=\"num\">5</span>, <span class=\"num\">6</span> } };</code></pre><h4>遍历二维数组</h4><pre><code><span class=\"type\">int</span>[,] m = { { <span class=\"num\">1</span>, <span class=\"num\">2</span> }, { <span class=\"num\">3</span>, <span class=\"num\">4</span> }, { <span class=\"num\">5</span>, <span class=\"num\">6</span> } };\n<span class=\"type\">int</span> rows = m.<span class=\"fn\">GetLength</span>(<span class=\"num\">0</span>);  <span class=\"cm\">// 行数：3</span>\n<span class=\"type\">int</span> cols = m.<span class=\"fn\">GetLength</span>(<span class=\"num\">1</span>);  <span class=\"cm\">// 列数：2</span>\n\n<span class=\"kw\">for</span> (<span class=\"type\">int</span> i = <span class=\"num\">0</span>; i < rows; i++)\n{\n    <span class=\"kw\">for</span> (<span class=\"type\">int</span> j = <span class=\"num\">0</span>; j < cols; j++)\n        <span class=\"type\">Console</span>.<span class=\"fn\">Write</span>(<span class=\"str\">$\"{m[i,j]}\\t\"</span>);\n    <span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>();\n}</code></pre><h4>一维 vs 二维</h4><table><tr><th>特性</th><th>一维int[]</th><th>二维int[,]</th></tr><tr><td>访问</td><td>arr[i]</td><td>mat[i,j]</td></tr><tr><td>长度</td><td>arr.Length</td><td>mat.GetLength(0/1)</td></tr><tr><td>声明</td><td>int[] arr</td><td>int[,] mat</td></tr></table><div class=\"highlight-box\"><strong>小贴士：</strong>二维数组在内存中按行优先存储，适合矩形数据。不规则结构用交错数组。三维及以上数组中括号里加逗号如<code>int[,,]</code>。</div>"
},
{
id: "basic-4",
title: "交错数组",
tags: ["交错数组","jagged"],
content: "<h4>什么是交错数组</h4><p>交错数组（jagged array）是\"数组的数组\"，即每个元素本身也是一个数组，且每个子数组长度可以不同。</p><h4>声明与初始化</h4><pre><code><span class=\"cm\">// 声明交错数组：3行，每行长度待定</span>\n<span class=\"type\">int</span>[][] jagged = <span class=\"kw\">new</span> <span class=\"type\">int</span>[<span class=\"num\">3</span>][];\njagged[<span class=\"num\">0</span>] = <span class=\"kw\">new</span> <span class=\"type\">int</span>[] { <span class=\"num\">1</span>, <span class=\"num\">2</span> };\njagged[<span class=\"num\">1</span>] = <span class=\"kw\">new</span> <span class=\"type\">int</span>[] { <span class=\"num\">3</span>, <span class=\"num\">4</span>, <span class=\"num\">5</span> };\njagged[<span class=\"num\">2</span>] = <span class=\"kw\">new</span> <span class=\"type\">int</span>[] { <span class=\"num\">6</span> };\n\n<span class=\"cm\">// 声明时完全初始化</span>\n<span class=\"type\">int</span>[][] jagged2 =\n{\n    <span class=\"kw\">new</span>[] { <span class=\"num\">1</span>, <span class=\"num\">2</span>, <span class=\"num\">3</span> },\n    <span class=\"kw\">new</span>[] { <span class=\"num\">4</span>, <span class=\"num\">5</span> },\n    <span class=\"kw\">new</span>[] { <span class=\"num\">6</span>, <span class=\"num\">7</span>, <span class=\"num\">8</span>, <span class=\"num\">9</span> }\n};</code></pre><h4>遍历交错数组</h4><pre><code><span class=\"kw\">for</span> (<span class=\"type\">int</span> i = <span class=\"num\">0</span>; i < jagged.Length; i++)\n{\n    <span class=\"kw\">for</span> (<span class=\"type\">int</span> j = <span class=\"num\">0</span>; j < jagged[i].Length; j++)\n        <span class=\"type\">Console</span>.<span class=\"fn\">Write</span>(<span class=\"str\">$\"{jagged[i][j]} \"</span>);\n    <span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>();\n}\n<span class=\"cm\">// 输出：</span>\n<span class=\"cm\">// 1 2</span>\n<span class=\"cm\">// 3 4 5</span>\n<span class=\"cm\">// 6</span></code></pre><h4>交错数组 vs 二维数组</h4><table><tr><th>特性</th><th>交错数组int[][]</th><th>二维数组int[,]</th></tr><tr><td>每行长度</td><td>可以不同</td><td>必须相同</td></tr><tr><td>访问语法</td><td>jag[i][j]</td><td>mat[i,j]</td></tr><tr><td>内存布局</td><td>各行独立分配</td><td>连续内存块</td></tr><tr><td>访问速度</td><td>稍快(CLR优化)</td><td>稍慢</td></tr><tr><td>初始化</td><td>new int[3][]</td><td>new int[3,2]</td></tr></table><div class=\"highlight-box\"><strong>小贴士：</strong>数据各行列数不一时用交错数组；矩阵运算等规则数据用二维数组。交错数组的<code>Length</code>是行数，每行长度用<code>jag[i].Length</code>。</div>"
},
{
id: "basic-5",
title: "值类型和引用类型",
tags: ["值类型","引用类型","内存"],
content: "<h4>核心区别</h4><p>C#的类型系统分为值类型和引用类型两大类，理解它们的区别对写出正确高效的代码至关重要。</p><h4>对比总览</h4><table><tr><th>特性</th><th>值类型</th><th>引用类型</th></tr><tr><td>存储位置</td><td>栈（局部变量）</td><td>堆（对象数据）</td></tr><tr><td>赋值行为</td><td>复制整个值</td><td>复制引用地址</td></tr><tr><td>默认值</td><td>0/false等</td><td>null</td></tr><tr><td>继承自</td><td>System.ValueType</td><td>System.Object</td></tr><tr><td>包含类型</td><td>int,bool,struct,enum等</td><td>class,string,array等</td></tr><tr><td>可为null</td><td>需要?后缀</td><td>默认可为null</td></tr></table><h4>赋值行为演示</h4><pre><code><span class=\"cm\">// 值类型：修改副本不影响原值</span>\n<span class=\"type\">int</span> a = <span class=\"num\">10</span>;\n<span class=\"type\">int</span> b = a;  <span class=\"cm\">// b得到a的副本</span>\nb = <span class=\"num\">20</span>;\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(a);  <span class=\"cm\">// 10（未变）</span>\n\n<span class=\"cm\">// 引用类型：修改通过任意引用影响原对象</span>\n<span class=\"type\">int</span>[] arr1 = { <span class=\"num\">1</span>, <span class=\"num\">2</span> };\n<span class=\"type\">int</span>[] arr2 = arr1;  <span class=\"cm\">// arr2指向同一数组</span>\narr2[<span class=\"num\">0</span>] = <span class=\"num\">99</span>;\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(arr1[<span class=\"num\">0</span>]);  <span class=\"cm\">// 99（变了！）</span></code></pre><h4>方法传参的区别</h4><pre><code><span class=\"cm\">// 值类型传参：传递副本，方法内修改不影响外部</span>\n<span class=\"kw\">static</span> <span class=\"type\">void</span> <span class=\"fn\">ChangeInt</span>(<span class=\"type\">int</span> x) { x = <span class=\"num\">100</span>; }\n\n<span class=\"cm\">// 引用类型传参：传递引用，方法内修改影响外部</span>\n<span class=\"kw\">static</span> <span class=\"type\">void</span> <span class=\"fn\">ChangeArray</span>(<span class=\"type\">int</span>[] arr) { arr[<span class=\"num\">0</span>] = <span class=\"num\">100</span>; }</code></pre><div class=\"highlight-box\"><strong>小贴士：</strong>结构体(struct)是值类型，类(class)是引用类型。需要语义上\"复制独立\"选值类型，需要\"共享同一对象\"选引用类型。</div>"
},
{
id: "basic-6",
title: "特殊引用类型string",
tags: ["string","不可变性","字符串池"],
content: "<h4>string的特殊性</h4><p>string虽然是引用类型，但表现出很多值类型的特性。它是<strong>不可变的</strong>，一旦创建就不能修改。</p><h4>不可变性演示</h4><pre><code><span class=\"type\">string</span> s1 = <span class=\"str\">\"Hello\"</span>;\n<span class=\"type\">string</span> s2 = s1;\ns1 = s1 + <span class=\"str\">\" World\"</span>;  <span class=\"cm\">// 创建了新字符串！</span>\n\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(s1);  <span class=\"cm\">// \"Hello World\"</span>\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(s2);  <span class=\"cm\">// \"Hello\"（未变！）</span></code></pre><h4>string的\"值类型\"行为</h4><pre><code><span class=\"cm\">// == 比较的是内容而非地址</span>\n<span class=\"type\">string</span> a = <span class=\"str\">\"abc\"</span>;\n<span class=\"type\">string</span> b = <span class=\"str\">\"abc\"</span>;\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(a == b);  <span class=\"cm\">// true（内容相同）</span>\n\n<span class=\"cm\">// 字符串池（interning）</span>\n<span class=\"type\">string</span> x = <span class=\"str\">\"Hello\"</span>;\n<span class=\"type\">string</span> y = <span class=\"str\">\"Hello\"</span>;\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"kw\">object</span>.<span class=\"fn\">ReferenceEquals</span>(x, y));  <span class=\"cm\">// true!</span></code></pre><h4>string vs 其他引用类型</h4><table><tr><th>行为</th><th>普通引用类型</th><th>string</th></tr><tr><td>==比较</td><td>比较引用地址</td><td>比较内容</td></tr><tr><td>修改操作</td><td>修改原对象</td><td>创建新对象</td></tr><tr><td>字符串池</td><td>无</td><td>编译时常量自动入池</td></tr><tr><td>方法传参修改</td><td>影响原对象</td><td>不影响（重赋值创建新对象）</td></tr></table><h4>StringBuilder</h4><pre><code><span class=\"cm\">// 大量字符串修改用StringBuilder</span>\n<span class=\"type\">StringBuilder</span> sb = <span class=\"kw\">new</span> <span class=\"type\">StringBuilder</span>(<span class=\"str\">\"Hello\"</span>);\nsb.<span class=\"fn\">Append</span>(<span class=\"str\">\" World\"</span>);\nsb.<span class=\"fn\">Replace</span>(<span class=\"str\">\"World\"</span>, <span class=\"str\">\"C#\"</span>);\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(sb.<span class=\"fn\">ToString</span>());  <span class=\"cm\">// \"Hello C#\"</span></code></pre><div class=\"highlight-box\"><strong>小贴士：</strong>string的不可变性保证了线程安全和字符串池的效率。频繁修改字符串用StringBuilder。密码等敏感信息用SecureString。</div>"
},
{
id: "basic-7",
title: "函数",
tags: ["函数","方法","参数","返回值"],
content: "<h4>什么是函数</h4><p>函数（方法）是一段可重复调用的代码块，用于组织代码、避免重复、提高可维护性。</p><h4>函数定义语法</h4><pre><code><span class=\"cm\">// 语法：访问修饰符 返回类型 函数名(参数列表) { 函数体 }</span>\n\n<span class=\"cm\">// 无返回值函数</span>\n<span class=\"kw\">static</span> <span class=\"type\">void</span> <span class=\"fn\">SayHello</span>()\n{\n    <span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"str\">\"Hello!\"</span>);\n}\n\n<span class=\"cm\">// 有返回值函数</span>\n<span class=\"kw\">static</span> <span class=\"type\">int</span> <span class=\"fn\">Add</span>(<span class=\"type\">int</span> a, <span class=\"type\">int</span> b)\n{\n    <span class=\"kw\">return</span> a + b;\n}\n\n<span class=\"cm\">// 有参数无返回值</span>\n<span class=\"kw\">static</span> <span class=\"type\">void</span> <span class=\"fn\">PrintInfo</span>(<span class=\"type\">string</span> name, <span class=\"type\">int</span> age)\n{\n    <span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"str\">$\"姓名：{name}，年龄：{age}\"</span>);\n}</code></pre><h4>函数调用</h4><pre><code><span class=\"fn\">SayHello</span>();  <span class=\"cm\">// 无参数调用</span>\n<span class=\"type\">int</span> sum = <span class=\"fn\">Add</span>(<span class=\"num\">10</span>, <span class=\"num\">20</span>);  <span class=\"cm\">// 有返回值调用</span>\n<span class=\"fn\">PrintInfo</span>(<span class=\"str\">\"张三\"</span>, <span class=\"num\">25</span>);  <span class=\"cm\">// 带参数调用</span></code></pre><h4>返回值类型</h4><table><tr><th>返回类型</th><th>说明</th><th>示例</th></tr><tr><td>void</td><td>无返回值</td><td>static void Print()</td></tr><tr><td>基本类型</td><td>返回int,double等</td><td>static int Add()</td></tr><tr><td>引用类型</td><td>返回对象</td><td>static string GetName()</td></tr><tr><td>数组/集合</td><td>返回数组</td><td>static int[] GetArray()</td></tr><tr><td>bool</td><td>判断函数</td><td>static bool IsValid()</td></tr></table><div class=\"highlight-box\"><strong>小贴士：</strong>函数应\"单一职责\"——一个函数只做一件事。函数名用动词开头（Get,Set,Calculate,Is）。参数不宜过多，超3个考虑封装为类。</div>"
},
{
id: "basic-8",
title: "ref和out",
tags: ["ref","out","引用传递"],
content: "<h4>参数传递方式</h4><p>默认情况下参数按值传递。使用<code>ref</code>和<code>out</code>可以实现按引用传递，让方法能够修改调用者的变量。</p><h4>ref关键字</h4><pre><code><span class=\"cm\">// ref：调用前必须初始化，方法可读取和修改</span>\n<span class=\"kw\">static</span> <span class=\"type\">void</span> <span class=\"fn\">Swap</span>(<span class=\"kw\">ref</span> <span class=\"type\">int</span> a, <span class=\"kw\">ref</span> <span class=\"type\">int</span> b)\n{\n    <span class=\"type\">int</span> temp = a;\n    a = b;\n    b = temp;\n}\n\n<span class=\"type\">int</span> x = <span class=\"num\">10</span>, y = <span class=\"num\">20</span>;\n<span class=\"fn\">Swap</span>(<span class=\"kw\">ref</span> x, <span class=\"kw\">ref</span> y);\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"str\">$\"x={x}, y={y}\"</span>);  <span class=\"cm\">// x=20, y=10</span></code></pre><h4>out关键字</h4><pre><code><span class=\"cm\">// out：调用前不需初始化，方法必须赋值</span>\n<span class=\"kw\">static</span> <span class=\"type\">void</span> <span class=\"fn\">Divide</span>(<span class=\"type\">int</span> a, <span class=\"type\">int</span> b, <span class=\"kw\">out</span> <span class=\"type\">int</span> quotient, <span class=\"kw\">out</span> <span class=\"type\">int</span> remainder)\n{\n    quotient = a / b;\n    remainder = a % b;\n}\n\n<span class=\"type\">int</span> q, r;\n<span class=\"fn\">Divide</span>(<span class=\"num\">10</span>, <span class=\"num\">3</span>, <span class=\"kw\">out</span> q, <span class=\"kw\">out</span> r);\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"str\">$\"商={q}, 余数={r}\"</span>);  <span class=\"cm\">// 商=3, 余数=1</span>\n\n<span class=\"cm\">// C#7.0+可以内联声明out变量</span>\n<span class=\"fn\">Divide</span>(<span class=\"num\">10</span>, <span class=\"num\">3</span>, <span class=\"kw\">out</span> <span class=\"type\">int</span> qq, <span class=\"kw\">out</span> <span class=\"type\">int</span> rr);</code></pre><h4>ref vs out 对比</h4><table><tr><th>特性</th><th>ref</th><th>out</th></tr><tr><td>调用前初始化</td><td>必须</td><td>不必</td></tr><tr><td>方法内赋值</td><td>可选</td><td>必须</td></tr><tr><td>数据流向</td><td>双向（进出）</td><td>单向（输出）</td></tr><tr><td>典型用途</td><td>交换、修改外部值</td><td>返回多个值、TryParse</td></tr></table><pre><code><span class=\"cm\">// out的典型应用：TryParse</span>\n<span class=\"kw\">if</span> (<span class=\"type\">int</span>.<span class=\"fn\">TryParse</span>(<span class=\"str\">\"123\"</span>, <span class=\"kw\">out</span> <span class=\"type\">int</span> result))\n    <span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(result);</code></pre><div class=\"highlight-box\"><strong>小贴士：</strong>ref需要双向通信时用；out只要多返回值时用。C#7.0引入的元组<code>(int,int)</code>也是返回多值的好方案。</div>"
},
{
id: "basic-9",
title: "变长参数和参数默认值",
tags: ["params","默认值","可选参数"],
content: "<h4>参数默认值（可选参数）</h4><p>方法参数可以指定默认值，调用时可以省略该参数。</p><pre><code><span class=\"cm\">// 有默认值的参数称为\"可选参数\"</span>\n<span class=\"kw\">static</span> <span class=\"type\">void</span> <span class=\"fn\">Greet</span>(<span class=\"type\">string</span> name, <span class=\"type\">string</span> greeting = <span class=\"str\">\"你好\"</span>)\n{\n    <span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"str\">$\"{greeting}，{name}！\"</span>);\n}\n\n<span class=\"fn\">Greet</span>(<span class=\"str\">\"张三\"</span>);          <span class=\"cm\">// \"你好，张三！\"</span>\n<span class=\"fn\">Greet</span>(<span class=\"str\">\"张三\"</span>, <span class=\"str\">\"早上好\"</span>);  <span class=\"cm\">// \"早上好，张三！\"</span>\n\n<span class=\"cm\">// 可选参数必须在必选参数之后</span>\n<span class=\"kw\">static</span> <span class=\"type\">void</span> <span class=\"fn\">Test</span>(<span class=\"type\">int</span> a, <span class=\"type\">int</span> b = <span class=\"num\">1</span>, <span class=\"type\">int</span> c = <span class=\"num\">2</span>) { }\n<span class=\"cm\">// static void Bad(int a=1, int b) { } // 错误！</span></code></pre><h4>params变长参数</h4><p>使用<code>params</code>关键字可以接收任意数量的参数。</p><pre><code><span class=\"cm\">// params：接收任意数量同类型参数</span>\n<span class=\"kw\">static</span> <span class=\"type\">int</span> <span class=\"fn\">Sum</span>(<span class=\"kw\">params</span> <span class=\"type\">int</span>[] numbers)\n{\n    <span class=\"type\">int</span> total = <span class=\"num\">0</span>;\n    <span class=\"kw\">foreach</span> (<span class=\"type\">int</span> n <span class=\"kw\">in</span> numbers)\n        total += n;\n    <span class=\"kw\">return</span> total;\n}\n\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"fn\">Sum</span>(<span class=\"num\">1</span>, <span class=\"num\">2</span>));         <span class=\"cm\">// 3</span>\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"fn\">Sum</span>(<span class=\"num\">1</span>, <span class=\"num\">2</span>, <span class=\"num\">3</span>, <span class=\"num\">4</span>));   <span class=\"cm\">// 10</span>\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"fn\">Sum</span>());                <span class=\"cm\">// 0</span>\n\n<span class=\"cm\">// 也可以直接传数组</span>\n<span class=\"type\">int</span>[] arr = { <span class=\"num\">1</span>, <span class=\"num\">2</span>, <span class=\"num\">3</span> };\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"fn\">Sum</span>(arr));  <span class=\"cm\">// 6</span></code></pre><h4>params规则</h4><table><tr><th>规则</th><th>说明</th></tr><tr><td>仅限一维数组</td><td>params后必须是一维数组类型</td></tr><tr><td>必须是最后一个参数</td><td>每个方法只能有一个params</td></tr><tr><td>可传0个参数</td><td>不传任何参数也合法</td></tr><tr><td>可与普通参数结合</td><td>params放在最后</td></tr></table><pre><code><span class=\"cm\">// 结合使用：普通参数 + params</span>\n<span class=\"kw\">static</span> <span class=\"type\">string</span> <span class=\"fn\">Concat</span>(<span class=\"type\">string</span> separator, <span class=\"kw\">params</span> <span class=\"type\">string</span>[] values)\n{\n    <span class=\"kw\">return</span> <span class=\"type\">string</span>.<span class=\"fn\">Join</span>(separator, values);\n}\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"fn\">Concat</span>(<span class=\"str\">\", \"</span>, <span class=\"str\">\"A\"</span>, <span class=\"str\">\"B\"</span>, <span class=\"str\">\"C\"</span>));  <span class=\"cm\">// \"A, B, C\"</span></code></pre><div class=\"highlight-box\"><strong>小贴士：</strong>Console.WriteLine等格式化方法大量使用params实现可变参数。自己写工具方法时，params让API更灵活友好。</div>"
},
{
id: "basic-10",
title: "函数重载",
tags: ["重载","overload","多态"],
content: "<h4>什么是函数重载</h4><p>函数重载（overload）是指在同一个类中定义多个同名但参数列表不同的方法。编译器根据调用时传入的参数自动选择匹配的版本。</p><h4>重载规则</h4><table><tr><th>规则</th><th>说明</th></tr><tr><td>方法名相同</td><td>必须是同一个方法名</td></tr><tr><td>参数列表不同</td><td>参数个数、类型或顺序至少有一项不同</td></tr><tr><td>返回值不算</td><td>仅返回值不同不能构成重载</td></tr><tr><td>在同一作用域</td><td>重载方法必须在同一类中</td></tr></table><pre><code><span class=\"cm\">// 正确：参数个数不同</span>\n<span class=\"kw\">static</span> <span class=\"type\">int</span> <span class=\"fn\">Add</span>(<span class=\"type\">int</span> a, <span class=\"type\">int</span> b) { <span class=\"kw\">return</span> a + b; }\n<span class=\"kw\">static</span> <span class=\"type\">int</span> <span class=\"fn\">Add</span>(<span class=\"type\">int</span> a, <span class=\"type\">int</span> b, <span class=\"type\">int</span> c) { <span class=\"kw\">return</span> a + b + c; }\n\n<span class=\"cm\">// 正确：参数类型不同</span>\n<span class=\"kw\">static</span> <span class=\"type\">double</span> <span class=\"fn\">Add</span>(<span class=\"type\">double</span> a, <span class=\"type\">double</span> b) { <span class=\"kw\">return</span> a + b; }\n\n<span class=\"cm\">// 错误：仅返回值不同</span>\n<span class=\"cm\">// static double Add(int a, int b) { return a + b; }</span></code></pre><h4>实际应用</h4><pre><code><span class=\"kw\">class</span> <span class=\"type\">Calculator</span>\n{\n    <span class=\"cm\">// 整数加法</span>\n    <span class=\"kw\">public</span> <span class=\"type\">int</span> <span class=\"fn\">Add</span>(<span class=\"type\">int</span> a, <span class=\"type\">int</span> b) => a + b;\n\n    <span class=\"cm\">// 小数加法</span>\n    <span class=\"kw\">public</span> <span class=\"type\">double</span> <span class=\"fn\">Add</span>(<span class=\"type\">double</span> a, <span class=\"type\">double</span> b) => a + b;\n\n    <span class=\"cm\">// 字符串拼接</span>\n    <span class=\"kw\">public</span> <span class=\"type\">string</span> <span class=\"fn\">Add</span>(<span class=\"type\">string</span> a, <span class=\"type\">string</span> b) => a + b;\n}\n\n<span class=\"kw\">var</span> calc = <span class=\"kw\">new</span> <span class=\"type\">Calculator</span>();\ncalc.<span class=\"fn\">Add</span>(<span class=\"num\">1</span>, <span class=\"num\">2</span>);           <span class=\"cm\">// 调用int版本</span>\ncalc.<span class=\"fn\">Add</span>(<span class=\"num\">1.5</span>, <span class=\"num\">2.5</span>);       <span class=\"cm\">// 调用double版本</span>\ncalc.<span class=\"fn\">Add</span>(<span class=\"str\">\"Hello\"</span>, <span class=\"str\">\"World\"</span>); <span class=\"cm\">// 调用string版本</span></code></pre><h4>重载决策</h4><pre><code><span class=\"cm\">// 编译器按以下优先级选择：</span>\n<span class=\"cm\">// 1. 精确匹配</span>\n<span class=\"cm\">// 2. 隐式转换后匹配</span>\n<span class=\"cm\">// 3. 如果有多个匹配且无法区分，编译报错</span>\n\n<span class=\"fn\">Add</span>(<span class=\"num\">1</span>, <span class=\"num\">2</span>);       <span class=\"cm\">// 精确匹配int版本</span>\n<span class=\"fn\">Add</span>(<span class=\"num\">1.0f</span>, <span class=\"num\">2.0f</span>); <span class=\"cm\">// float隐式转换后匹配double版本</span></code></pre><div class=\"highlight-box\"><strong>小贴士：</strong>重载让API更直观——同一个操作名处理不同类型。Console.WriteLine有19个重载版本！配合可选参数可实现灵活API。</div>"
},
{
id: "basic-11",
title: "递归函数",
tags: ["递归","Recursion","斐波那契"],
content: "<h4>什么是递归</h4><p>递归是函数直接或间接调用自身的过程，用于解决具有自相似结构的问题。</p><h4>递归三要素</h4><table><tr><th>要素</th><th>说明</th></tr><tr><td>基准条件</td><td>递归终止条件，防止无限递归</td></tr><tr><td>递归条件</td><td>将问题分解为更小的子问题</td></tr><tr><td>向基准收敛</td><td>每次递归必须向基准条件靠近</td></tr></table><pre><code><span class=\"cm\">// 示例1：阶乘 n! = n * (n-1)!</span>\n<span class=\"kw\">static</span> <span class=\"type\">int</span> <span class=\"fn\">Factorial</span>(<span class=\"type\">int</span> n)\n{\n    <span class=\"kw\">if</span> (n <= <span class=\"num\">1</span>) <span class=\"kw\">return</span> <span class=\"num\">1</span>;      <span class=\"cm\">// 基准条件</span>\n    <span class=\"kw\">return</span> n * <span class=\"fn\">Factorial</span>(n - <span class=\"num\">1</span>);  <span class=\"cm\">// 递归条件</span>\n}\n\n<span class=\"cm\">// Factorial(5) = 5*4*3*2*1 = 120</span></code></pre><h4>经典递归示例</h4><pre><code><span class=\"cm\">// 斐波那契数列：F(n) = F(n-1) + F(n-2)</span>\n<span class=\"kw\">static</span> <span class=\"type\">int</span> <span class=\"fn\">Fibonacci</span>(<span class=\"type\">int</span> n)\n{\n    <span class=\"kw\">if</span> (n <= <span class=\"num\">1</span>) <span class=\"kw\">return</span> n;\n    <span class=\"kw\">return</span> <span class=\"fn\">Fibonacci</span>(n - <span class=\"num\">1</span>) + <span class=\"fn\">Fibonacci</span>(n - <span class=\"num\">2</span>);\n}\n<span class=\"cm\">// 0,1,1,2,3,5,8,13,21...</span>\n\n<span class=\"cm\">// 汉诺塔问题</span>\n<span class=\"kw\">static</span> <span class=\"type\">void</span> <span class=\"fn\">Hanoi</span>(<span class=\"type\">int</span> n, <span class=\"type\">string</span> from, <span class=\"type\">string</span> to, <span class=\"type\">string</span> aux)\n{\n    <span class=\"kw\">if</span> (n == <span class=\"num\">1</span>) { <span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"str\">$\"移动 {from} -> {to}\"</span>); <span class=\"kw\">return</span>; }\n    <span class=\"fn\">Hanoi</span>(n - <span class=\"num\">1</span>, from, aux, to);\n    <span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"str\">$\"移动 {from} -> {to}\"</span>);\n    <span class=\"fn\">Hanoi</span>(n - <span class=\"num\">1</span>, aux, to, from);\n}</code></pre><h4>递归 vs 迭代</h4><table><tr><th>特性</th><th>递归</th><th>迭代(循环)</th></tr><tr><td>代码简洁性</td><td>代码更简洁优雅</td><td>代码可能冗长</td></tr><tr><td>性能</td><td>函数调用有开销</td><td>更高效</td></tr><tr><td>栈溢出风险</td><td>深度递归可能栈溢出</td><td>无此问题</td></tr><tr><td>适用场景</td><td>树/图遍历、分治</td><td>简单循环</td></tr></table><div class=\"highlight-box\"><strong>小贴士：</strong>递归让代码更优雅但需警惕性能和栈溢出。可考虑尾递归优化（C#不完全支持）或用迭代改写。优先考虑代码清晰度。</div>"
},
{
id: "basic-12",
title: "结构体",
tags: ["struct","值类型","结构体"],
content: "<h4>什么是结构体</h4><p>结构体（struct）是C#中的轻量级值类型，用于封装小型相关数据组。与类（class）不同，结构体是值类型。</p><h4>定义结构体</h4><pre><code><span class=\"kw\">struct</span> <span class=\"type\">Point</span>\n{\n    <span class=\"kw\">public</span> <span class=\"type\">int</span> X;\n    <span class=\"kw\">public</span> <span class=\"type\">int</span> Y;\n\n    <span class=\"cm\">// 构造函数（必须初始化所有字段）</span>\n    <span class=\"kw\">public</span> <span class=\"fn\">Point</span>(<span class=\"type\">int</span> x, <span class=\"type\">int</span> y)\n    {\n        X = x;\n        Y = y;\n    }\n\n    <span class=\"cm\">// 方法</span>\n    <span class=\"kw\">public</span> <span class=\"type\">double</span> <span class=\"fn\">Distance</span>()\n    {\n        <span class=\"kw\">return</span> <span class=\"type\">Math</span>.<span class=\"fn\">Sqrt</span>(X * X + Y * Y);\n    }\n}</code></pre><h4>使用结构体</h4><pre><code><span class=\"type\">Point</span> p1 = <span class=\"kw\">new</span> <span class=\"type\">Point</span>(<span class=\"num\">3</span>, <span class=\"num\">4</span>);\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(p1.<span class=\"fn\">Distance</span>());  <span class=\"cm\">// 5</span>\n\n<span class=\"cm\">// 值类型行为：赋值时复制</span>\n<span class=\"type\">Point</span> p2 = p1;\np2.X = <span class=\"num\">100</span>;\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(p1.X);  <span class=\"cm\">// 3（未变！）</span></code></pre><h4>struct vs class</h4><table><tr><th>特性</th><th>struct</th><th>class</th></tr><tr><td>类型</td><td>值类型</td><td>引用类型</td></tr><tr><td>继承</td><td>不能继承（密封）</td><td>可继承</td></tr><tr><td>默认构造器</td><td>不能自定义无参构造器</td><td>可以</td></tr><tr><td>析构器</td><td>不允许</td><td>允许</td></tr><tr><td>null</td><td>不能为null（除非?）</td><td>可以为null</td></tr><tr><td>适用</td><td>小型数据、频繁创建</td><td>复杂对象、需要继承</td></tr></table><h4>何时用struct</h4><pre><code><span class=\"cm\">// 适合struct的场景：</span>\n<span class=\"cm\">// 1. 逻辑上表示单个值（如坐标、颜色、复数）</span>\n<span class=\"cm\">// 2. 实例小于16字节</span>\n<span class=\"cm\">// 3. 不可变（immutable）</span>\n<span class=\"cm\">// 4. 不需要频繁装箱拆箱</span>\n\n<span class=\"kw\">struct</span> <span class=\"type\">Color</span> { <span class=\"kw\">public</span> <span class=\"type\">byte</span> R, G, B; }\n<span class=\"kw\">struct</span> <span class=\"type\">Complex</span> { <span class=\"kw\">public</span> <span class=\"type\">double</span> Real, Imaginary; }</code></pre><div class=\"highlight-box\"><strong>小贴士：</strong>不确定用class还是struct时，默认用class。int、double等内建类型底层都是struct。readonly struct（C#7.2+）可防止意外修改。</div>"
},
{
id: "basic-13",
title: "冒泡排序",
tags: ["排序","冒泡排序","算法"],
content: "<h4>冒泡排序原理</h4><p>冒泡排序通过反复比较相邻元素并交换，像气泡一样将最大（或最小）的元素逐步\"浮\"到数组末尾。时间复杂度O(n^2)，稳定排序。</p><h4>算法步骤</h4><p>1.比较相邻元素，如果顺序错误则交换<br>2.每轮将最大的元素推到末尾<br>3.重复n-1轮</p><h4>代码实现</h4><pre><code><span class=\"kw\">static</span> <span class=\"type\">void</span> <span class=\"fn\">BubbleSort</span>(<span class=\"type\">int</span>[] arr)\n{\n    <span class=\"type\">int</span> n = arr.Length;\n    <span class=\"kw\">for</span> (<span class=\"type\">int</span> i = <span class=\"num\">0</span>; i < n - <span class=\"num\">1</span>; i++)\n    {\n        <span class=\"cm\">// 每轮将最大值\"冒泡\"到最后</span>\n        <span class=\"kw\">for</span> (<span class=\"type\">int</span> j = <span class=\"num\">0</span>; j < n - <span class=\"num\">1</span> - i; j++)\n        {\n            <span class=\"kw\">if</span> (arr[j] > arr[j + <span class=\"num\">1</span>])\n            {\n                <span class=\"cm\">// 交换</span>\n                <span class=\"type\">int</span> temp = arr[j];\n                arr[j] = arr[j + <span class=\"num\">1</span>];\n                arr[j + <span class=\"num\">1</span>] = temp;\n            }\n        }\n    }\n}</code></pre><h4>优化版（加标志位）</h4><pre><code><span class=\"kw\">static</span> <span class=\"type\">void</span> <span class=\"fn\">BubbleSortOptimized</span>(<span class=\"type\">int</span>[] arr)\n{\n    <span class=\"type\">int</span> n = arr.Length;\n    <span class=\"kw\">for</span> (<span class=\"type\">int</span> i = <span class=\"num\">0</span>; i < n - <span class=\"num\">1</span>; i++)\n    {\n        <span class=\"type\">bool</span> swapped = <span class=\"kw\">false</span>;\n        <span class=\"kw\">for</span> (<span class=\"type\">int</span> j = <span class=\"num\">0</span>; j < n - <span class=\"num\">1</span> - i; j++)\n        {\n            <span class=\"kw\">if</span> (arr[j] > arr[j + <span class=\"num\">1</span>])\n            {\n                (<span class=\"type\">int</span> temp = arr[j]; arr[j] = arr[j+<span class=\"num\">1</span>]; arr[j+<span class=\"num\">1</span>] = temp;)  <span class=\"cm\">// C#7.0+元组交换</span>\n                swapped = <span class=\"kw\">true</span>;\n            }\n        }\n        <span class=\"kw\">if</span> (!swapped) <span class=\"kw\">break</span>;  <span class=\"cm\">// 本轮无交换，已有序</span>\n    }\n}</code></pre><h4>完整测试</h4><pre><code><span class=\"type\">int</span>[] nums = { <span class=\"num\">64</span>, <span class=\"num\">34</span>, <span class=\"num\">25</span>, <span class=\"num\">12</span>, <span class=\"num\">22</span>, <span class=\"num\">11</span>, <span class=\"num\">90</span> };\n<span class=\"fn\">BubbleSort</span>(nums);\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"type\">string</span>.<span class=\"fn\">Join</span>(<span class=\"str\">\" \"</span>, nums));\n<span class=\"cm\">// 输出：11 12 22 25 34 64 90</span></code></pre><h4>算法分析</h4><table><tr><th>指标</th><th>值</th></tr><tr><td>时间复杂度</td><td>O(n^2)，最优O(n)</td></tr><tr><td>空间复杂度</td><td>O(1)</td></tr><tr><td>稳定性</td><td>稳定</td></tr><tr><td>适用</td><td>小数据量、教学</td></tr></table><div class=\"highlight-box\"><strong>小贴士：</strong>冒泡排序简单直观适合教学，但实际项目用小数据量或已接近有序的数据。大数组用Array.Sort()（快速排序实现）。</div>"
},
{
id: "basic-14",
title: "选择排序",
tags: ["排序","选择排序","算法"],
content: "<h4>选择排序原理</h4><p>选择排序每轮找到未排序部分的最小元素，将其放到已排序部分的末尾。时间复杂度O(n^2)，不稳定排序。</p><h4>算法步骤</h4><p>1.从待排序序列中找到最小元素<br>2.将最小元素与待排序序列第一个元素交换<br>3.缩小待排序范围，重复直到全部有序</p><h4>代码实现</h4><pre><code><span class=\"kw\">static</span> <span class=\"type\">void</span> <span class=\"fn\">SelectionSort</span>(<span class=\"type\">int</span>[] arr)\n{\n    <span class=\"type\">int</span> n = arr.Length;\n    <span class=\"kw\">for</span> (<span class=\"type\">int</span> i = <span class=\"num\">0</span>; i < n - <span class=\"num\">1</span>; i++)\n    {\n        <span class=\"type\">int</span> minIndex = i;  <span class=\"cm\">// 假设当前位最小</span>\n        <span class=\"kw\">for</span> (<span class=\"type\">int</span> j = i + <span class=\"num\">1</span>; j < n; j++)\n        {\n            <span class=\"kw\">if</span> (arr[j] < arr[minIndex])\n                minIndex = j;  <span class=\"cm\">// 找到更小的</span>\n        }\n        <span class=\"cm\">// 交换</span>\n        <span class=\"kw\">if</span> (minIndex != i)\n        {\n            <span class=\"type\">int</span> temp = arr[i];\n            arr[i] = arr[minIndex];\n            arr[minIndex] = temp;\n        }\n    }\n}</code></pre><h4>完整测试</h4><pre><code><span class=\"type\">int</span>[] nums = { <span class=\"num\">64</span>, <span class=\"num\">25</span>, <span class=\"num\">12</span>, <span class=\"num\">22</span>, <span class=\"num\">11</span> };\n<span class=\"fn\">SelectionSort</span>(nums);\n<span class=\"type\">Console</span>.<span class=\"fn\">WriteLine</span>(<span class=\"type\">string</span>.<span class=\"fn\">Join</span>(<span class=\"str\">\" \"</span>, nums));\n<span class=\"cm\">// 输出：11 12 22 25 64</span></code></pre><h4>选择排序 vs 冒泡排序</h4><table><tr><th>特性</th><th>冒泡排序</th><th>选择排序</th></tr><tr><td>交换次数</td><td>多（每次比较可能交换）</td><td>少（每轮最多一次交换）</td></tr><tr><td>比较次数</td><td>O(n^2)</td><td>O(n^2)</td></tr><tr><td>稳定性</td><td>稳定</td><td>不稳定</td></tr><tr><td>写操作</td><td>多</td><td>少</td></tr><tr><td>适用场景</td><td>接近有序的数据</td><td>写操作开销大的场景</td></tr></table><h4>算法分析</h4><table><tr><th>指标</th><th>值</th></tr><tr><td>时间复杂度</td><td>O(n^2)，始终n^2/2次比较</td></tr><tr><td>空间复杂度</td><td>O(1)</td></tr><tr><td>稳定性</td><td>不稳定（交换可能破坏顺序）</td></tr></table><div class=\"highlight-box\"><strong>小贴士：</strong>选择排序写操作少，适合写成本高的场景（如Flash存储）。但实际开发中，小数组用插入排序，大数组用快速排序或归并排序。</div>"
}
]
,{id:"core",title:"三、CSharp核心教学",desc:"面向对象编程是C#的灵魂。系统讲解封装、继承、多态三大支柱，涵盖属性、索引器、运算符重载、接口、抽象类等核心机制。",badge:"核心",lessons:[
{id:"core-1",title:"类和对象",tags:["class","对象","new"],content:"<h4>什么是类(Class)</h4><p>类是对一类事物的抽象描述，定义了该类共有的<strong>数据(字段)</strong>和<strong>行为(方法)</strong>。类就像蓝图或模具，本身不占堆内存。</p><h4>什么是对象(Object)</h4><p>对象是类的具体实例，通过<code>new</code>关键字在托管堆上创建。一个类可创建无数个对象，每个拥有独立的成员数据副本。</p><h4>类的声明</h4><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"kw\">class</span> <span class=\"type\">Person</span>\n{\n    <span class=\"kw\">public</span> <span class=\"kw\">string</span> name;\n    <span class=\"kw\">public</span> <span class=\"kw\">int</span> age;\n\n    <span class=\"kw\">public</span> <span class=\"kw\">void</span> <span class=\"fn\">Introduce</span>()\n    {\n        Console.WriteLine($<span class=\"str\">\"我叫{name}，{age}岁\"</span>);\n    }\n}</pre></div><h4>创建对象</h4><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"type\">Person</span> p1 = <span class=\"kw\">new</span> <span class=\"type\">Person</span>();\np1.name = <span class=\"str\">\"张三\"</span>;\np1.age = <span class=\"num\">25</span>;\np1.<span class=\"fn\">Introduce</span>();  <span class=\"cm\">// 我叫张三，25岁</span>\n\n<span class=\"cm\">// 对象初始化器 (C# 3.0+)</span>\n<span class=\"kw\">var</span> p2 = <span class=\"kw\">new</span> <span class=\"type\">Person</span>\n{\n    name = <span class=\"str\">\"李四\"</span>,\n    age = <span class=\"num\">30</span>\n};</pre></div><h4>类与对象区别</h4><table><tr><th>类(Class)</th><th>对象(Object)</th></tr><tr><td>抽象概念/模板</td><td>具体实体/实例</td></tr><tr><td>不占堆内存</td><td>占堆内存</td></tr><tr><td>用class声明</td><td>用new创建</td></tr></table><div class=\"highlight-box\"><strong>内存原理：</strong>类是引用类型。声明变量在栈分配引用，new在堆分配实际内存。多个变量可引用同一对象。</div>"},
{id:"core-2",title:"成员变量和访问修饰符",tags:["字段","访问修饰符","public","private"],content:"<h4>成员变量(字段Field)</h4><p>字段是声明在类体中的变量，存储对象状态数据。类型可为值类型、引用类型、数组等。</p><h4>六大访问修饰符</h4><table><tr><th>修饰符</th><th>范围</th><th>场景</th></tr><tr><td><code>public</code></td><td>任何地方</td><td>对外API</td></tr><tr><td><code>private</code></td><td>类内部(默认)</td><td>内部实现</td></tr><tr><td><code>protected</code></td><td>类+子类</td><td>派生类使用</td></tr><tr><td><code>internal</code></td><td>同程序集</td><td>程序集API</td></tr><tr><td><code>protected internal</code></td><td>同程序集或子类</td><td>满足其一</td></tr><tr><td><code>private protected</code></td><td>同程序集中子类</td><td>C#7.2+</td></tr></table><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"kw\">class</span> <span class=\"type\">Student</span>\n{\n    <span class=\"kw\">public</span> <span class=\"kw\">string</span> Name;              <span class=\"cm\">// 任何人访问</span>\n    <span class=\"kw\">private</span> <span class=\"kw\">int</span> Score;               <span class=\"cm\">// 仅本类</span>\n    <span class=\"kw\">protected</span> <span class=\"kw\">string</span> StudentId;       <span class=\"cm\">// 本类+子类</span>\n    <span class=\"kw\">internal</span> <span class=\"kw\">bool</span> IsEnrolled;         <span class=\"cm\">// 同程序集</span>\n}</pre></div><div class=\"highlight-box\"><strong>最佳实践：</strong>字段设为<code>private</code>，通过<strong>属性</strong>对外暴露。遵循封装原则。</div>"},
{id:"core-3",title:"成员方法",tags:["方法","参数","返回值","重载","ref","out"],content:"<h4>成员方法</h4><p>方法定义了对象的行为，包含访问修饰符、返回值类型、方法名、参数列表和方法体。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"kw\">class</span> <span class=\"type\">Calculator</span>\n{\n    <span class=\"kw\">public</span> <span class=\"kw\">void</span> <span class=\"fn\">PrintInfo</span>()\n    { Console.WriteLine(<span class=\"str\">\"计算器就绪\"</span>); }\n\n    <span class=\"kw\">public</span> <span class=\"kw\">int</span> <span class=\"fn\">Add</span>(<span class=\"kw\">int</span> a, <span class=\"kw\">int</span> b) => a + b;\n\n    <span class=\"kw\">public</span> <span class=\"kw\">int</span> <span class=\"fn\">Multiply</span>(<span class=\"kw\">int</span> a, <span class=\"kw\">int</span> b) => a * b;\n}</pre></div><h4>方法重载</h4><p>同名但参数列表不同的多个方法。返回值不同不构成重载。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"kw\">public</span> <span class=\"kw\">int</span> <span class=\"fn\">Add</span>(<span class=\"kw\">int</span> a, <span class=\"kw\">int</span> b) => a + b;\n<span class=\"kw\">public</span> <span class=\"kw\">double</span> <span class=\"fn\">Add</span>(<span class=\"kw\">double</span> a, <span class=\"kw\">double</span> b) => a + b;\n<span class=\"kw\">public</span> <span class=\"kw\">int</span> <span class=\"fn\">Add</span>(<span class=\"kw\">int</span> a, <span class=\"kw\">int</span> b, <span class=\"kw\">int</span> c) => a + b + c;</pre></div><h4>参数修饰符</h4><table><tr><th>修饰符</th><th>传递方式</th><th>要求</th></tr><tr><td>无</td><td>值传递</td><td>无</td></tr><tr><td><code>ref</code></td><td>引用(双向)</td><td>调用前初始化</td></tr><tr><td><code>out</code></td><td>引用(仅输出)</td><td>方法内赋值</td></tr><tr><td><code>params</code></td><td>可变数量</td><td>最后参数</td></tr></table><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"kw\">public</span> <span class=\"kw\">void</span> <span class=\"fn\">Swap</span>(<span class=\"kw\">ref</span> <span class=\"kw\">int</span> a, <span class=\"kw\">ref</span> <span class=\"kw\">int</span> b)\n{ <span class=\"kw\">int</span> t = a; a = b; b = t; }\n\n<span class=\"kw\">public</span> <span class=\"kw\">int</span> <span class=\"fn\">Sum</span>(<span class=\"kw\">params</span> <span class=\"kw\">int</span>[] nums)\n{\n    <span class=\"kw\">int</span> s = <span class=\"num\">0</span>;\n    <span class=\"kw\">foreach</span> (<span class=\"kw\">var</span> n <span class=\"kw\">in</span> nums) s += n;\n    <span class=\"kw\">return</span> s;\n}</pre></div>"},
{id:"core-4",title:"构造函数和析构函数",tags:["构造函数","析构函数","this","重载"],content:"<h4>构造函数(Constructor)</h4><p>创建对象时自动调用的特殊方法，名与类名相同，无返回值。用于初始化对象状态。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"kw\">class</span> <span class=\"type\">Person</span>\n{\n    <span class=\"kw\">public</span> <span class=\"kw\">string</span> Name;\n    <span class=\"kw\">public</span> <span class=\"kw\">int</span> Age;\n\n    <span class=\"kw\">public</span> <span class=\"fn\">Person</span>() { Name = <span class=\"str\">\"未知\"</span>; Age = <span class=\"num\">0</span>; }\n    <span class=\"kw\">public</span> <span class=\"fn\">Person</span>(<span class=\"kw\">string</span> n, <span class=\"kw\">int</span> a) { Name = n; Age = a; }\n    <span class=\"cm\">// 构造链：用this调用其他构造</span>\n    <span class=\"kw\">public</span> <span class=\"fn\">Person</span>(<span class=\"kw\">string</span> n) : <span class=\"kw\">this</span>(n, <span class=\"num\">0</span>) { }\n}</pre></div><h4>析构函数</h4><p>GC回收时调用，释放非托管资源。推荐使用<code>IDisposable</code>+<code>using</code>语句。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"kw\">class</span> <span class=\"type\">Resource</span> : <span class=\"type\">IDisposable</span>\n{\n    ~<span class=\"fn\">Resource</span>() { <span class=\"fn\">Dispose</span>(<span class=\"kw\">false</span>); }\n    <span class=\"kw\">public</span> <span class=\"kw\">void</span> <span class=\"fn\">Dispose</span>()\n    { <span class=\"fn\">Dispose</span>(<span class=\"kw\">true</span>); <span class=\"type\">GC</span>.SuppressFinalize(<span class=\"kw\">this</span>); }\n    <span class=\"kw\">protected</span> <span class=\"kw\">virtual</span> <span class=\"kw\">void</span> <span class=\"fn\">Dispose</span>(<span class=\"kw\">bool</span> d) { <span class=\"kw\">if</span>(d){<span class=\"cm\">/*释放托管*/</span>} }\n}</pre></div><div class=\"highlight-box\"><strong>注意：</strong>定义带参构造后编译器不生成无参构造。利用<code>using var x = new Resource()</code>自动释放。</div>"},
{id:"core-5",title:"成员属性",tags:["属性","get","set","自动属性","init"],content:"<h4>属性(Property)</h4><p>属性是对字段的封装，通过<strong>get/set访问器</strong>控制数据读写。本质是特殊方法，使用时像字段一样自然。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"kw\">class</span> <span class=\"type\">Person</span>\n{\n    <span class=\"kw\">private</span> <span class=\"kw\">string</span> _name;\n\n    <span class=\"kw\">public</span> <span class=\"kw\">string</span> Name\n    {\n        <span class=\"kw\">get</span> { <span class=\"kw\">return</span> _name; }\n        <span class=\"kw\">set</span>\n        {\n            <span class=\"kw\">if</span> (<span class=\"kw\">string</span>.IsNullOrWhiteSpace(<span class=\"kw\">value</span>))\n                <span class=\"kw\">throw</span> <span class=\"kw\">new</span> <span class=\"type\">ArgumentException</span>();\n            _name = <span class=\"kw\">value</span>;\n        }\n    }\n\n    <span class=\"cm\">// 自动属性：编译器生成后备字段</span>\n    <span class=\"kw\">public</span> <span class=\"kw\">int</span> Age { <span class=\"kw\">get</span>; <span class=\"kw\">set</span>; }\n    <span class=\"kw\">public</span> <span class=\"kw\">string</span> Id { <span class=\"kw\">get</span>; <span class=\"kw\">private</span> <span class=\"kw\">set</span>; }\n    <span class=\"kw\">public</span> <span class=\"kw\">int</span> Value { <span class=\"kw\">get</span>; <span class=\"kw\">init</span>; }  <span class=\"cm\">// C# 9+</span>\n\n    <span class=\"cm\">// 表达式体属性</span>\n    <span class=\"kw\">public</span> <span class=\"kw\">bool</span> IsAdult => Age >= <span class=\"num\">18</span>;\n}</pre></div><div class=\"highlight-box\"><strong>属性 vs 字段：</strong>属性可包含验证/计算逻辑。数据绑定只能绑定属性。永远使用属性暴露数据。</div>"},
{id:"core-6",title:"索引器",tags:["索引器","this","集合"],content:"<h4>索引器(Indexer)</h4><p>索引器让对象像数组一样通过<code>[]</code>访问内部数据。用<code>this</code>声明，是带参数的特殊属性。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"kw\">class</span> <span class=\"type\">StringCollection</span>\n{\n    <span class=\"kw\">string</span>[] _items = <span class=\"kw\">new</span> <span class=\"kw\">string</span>[<span class=\"num\">10</span>];\n    <span class=\"kw\">public</span> <span class=\"kw\">string</span> <span class=\"kw\">this</span>[<span class=\"kw\">int</span> index]\n    {\n        <span class=\"kw\">get</span> => index >= <span class=\"num\">0</span> && index < _items.Length ? _items[index] : <span class=\"kw\">null</span>;\n        <span class=\"kw\">set</span> { <span class=\"kw\">if</span> (index >= <span class=\"num\">0</span> && index < _items.Length) _items[index] = <span class=\"kw\">value</span>; }\n    }\n}\n<span class=\"cm\">// sc[0] = \"Hello\"; Console.WriteLine(sc[0]);</span>\n\n<span class=\"cm\">// 多参数索引器（矩阵）</span>\n<span class=\"kw\">class</span> <span class=\"type\">Matrix</span>\n{\n    <span class=\"kw\">int</span>[,] _d;\n    <span class=\"kw\">public</span> <span class=\"kw\">int</span> <span class=\"kw\">this</span>[<span class=\"kw\">int</span> r, <span class=\"kw\">int</span> c]\n    { <span class=\"kw\">get</span> => _d[r,c]; <span class=\"kw\">set</span> => _d[r,c] = <span class=\"kw\">value</span>; }\n}</pre></div><div class=\"highlight-box\"><strong>应用：</strong>List&lt;T&gt;和Dictionary&lt;K,V&gt;都依赖索引器。索引器可重载（不同参数类型）。索引不限于整数。</div>"},
{id:"core-7",title:"静态成员",tags:["static","静态字段","静态方法","const"],content:"<h4>static关键字</h4><p>静态成员属于<strong>类本身</strong>而非实例。全局唯一，类首次使用时初始化。所有实例共享。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"kw\">class</span> <span class=\"type\">Player</span>\n{\n    <span class=\"kw\">public</span> <span class=\"kw\">static</span> <span class=\"kw\">int</span> TotalCount = <span class=\"num\">0</span>;\n    <span class=\"kw\">public</span> <span class=\"kw\">string</span> Name;\n\n    <span class=\"kw\">public</span> <span class=\"fn\">Player</span>(<span class=\"kw\">string</span> name)\n    { Name = name; TotalCount++; }\n}\n<span class=\"cm\">// Player.TotalCount 通过类名访问</span>\n\n<span class=\"cm\">// 静态工具类</span>\n<span class=\"kw\">public</span> <span class=\"kw\">static</span> <span class=\"kw\">class</span> <span class=\"type\">MathUtils</span>\n{\n    <span class=\"kw\">public</span> <span class=\"kw\">static</span> <span class=\"kw\">int</span> <span class=\"fn\">Square</span>(<span class=\"kw\">int</span> x) => x * x;\n    <span class=\"kw\">public</span> <span class=\"kw\">static</span> <span class=\"kw\">double</span> <span class=\"fn\">PI</span> => <span class=\"num\">3.14159265</span>;\n}</pre></div><h4>const vs static readonly</h4><table><tr><th>特性</th><th>const</th><th>static readonly</th></tr><tr><td>赋值</td><td>编译时</td><td>运行时</td></tr><tr><td>类型</td><td>基本类型+string</td><td>任意</td></tr><tr><td>版本</td><td>修改后引用方需重编译</td><td>无需重编译</td></tr></table><div class=\"highlight-box\"><strong>静态方法</strong>不能使用<code>this</code>。可能变化的值用<code>static readonly</code>。</div>"},
{id:"core-8",title:"静态类和静态构造函数",tags:["static class","静态构造","单例"],content:"<h4>静态类</h4><p><code>static class</code>不能实例化，只含静态成员，隐式sealed。用于组织工具方法和扩展方法。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"kw\">public</span> <span class=\"kw\">static</span> <span class=\"kw\">class</span> <span class=\"type\">StringUtils</span>\n{\n    <span class=\"kw\">public</span> <span class=\"kw\">static</span> <span class=\"kw\">string</span> <span class=\"fn\">Truncate</span>(<span class=\"kw\">string</span> s, <span class=\"kw\">int</span> max)\n        => s?.Length > max ? s[..max] + <span class=\"str\">\"...\"</span> : s;\n\n    <span class=\"kw\">public</span> <span class=\"kw\">static</span> <span class=\"kw\">string</span> <span class=\"fn\">Reverse</span>(<span class=\"kw\">string</span> s)\n    {\n        <span class=\"kw\">var</span> chars = s.ToCharArray();\n        <span class=\"type\">Array</span>.Reverse(chars);\n        <span class=\"kw\">return</span> <span class=\"kw\">new</span> <span class=\"kw\">string</span>(chars);\n    }\n}</pre></div><h4>静态构造函数</h4><p>无修饰符无参数，类首次使用时CLR自动调用一次（线程安全）。常用于单例模式。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"kw\">class</span> <span class=\"type\">Database</span>\n{\n    <span class=\"kw\">public</span> <span class=\"kw\">static</span> <span class=\"kw\">string</span> ConnStr;\n    <span class=\"kw\">static</span> <span class=\"fn\">Database</span>()  <span class=\"cm\">// 静态构造</span>\n    {\n        ConnStr = <span class=\"str\">\"Server=.;Database=AppDB;...\"</span>;\n    }\n}\n\n<span class=\"cm\">// 利用静态构造实现线程安全单例</span>\n<span class=\"kw\">public</span> <span class=\"kw\">sealed</span> <span class=\"kw\">class</span> <span class=\"type\">Singleton</span>\n{\n    <span class=\"kw\">private</span> <span class=\"kw\">static</span> <span class=\"kw\">readonly</span> <span class=\"type\">Singleton</span> _inst = <span class=\"kw\">new</span>();\n    <span class=\"kw\">public</span> <span class=\"kw\">static</span> <span class=\"type\">Singleton</span> Instance => _inst;\n    <span class=\"kw\">private</span> <span class=\"fn\">Singleton</span>() { }\n}</pre></div>"},
{id:"core-9",title:"拓展方法",tags:["扩展方法","this","LINQ"],content:"<h4>扩展方法(Extension Method)</h4><p>不修改原始类型即可<strong>添加新方法</strong>。LINQ的基石。必须是静态类中静态方法，首参数<code>this</code>修饰。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"kw\">public</span> <span class=\"kw\">static</span> <span class=\"kw\">class</span> <span class=\"type\">StringExtensions</span>\n{\n    <span class=\"kw\">public</span> <span class=\"kw\">static</span> <span class=\"kw\">bool</span> <span class=\"fn\">IsValidEmail</span>(<span class=\"kw\">this</span> <span class=\"kw\">string</span> str)\n    {\n        <span class=\"kw\">if</span> (<span class=\"kw\">string</span>.IsNullOrEmpty(str)) <span class=\"kw\">return</span> <span class=\"kw\">false</span>;\n        <span class=\"kw\">return</span> str.Contains(<span class=\"str\">\"@\"</span>) && str.Contains(<span class=\"str\">\".\"</span>);\n    }\n\n    <span class=\"kw\">public</span> <span class=\"kw\">static</span> <span class=\"kw\">string</span> <span class=\"fn\">Repeat</span>(<span class=\"kw\">this</span> <span class=\"kw\">string</span> s, <span class=\"kw\">int</span> n)\n        => <span class=\"kw\">string</span>.Concat(<span class=\"type\">Enumerable</span>.Repeat(s, n));\n}\n\n<span class=\"cm\">// 像实例方法一样调用</span>\n<span class=\"kw\">bool</span> ok = <span class=\"str\">\"a@b.com\"</span>.<span class=\"fn\">IsValidEmail</span>();  <span class=\"cm\">// true</span>\n<span class=\"kw\">string</span> s = <span class=\"str\">\"=\"</span>.<span class=\"fn\">Repeat</span>(<span class=\"num\">10</span>);        <span class=\"cm\">// \"==========\"</span></pre></div><h4>方法解析优先级</h4><table><tr><th>优先级</th><th>来源</th></tr><tr><td>1(最高)</td><td>实例方法</td></tr><tr><td>2</td><td>当前命名空间扩展方法</td></tr><tr><td>3</td><td>using导入的扩展方法</td></tr></table><div class=\"highlight-box\"><strong>LINQ的秘密：</strong>Where/Select/OrderBy等都是<code>IEnumerable&lt;T&gt;</code>的扩展方法。只需<code>using System.Linq;</code>。</div>"},
{id:"core-10",title:"运算符重载",tags:["运算符重载","operator","==","+"],content:"<h4>运算符重载</h4><p>为自定义类型定义<strong>+、-、*、==、!=</strong>等运算符行为。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"kw\">struct</span> <span class=\"type\">Vector2</span>\n{\n    <span class=\"kw\">public</span> <span class=\"kw\">int</span> X, Y;\n    <span class=\"kw\">public</span> <span class=\"fn\">Vector2</span>(<span class=\"kw\">int</span> x, <span class=\"kw\">int</span> y) { X = x; Y = y; }\n\n    <span class=\"kw\">public</span> <span class=\"kw\">static</span> <span class=\"type\">Vector2</span> <span class=\"kw\">operator</span> +(<span class=\"type\">Vector2</span> a, <span class=\"type\">Vector2</span> b)\n        => <span class=\"kw\">new</span> <span class=\"type\">Vector2</span>(a.X + b.X, a.Y + b.Y);\n\n    <span class=\"kw\">public</span> <span class=\"kw\">static</span> <span class=\"type\">Vector2</span> <span class=\"kw\">operator</span> *(<span class=\"type\">Vector2</span> v, <span class=\"kw\">int</span> s)\n        => <span class=\"kw\">new</span> <span class=\"type\">Vector2</span>(v.X * s, v.Y * s);\n\n    <span class=\"kw\">public</span> <span class=\"kw\">static</span> <span class=\"kw\">bool</span> <span class=\"kw\">operator</span> ==(<span class=\"type\">Vector2</span> a, <span class=\"type\">Vector2</span> b)\n        => a.X == b.X && a.Y == b.Y;\n    <span class=\"kw\">public</span> <span class=\"kw\">static</span> <span class=\"kw\">bool</span> <span class=\"kw\">operator</span> !=(<span class=\"type\">Vector2</span> a, <span class=\"type\">Vector2</span> b) => !(a == b);\n\n    <span class=\"kw\">public</span> <span class=\"kw\">override</span> <span class=\"kw\">bool</span> <span class=\"fn\">Equals</span>(<span class=\"kw\">object</span> o) => o <span class=\"kw\">is</span> <span class=\"type\">Vector2</span> v && <span class=\"kw\">this</span> == v;\n    <span class=\"kw\">public</span> <span class=\"kw\">override</span> <span class=\"kw\">int</span> <span class=\"fn\">GetHashCode</span>() => <span class=\"type\">HashCode</span>.Combine(X, Y);\n}</pre></div><table><tr><th>可重载</th><th>不可重载</th></tr><tr><td>+ - * / % ++ --</td><td>= . ?: new is as</td></tr><tr><td>== != < > <= >=</td><td>&& || typeof -></td></tr></table><div class=\"highlight-box\"><strong>必须成对重载：</strong>==和!=、&lt;和&gt;、&lt;=和&gt;=。必须是public static。重载==时务必重写Equals/GetHashCode。</div>"},
{id:"core-11",title:"内部类和分部类",tags:["嵌套类","partial","分部方法"],content:"<h4>内部类(嵌套类)</h4><p>在类内部定义的类。可访问外部类的私有成员。常用于封装仅在外部类中使用的辅助类型。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"kw\">class</span> <span class=\"type\">LinkedList</span>\n{\n    <span class=\"kw\">private</span> <span class=\"kw\">class</span> <span class=\"type\">Node</span>  <span class=\"cm\">// 隐藏实现细节</span>\n    {\n        <span class=\"kw\">public</span> <span class=\"kw\">int</span> Data;\n        <span class=\"kw\">public</span> <span class=\"type\">Node</span> Next;\n    }\n    <span class=\"kw\">private</span> <span class=\"type\">Node</span> _head;\n}</pre></div><h4>分部类(partial)</h4><p>将一个类拆分到多个文件。编译器会合并所有部分。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"cm\">// File1.cs</span>\n<span class=\"kw\">public</span> <span class=\"kw\">partial</span> <span class=\"kw\">class</span> <span class=\"type\">Person</span>\n{\n    <span class=\"kw\">public</span> <span class=\"kw\">string</span> Name { <span class=\"kw\">get</span>; <span class=\"kw\">set</span>; }\n}\n<span class=\"cm\">// File2.cs</span>\n<span class=\"kw\">public</span> <span class=\"kw\">partial</span> <span class=\"kw\">class</span> <span class=\"type\">Person</span>\n{\n    <span class=\"kw\">public</span> <span class=\"kw\">bool</span> <span class=\"fn\">IsValid</span>() => !<span class=\"kw\">string</span>.IsNullOrEmpty(Name);\n}</pre></div><div class=\"highlight-box\"><strong>典型应用：</strong>WinForms/WPF的.designer.cs（自动生成）+ .cs（手写）。EF Core实体。分部方法如不实现则调用被编译器移除。</div>"},
{id:"core-12",title:"继承基本规则",tags:["继承","base","父类","子类"],content:"<h4>继承(Inheritance)</h4><p>子类从父类<strong>继承非私有成员</strong>。C#单继承（一个类只能有一个直接父类），但可实现多个接口。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"cm\">// 基类</span>\n<span class=\"kw\">class</span> <span class=\"type\">Animal</span>\n{\n    <span class=\"kw\">public</span> <span class=\"kw\">string</span> Name { <span class=\"kw\">get</span>; <span class=\"kw\">set</span>; }\n    <span class=\"kw\">public</span> <span class=\"kw\">void</span> <span class=\"fn\">Eat</span>() => Console.WriteLine(<span class=\"str\">\"吃东西\"</span>);\n    <span class=\"kw\">public</span> <span class=\"kw\">virtual</span> <span class=\"kw\">void</span> <span class=\"fn\">MakeSound</span>()\n        => Console.WriteLine(<span class=\"str\">\"动物发声\"</span>);\n}\n\n<span class=\"cm\">// 派生类</span>\n<span class=\"kw\">class</span> <span class=\"type\">Dog</span> : <span class=\"type\">Animal</span>\n{\n    <span class=\"kw\">public</span> <span class=\"kw\">string</span> Breed { <span class=\"kw\">get</span>; <span class=\"kw\">set</span>; }\n    <span class=\"kw\">public</span> <span class=\"kw\">override</span> <span class=\"kw\">void</span> <span class=\"fn\">MakeSound</span>()\n        => Console.WriteLine(<span class=\"str\">\"汪汪!\"</span>);\n}\n\n<span class=\"kw\">var</span> dog = <span class=\"kw\">new</span> <span class=\"type\">Dog</span> { Name = <span class=\"str\">\"旺财\"</span> };\ndog.<span class=\"fn\">Eat</span>();        <span class=\"cm\">// 继承自Animal</span>\ndog.<span class=\"fn\">MakeSound</span>(); <span class=\"cm\">// 汪汪! (重写后)</span></pre></div><table><tr><th>父类修饰符</th><th>子类访问</th><th>外部访问</th></tr><tr><td>public</td><td>能</td><td>能</td></tr><tr><td>protected</td><td>能</td><td>不能</td></tr><tr><td>private</td><td>不能</td><td>不能</td></tr></table><div class=\"highlight-box\"><strong>C#规则：</strong>单继承类，多实现接口。所有类最终继承Object。结构体隐式继承ValueType。</div>"},
{id:"core-13",title:"里氏替换原则",tags:["LSP","里氏替换","is","as"],content:"<h4>里氏替换原则(LSP)</h4><p>SOLID中的L：<strong>子类对象应能替换父类对象，程序行为不变</strong>。任何需要父类的地方都可安全使用子类。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"kw\">class</span> <span class=\"type\">Animal</span>\n{\n    <span class=\"kw\">public</span> <span class=\"kw\">virtual</span> <span class=\"kw\">void</span> <span class=\"fn\">Move</span>() => Console.WriteLine(<span class=\"str\">\"动物移动\"</span>);\n}\n<span class=\"kw\">class</span> <span class=\"type\">Bird</span> : <span class=\"type\">Animal</span>\n{\n    <span class=\"kw\">public</span> <span class=\"kw\">override</span> <span class=\"kw\">void</span> <span class=\"fn\">Move</span>() => Console.WriteLine(<span class=\"str\">\"鸟在飞\"</span>);\n}\n<span class=\"kw\">class</span> <span class=\"type\">Fish</span> : <span class=\"type\">Animal</span>\n{\n    <span class=\"kw\">public</span> <span class=\"kw\">override</span> <span class=\"kw\">void</span> <span class=\"fn\">Move</span>() => Console.WriteLine(<span class=\"str\">\"鱼在游\"</span>);\n}\n\n<span class=\"cm\">// LSP：父类引用接收子类对象</span>\n<span class=\"type\">Animal</span>[] zoo = { <span class=\"kw\">new</span> <span class=\"type\">Bird</span>(), <span class=\"kw\">new</span> <span class=\"type\">Fish</span>() };\n<span class=\"kw\">foreach</span> (<span class=\"kw\">var</span> a <span class=\"kw\">in</span> zoo) a.<span class=\"fn\">Move</span>(); <span class=\"cm\">// 鸟在飞 / 鱼在游</span></pre></div><h4>is和as</h4><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"type\">Animal</span> a = <span class=\"kw\">new</span> <span class=\"type\">Bird</span>();\n\n<span class=\"cm\">// is: 类型检查+模式匹配 (C# 7.0+)</span>\n<span class=\"kw\">if</span> (a <span class=\"kw\">is</span> <span class=\"type\">Bird</span> b) b.<span class=\"fn\">Fly</span>();\n\n<span class=\"cm\">// as: 安全转换，失败返回null</span>\n<span class=\"type\">Bird</span> b2 = a <span class=\"kw\">as</span> <span class=\"type\">Bird</span>;\n<span class=\"kw\">if</span> (b2 != <span class=\"kw\">null</span>) b2.<span class=\"fn\">Fly</span>();\n\n<span class=\"cm\">// switch模式匹配</span>\n<span class=\"kw\">switch</span> (a)\n{\n    <span class=\"kw\">case</span> <span class=\"type\">Bird</span> bird: bird.<span class=\"fn\">Fly</span>(); <span class=\"kw\">break</span>;\n    <span class=\"kw\">case</span> <span class=\"type\">Fish</span> fish: fish.<span class=\"fn\">Swim</span>(); <span class=\"kw\">break</span>;\n}</pre></div>"},
,{id:"core-14",title:"继承中的构造函数",tags:["base","构造函数","继承链"],content:"<h4>继承中的构造顺序</h4><p>创建子类对象时，<strong>先调用父类构造，再调用子类构造</strong>。默认调用父类无参构造。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"kw\">class</span> <span class=\"type\">Base</span>\n{\n    <span class=\"kw\">public</span> <span class=\"fn\">Base</span>() { Console.WriteLine(<span class=\"str\">\"Base()\"</span>); }\n    <span class=\"kw\">public</span> <span class=\"fn\">Base</span>(<span class=\"kw\">string</span> msg) { Console.WriteLine(msg); }\n}\n\n<span class=\"kw\">class</span> <span class=\"type\">Derived</span> : <span class=\"type\">Base</span>\n{\n    <span class=\"kw\">public</span> <span class=\"fn\">Derived</span>() { Console.WriteLine(<span class=\"str\">\"Derived()\"</span>); }\n    <span class=\"cm\">// 用base指定调用父类哪个构造</span>\n    <span class=\"kw\">public</span> <span class=\"fn\">Derived</span>(<span class=\"kw\">string</span> m) : <span class=\"kw\">base</span>(m) { }\n}\n<span class=\"kw\">var</span> d = <span class=\"kw\">new</span> <span class=\"type\">Derived</span>(<span class=\"str\">\"Hi\"</span>);\n<span class=\"cm\">// 输出: Hi</span></pre></div><div class=\"highlight-box\"><strong>规则：</strong>子类构造中不写:base()则编译器自动调用父类无参构造。父类无无参构造且子类未显式调用base会导致编译错误。构造顺序：Object->...->父类->子类。</div>"},
{id:"core-15",title:"万物之父和装箱拆箱",tags:["Object","装箱","拆箱","值类型"],content:"<h4>万物之父System.Object</h4><p>C#中所有类型最终都继承自Object。定义了ToString()、Equals()、GetHashCode()、GetType()等基本方法。</p><h4>装箱(Boxing)</h4><p>值类型->object：在堆分配内存并复制值，有性能开销。</p><h4>拆箱(Unboxing)</h4><p>object->值类型：需显式转换，类型必须完全匹配。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"cm\">// 装箱</span>\n<span class=\"kw\">int</span> num = <span class=\"num\">42</span>;\n<span class=\"kw\">object</span> obj = num;  <span class=\"cm\">// 装箱！堆分配</span>\n\n<span class=\"cm\">// 拆箱</span>\n<span class=\"kw\">int</span> unboxed = (<span class=\"kw\">int</span>)obj;  <span class=\"cm\">// 拆箱！类型必须匹配</span>\n\n<span class=\"cm\">// 常见装箱场景（应避免）</span>\n<span class=\"type\">ArrayList</span> list = <span class=\"kw\">new</span> <span class=\"type\">ArrayList</span>();\nlist.Add(<span class=\"num\">1</span>);  <span class=\"cm\">// 装箱!</span>\n\n<span class=\"cm\">// 泛型避免装箱（推荐）</span>\n<span class=\"type\">List</span><<span class=\"kw\">int</span>> list2 = <span class=\"kw\">new</span> <span class=\"type\">List</span><<span class=\"kw\">int</span>>();\nlist2.Add(<span class=\"num\">1</span>);  <span class=\"cm\">// 无装箱!</span></pre></div><table><tr><th>操作</th><th>内存</th><th>速度</th></tr><tr><td>装箱</td><td>堆分配+复制</td><td>慢</td></tr><tr><td>拆箱</td><td>无额外分配</td><td>较快</td></tr><tr><td>泛型</td><td>无装箱</td><td>快</td></tr></table><div class=\"highlight-box\"><strong>最佳实践：</strong>始终使用泛型集合。使用ToString()代替隐式装箱的字符串拼接。</div>"},
{id:"core-16",title:"密封类",tags:["sealed","密封","禁止继承"],content:"<h4>sealed关键字</h4><p><code>sealed</code>修饰的类<strong>不能被继承</strong>。是对继承链的终结声明。密封类可继承其他类或接口。静态类隐式是sealed的。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"kw\">public</span> <span class=\"kw\">sealed</span> <span class=\"kw\">class</span> <span class=\"type\">MathHelper</span>\n{\n    <span class=\"kw\">public</span> <span class=\"kw\">static</span> <span class=\"kw\">double</span> PI = <span class=\"num\">3.14159</span>;\n    <span class=\"kw\">public</span> <span class=\"kw\">static</span> <span class=\"kw\">int</span> <span class=\"fn\">Add</span>(<span class=\"kw\">int</span> a, <span class=\"kw\">int</span> b) => a + b;\n}\n<span class=\"cm\">// class MyMath : MathHelper { } // 编译错误！</span>\n\n<span class=\"cm\">// 密封类可继承基类</span>\n<span class=\"kw\">public</span> <span class=\"kw\">sealed</span> <span class=\"kw\">class</span> <span class=\"type\">Circle</span> : <span class=\"type\">Shape</span>\n{\n    <span class=\"kw\">public</span> <span class=\"kw\">override</span> <span class=\"kw\">double</span> <span class=\"fn\">Area</span>() => <span class=\"type\">Math</span>.PI * R * R;\n}\n\n<span class=\"cm\">// record也可密封</span>\n<span class=\"kw\">public</span> <span class=\"kw\">sealed</span> <span class=\"kw\">record</span> <span class=\"type\">Point</span>(<span class=\"kw\">int</span> X, <span class=\"kw\">int</span> Y);</pre></div><table><tr><th>场景</th><th>说明</th></tr><tr><td>安全</td><td>防止通过继承篡改关键行为</td></tr><tr><td>性能</td><td>JIT可去虚拟化虚方法调用</td></tr><tr><td>设计</td><td>明确表达不应被扩展</td></tr></table><div class=\"highlight-box\"><strong>性能优势：</strong>密封类虚方法调用可被JIT优化为直接调用。string就是sealed的。</div>"},
{id:"core-17",title:"多态vob",tags:["多态","virtual","override","VMT"],content:"<h4>多态(Polymorphism)</h4><p>使用父类引用调用子类重写的方法，实际执行子类实现。底层依赖<strong>虚方法表(VMT)</strong>。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"kw\">class</span> <span class=\"type\">Shape</span>\n{\n    <span class=\"kw\">public</span> <span class=\"kw\">virtual</span> <span class=\"kw\">void</span> <span class=\"fn\">Draw</span>() => Console.WriteLine(<span class=\"str\">\"绘制形状\"</span>);\n    <span class=\"kw\">public</span> <span class=\"kw\">virtual</span> <span class=\"kw\">double</span> <span class=\"fn\">Area</span>() => <span class=\"num\">0</span>;\n}\n<span class=\"kw\">class</span> <span class=\"type\">Circle</span> : <span class=\"type\">Shape</span>\n{\n    <span class=\"kw\">public</span> <span class=\"kw\">double</span> R;\n    <span class=\"kw\">public</span> <span class=\"kw\">override</span> <span class=\"kw\">void</span> <span class=\"fn\">Draw</span>() => Console.WriteLine(<span class=\"str\">\"绘制圆形\"</span>);\n    <span class=\"kw\">public</span> <span class=\"kw\">override</span> <span class=\"kw\">double</span> <span class=\"fn\">Area</span>() => <span class=\"type\">Math</span>.PI * R * R;\n}\n<span class=\"kw\">class</span> <span class=\"type\">Rect</span> : <span class=\"type\">Shape</span>\n{\n    <span class=\"kw\">public</span> <span class=\"kw\">double</span> W, H;\n    <span class=\"kw\">public</span> <span class=\"kw\">override</span> <span class=\"kw\">void</span> <span class=\"fn\">Draw</span>() => Console.WriteLine(<span class=\"str\">\"绘制矩形\"</span>);\n    <span class=\"kw\">public</span> <span class=\"kw\">override</span> <span class=\"kw\">double</span> <span class=\"fn\">Area</span>() => W * H;\n}\n\n<span class=\"cm\">// 多态：父类引用调用子类方法</span>\n<span class=\"type\">Shape</span>[] shapes = { <span class=\"kw\">new</span> <span class=\"type\">Circle</span> { R = <span class=\"num\">5</span> }, <span class=\"kw\">new</span> <span class=\"type\">Rect</span> { W = <span class=\"num\">3</span>, H = <span class=\"num\">4</span> } };\n<span class=\"kw\">foreach</span> (<span class=\"kw\">var</span> s <span class=\"kw\">in</span> shapes) { s.<span class=\"fn\">Draw</span>(); Console.WriteLine(s.<span class=\"fn\">Area</span>()); }</pre></div><h4>virtual/override/new对比</h4><table><tr><th>关键字</th><th>行为</th><th>父类引用调用</th></tr><tr><td>virtual(父)</td><td>声明可重写</td><td>走VMT</td></tr><tr><td>override(子)</td><td>重写(实现多态)</td><td>调子类版本</td></tr><tr><td>new(子)</td><td>隐藏(非重写!)</td><td>调父类版本</td></tr></table><div class=\"highlight-box\"><strong>VMT原理：</strong>每类型有一虚方法表，virtual方法占槽位。子类override替换槽位地址，运行时根据实际类型查VMT调用正确版本。</div>"},
{id:"core-18",title:"抽象类和抽象方法",tags:["abstract","抽象类","抽象方法"],content:"<h4>抽象类(Abstract Class)</h4><p><code>abstract</code>修饰的类不能实例化，只能作基类。可包含抽象方法(无实现)和具体方法(有实现)。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"kw\">public</span> <span class=\"kw\">abstract</span> <span class=\"kw\">class</span> <span class=\"type\">Shape</span>\n{\n    <span class=\"kw\">public</span> <span class=\"kw\">string</span> Color { <span class=\"kw\">get</span>; <span class=\"kw\">set</span>; } = <span class=\"str\">\"Black\"</span>;\n\n    <span class=\"cm\">// 抽象方法：无实现体，子类必须重写</span>\n    <span class=\"kw\">public</span> <span class=\"kw\">abstract</span> <span class=\"kw\">double</span> <span class=\"fn\">GetArea</span>();\n    <span class=\"kw\">public</span> <span class=\"kw\">abstract</span> <span class=\"kw\">double</span> <span class=\"fn\">GetPerimeter</span>();\n\n    <span class=\"cm\">// 普通方法：提供默认实现</span>\n    <span class=\"kw\">public</span> <span class=\"kw\">virtual</span> <span class=\"kw\">void</span> <span class=\"fn\">Display</span>()\n        => Console.WriteLine($<span class=\"str\">\"面积:{GetArea()} 周长:{GetPerimeter()}\"</span>);\n}\n\n<span class=\"kw\">public</span> <span class=\"kw\">class</span> <span class=\"type\">Circle</span> : <span class=\"type\">Shape</span>\n{\n    <span class=\"kw\">public</span> <span class=\"kw\">double</span> R;\n    <span class=\"kw\">public</span> <span class=\"kw\">override</span> <span class=\"kw\">double</span> <span class=\"fn\">GetArea</span>() => <span class=\"type\">Math</span>.PI * R * R;\n    <span class=\"kw\">public</span> <span class=\"kw\">override</span> <span class=\"kw\">double</span> <span class=\"fn\">GetPerimeter</span>() => <span class=\"num\">2</span> * <span class=\"type\">Math</span>.PI * R;\n}</pre></div><div class=\"highlight-box\"><strong>规则：</strong>不能new抽象类。抽象方法隐式virtual。非抽象子类必须实现所有抽象方法。可以有构造（供子类base调用）。表达is-a关系。</div>"},
{id:"core-19",title:"接口",tags:["interface","接口","多实现","契约"],content:"<h4>接口(Interface)</h4><p>接口定义了一组<strong>行为契约</strong>——只声明方法/属性/事件/索引器签名，不提供实现(C#8前)。实现接口的类必须实现所有成员。是实现多态和松耦合的关键机制。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"cm\">// 定义接口（约定以I开头）</span>\n<span class=\"kw\">public</span> <span class=\"kw\">interface</span> <span class=\"type\">IFlyable</span>\n{\n    <span class=\"kw\">void</span> <span class=\"fn\">Fly</span>();\n    <span class=\"kw\">int</span> MaxHeight { <span class=\"kw\">get</span>; <span class=\"kw\">set</span>; }\n}\n\n<span class=\"kw\">public</span> <span class=\"kw\">interface</span> <span class=\"type\">ISwimmable</span>\n{\n    <span class=\"kw\">void</span> <span class=\"fn\">Swim</span>();\n}\n\n<span class=\"cm\">// 一个类可实现多个接口</span>\n<span class=\"kw\">public</span> <span class=\"kw\">class</span> <span class=\"type\">Duck</span> : <span class=\"type\">Animal</span>, <span class=\"type\">IFlyable</span>, <span class=\"type\">ISwimmable</span>\n{\n    <span class=\"kw\">public</span> <span class=\"kw\">int</span> MaxHeight { <span class=\"kw\">get</span>; <span class=\"kw\">set</span>; } = <span class=\"num\">100</span>;\n    <span class=\"kw\">public</span> <span class=\"kw\">void</span> <span class=\"fn\">Fly</span>() => Console.WriteLine(<span class=\"str\">\"鸭子在飞\"</span>);\n    <span class=\"kw\">public</span> <span class=\"kw\">void</span> <span class=\"fn\">Swim</span>() => Console.WriteLine(<span class=\"str\">\"鸭子在游\"</span>);\n}</pre></div><h4>常用内置接口</h4><table><tr><th>接口</th><th>用途</th><th>主要成员</th></tr><tr><td>IDisposable</td><td>释放资源</td><td>Dispose()</td></tr><tr><td>IComparable&lt;T&gt;</td><td>排序比较</td><td>CompareTo(T)</td></tr><tr><td>IEnumerable&lt;T&gt;</td><td>支持foreach</td><td>GetEnumerator()</td></tr><tr><td>IEquatable&lt;T&gt;</td><td>类型安全相等</td><td>Equals(T)</td></tr></table><div class=\"highlight-box\"><strong>接口vs抽象类：</strong>接口=can-do(能力)，抽象类=is-a(身份)。C#8+接口支持默认实现。接口成员默认public。命名以I开头。</div>"},
{id:"core-20",title:"密封方法",tags:["sealed","密封方法","override"],content:"<h4>密封方法(sealed override)</h4><p><code>sealed</code>配合<code>override</code>使用，阻止子类再次重写该方法。只能用于已经被override的方法。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"kw\">class</span> <span class=\"type\">Base</span>\n{\n    <span class=\"kw\">public</span> <span class=\"kw\">virtual</span> <span class=\"kw\">void</span> <span class=\"fn\">Method1</span>() { }\n    <span class=\"kw\">public</span> <span class=\"kw\">virtual</span> <span class=\"kw\">void</span> <span class=\"fn\">Method2</span>() { }\n}\n\n<span class=\"kw\">class</span> <span class=\"type\">Derived</span> : <span class=\"type\">Base</span>\n{\n    <span class=\"cm\">// sealed override：子类不可再重写</span>\n    <span class=\"kw\">public</span> <span class=\"kw\">sealed</span> <span class=\"kw\">override</span> <span class=\"kw\">void</span> <span class=\"fn\">Method1</span>()\n    {\n        <span class=\"kw\">base</span>.<span class=\"fn\">Method1</span>();\n        Console.WriteLine(<span class=\"str\">\"已密封\"</span>);\n    }\n\n    <span class=\"cm\">// 普通重写——可继续重写</span>\n    <span class=\"kw\">public</span> <span class=\"kw\">override</span> <span class=\"kw\">void</span> <span class=\"fn\">Method2</span>() { }\n}\n\n<span class=\"kw\">class</span> <span class=\"type\">GrandChild</span> : <span class=\"type\">Derived</span>\n{\n    <span class=\"cm\">// override void Method1() { } // 编译错误！已被密封</span>\n    <span class=\"kw\">public</span> <span class=\"kw\">override</span> <span class=\"kw\">void</span> <span class=\"fn\">Method2</span>() { }  <span class=\"cm\">// 可以</span>\n}</pre></div><div class=\"highlight-box\"><strong>使用场景：</strong>需要固定某个方法行为，不允许后续派生类再改变时。模板方法模式中固定算法骨架步骤。</div>"},
{id:"core-21",title:"命名空间",tags:["namespace","using","程序集"],content:"<h4>命名空间(Namespace)</h4><p>用于<strong>组织和管理代码</strong>，避免类名冲突，提供层次结构。类似文件系统中的文件夹。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"kw\">namespace</span> <span class=\"type\">MyApp.Models</span>\n{\n    <span class=\"kw\">public</span> <span class=\"kw\">class</span> <span class=\"type\">User</span>\n    {\n        <span class=\"kw\">public</span> <span class=\"kw\">int</span> Id { <span class=\"kw\">get</span>; <span class=\"kw\">set</span>; }\n        <span class=\"kw\">public</span> <span class=\"kw\">string</span> Name { <span class=\"kw\">get</span>; <span class=\"kw\">set</span>; }\n    }\n}\n\n<span class=\"cm\">// using指令</span>\n<span class=\"kw\">using</span> System;\n<span class=\"kw\">using</span> MyApp.Models;\n<span class=\"kw\">using</span> <span class=\"kw\">static</span> System.Math;     <span class=\"cm\">// 导入静态成员(C#6)</span>\n<span class=\"kw\">using</span> Timer = System.Timers.Timer; <span class=\"cm\">// 别名</span>\n\n<span class=\"cm\">// 文件作用域命名空间 (C# 10.0+)</span>\n<span class=\"kw\">namespace</span> <span class=\"type\">MyApp.Controllers</span>;\n<span class=\"cm\">// 整个文件属于此命名空间，无需{}</span></pre></div><table><tr><th>特性</th><th>命名空间</th><th>程序集</th></tr><tr><td>是什么</td><td>逻辑组织</td><td>物理部署(.dll/.exe)</td></tr><tr><td>关系</td><td>同程序集可有多命名空间</td><td>同命名空间可跨多程序集</td></tr></table><div class=\"highlight-box\"><strong>最佳实践：</strong>遵循公司名.项目名.模块层命名规范。使用C#10文件作用域命名空间减少缩进。</div>"},
{id:"core-22",title:"万物之父中的方法",tags:["Object","ToString","Equals","GetHashCode"],content:"<h4>重写Object方法</h4><p>Object提供了六个实例方法，开发中常需重写ToString、Equals、GetHashCode。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"kw\">class</span> <span class=\"type\">Person</span> : <span class=\"type\">IEquatable</span><<span class=\"type\">Person</span>>\n{\n    <span class=\"kw\">public</span> <span class=\"kw\">string</span> Name { <span class=\"kw\">get</span>; <span class=\"kw\">set</span>; }\n    <span class=\"kw\">public</span> <span class=\"kw\">int</span> Age { <span class=\"kw\">get</span>; <span class=\"kw\">set</span>; }\n\n    <span class=\"cm\">// 重写ToString：提供可读字符串表示</span>\n    <span class=\"kw\">public</span> <span class=\"kw\">override</span> <span class=\"kw\">string</span> <span class=\"fn\">ToString</span>()\n        => $<span class=\"str\">\"Person({Name}, {Age}岁)\"</span>;\n\n    <span class=\"cm\">// 重写Equals：自定义相等逻辑</span>\n    <span class=\"kw\">public</span> <span class=\"kw\">override</span> <span class=\"kw\">bool</span> <span class=\"fn\">Equals</span>(<span class=\"kw\">object</span> obj)\n        => obj <span class=\"kw\">is</span> <span class=\"type\">Person</span> p && Name == p.Name && Age == p.Age;\n\n    <span class=\"kw\">public</span> <span class=\"kw\">bool</span> <span class=\"fn\">Equals</span>(<span class=\"type\">Person</span> other)\n        => other != <span class=\"kw\">null</span> && Name == other.Name && Age == other.Age;\n\n    <span class=\"cm\">// 重写GetHashCode：重写Equals时必须重写</span>\n    <span class=\"kw\">public</span> <span class=\"kw\">override</span> <span class=\"kw\">int</span> <span class=\"fn\">GetHashCode</span>()\n        => <span class=\"type\">HashCode</span>.Combine(Name, Age);\n}</pre></div><table><tr><th>方法</th><th>签名</th><th>何时重写</th></tr><tr><td>ToString()</td><td>virtual string</td><td>需要可读字符串表示</td></tr><tr><td>Equals()</td><td>virtual bool</td><td>需要自定义相等逻辑</td></tr><tr><td>GetHashCode()</td><td>virtual int</td><td>重写Equals时几乎必须</td></tr><tr><td>GetType()</td><td>Type (non-virtual)</td><td>一般不重写</td></tr></table><div class=\"highlight-box\"><strong>规则：</strong>Equals和GetHashCode必须同重写。相等对象必须有相同哈希码。用HashCode.Combine生成组合哈希码。</div>"},
{id:"core-23",title:"string操作",tags:["string","字符串","不可变","方法"],content:"<h4>string特性</h4><p>string是<strong>不可变(Immutable)</strong>引用类型——一旦创建不能修改。任何修改操作都创建新字符串。但==比较的是内容（值语义）。</p><h4>常用方法</h4><table><tr><th>方法</th><th>说明</th><th>示例</th></tr><tr><td>Length</td><td>字符数</td><td>\"Hello\".Length => 5</td></tr><tr><td>IndexOf</td><td>查找子串位置</td><td>s.IndexOf(\"a\")</td></tr><tr><td>Substring</td><td>截取子串</td><td>s.Substring(0,3)</td></tr><tr><td>Split/Join</td><td>分割/连接</td><td>s.Split(\',\')</td></tr><tr><td>Replace</td><td>替换</td><td>s.Replace(\"a\",\"b\")</td></tr><tr><td>ToUpper/ToLower</td><td>大小写转换</td><td>s.ToUpper()</td></tr><tr><td>Trim</td><td>去空白</td><td>s.Trim()</td></tr><tr><td>Contains</td><td>包含判断</td><td>s.Contains(\"x\")</td></tr><tr><td>Format</td><td>格式化</td><td>string.Format(\"{0}\", x)</td></tr></table><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"kw\">string</span> s = <span class=\"str\">\"  Hello World  \"</span>;\n<span class=\"kw\">string</span> trimmed = s.<span class=\"fn\">Trim</span>();        <span class=\"cm\">// \"Hello World\"</span>\n<span class=\"kw\">string</span> upper = s.<span class=\"fn\">ToUpper</span>();        <span class=\"cm\">// \"  HELLO WORLD  \"</span>\n<span class=\"kw\">string</span>[] parts = <span class=\"str\">\"a,b,c\"</span>.<span class=\"fn\">Split</span>(<span class=\"str\">','</span>); <span class=\"cm\">// [\"a\",\"b\",\"c\"]</span>\n<span class=\"kw\">string</span> joined = <span class=\"kw\">string</span>.<span class=\"fn\">Join</span>(<span class=\"str\">\"-\"</span>, parts); <span class=\"cm\">// \"a-b-c\"</span>\n\n<span class=\"cm\">// 字符串插值（推荐）</span>\n<span class=\"kw\">string</span> name = <span class=\"str\">\"张三\"</span>;\n<span class=\"kw\">int</span> age = <span class=\"num\">25</span>;\n<span class=\"kw\">string</span> info = $<span class=\"str\">\"{name}, {age}岁\"</span>;</pre></div><div class=\"highlight-box\"><strong>不可变性：</strong>每次修改创建新对象。频繁拼接时用<code>StringBuilder</code>。字符串存在驻留池(Intern Pool)中。</div>"},
{id:"core-24",title:"StringBuilder",tags:["StringBuilder","字符串拼接","性能","可变"],content:"<h4>为什么需要StringBuilder</h4><p>string每次拼接都创建新对象。大量拼接时<code>StringBuilder</code>维护可变字符缓冲区，在内存中原地修改，极大提升性能。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"cm\">// 不推荐：每次+都创建新string</span>\n<span class=\"kw\">string</span> result = <span class=\"str\">\"\"</span>;\n<span class=\"kw\">for</span> (<span class=\"kw\">int</span> i = <span class=\"num\">0</span>; i < <span class=\"num\">10000</span>; i++)\n    result += i;  <span class=\"cm\">// 严重性能问题！</span>\n\n<span class=\"cm\">// 推荐：使用StringBuilder</span>\n<span class=\"kw\">var</span> sb = <span class=\"kw\">new</span> <span class=\"type\">System</span>.<span class=\"type\">Text</span>.<span class=\"type\">StringBuilder</span>();\n<span class=\"kw\">for</span> (<span class=\"kw\">int</span> i = <span class=\"num\">0</span>; i < <span class=\"num\">10000</span>; i++)\n    sb.<span class=\"fn\">Append</span>(i);\n<span class=\"kw\">string</span> result2 = sb.<span class=\"fn\">ToString</span>();\n\n<span class=\"cm\">// 也支持AppendLine、Insert、Remove、Replace</span>\nsb.<span class=\"fn\">AppendLine</span>(<span class=\"str\">\"第一行\"</span>);\nsb.<span class=\"fn\">Insert</span>(<span class=\"num\">0</span>, <span class=\"str\">\"开头:\"</span>);\nsb.<span class=\"fn\">Replace</span>(<span class=\"str\">\"a\"</span>, <span class=\"str\">\"b\"</span>);\nsb.<span class=\"fn\">Clear</span>();  <span class=\"cm\">// 清空</span></pre></div><div class=\"highlight-box\"><strong>性能提示：</strong>预知字符数时指定初始容量<code>new StringBuilder(1000)</code>避免内部缓冲区频繁扩容。少于5次拼接直接用+或$即可。</div>"},
{id:"core-25",title:"结构体和类的区别",tags:["struct","class","值类型","引用类型"],content:"<h4>struct vs class</h4><p>struct是<strong>值类型</strong>(栈分配)，class是<strong>引用类型</strong>(堆分配)。选择取决于语义和性能需求。</p><table><tr><th>特性</th><th>struct</th><th>class</th></tr><tr><td>类型</td><td>值类型</td><td>引用类型</td></tr><tr><td>内存</td><td>栈(或内联)</td><td>堆</td></tr><tr><td>null</td><td>不能(除非Nullable)</td><td>可null</td></tr><tr><td>继承</td><td>不支持</td><td>单继承</td></tr><tr><td>析构</td><td>不能有</td><td>可有</td></tr><tr><td>赋值</td><td>复制整个值</td><td>复制引用</td></tr><tr><td>默认构造</td><td>不能自定义(C#9前)</td><td>可以</td></tr></table><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"cm\">// struct：值类型</span>\n<span class=\"kw\">struct</span> <span class=\"type\">Point</span>\n{\n    <span class=\"kw\">public</span> <span class=\"kw\">int</span> X, Y;\n    <span class=\"kw\">public</span> <span class=\"fn\">Point</span>(<span class=\"kw\">int</span> x, <span class=\"kw\">int</span> y) { X = x; Y = y; }\n}\n\n<span class=\"type\">Point</span> p1 = <span class=\"kw\">new</span> <span class=\"type\">Point</span>(<span class=\"num\">1</span>, <span class=\"num\">2</span>);\n<span class=\"type\">Point</span> p2 = p1;  <span class=\"cm\">// 复制整个值！独立副本</span>\np2.X = <span class=\"num\">99</span>;  <span class=\"cm\">// p1.X仍为1</span>\n\n<span class=\"cm\">// class：引用类型</span>\n<span class=\"kw\">class</span> <span class=\"type\">Person</span> { <span class=\"kw\">public</span> <span class=\"kw\">string</span> Name; }\n<span class=\"type\">Person</span> per1 = <span class=\"kw\">new</span> <span class=\"type\">Person</span> { Name = <span class=\"str\">\"张三\"</span> };\n<span class=\"type\">Person</span> per2 = per1;  <span class=\"cm\">// 复制引用！同一对象</span>\nper2.Name = <span class=\"str\">\"李四\"</span>;  <span class=\"cm\">// per1.Name也变\"李四\"</span></pre></div><div class=\"highlight-box\"><strong>选择指南：</strong>小数据(<=16字节)、简单逻辑、不需继承时用struct。复杂业务对象用class。推荐使用<code>readonly struct</code>。</div>"},
{id:"core-26",title:"抽象类和接口的区别",tags:["抽象类","接口","区别","设计"],content:"<h4>核心区别</h4><p>抽象类表达<strong>is-a(身份)</strong>，接口表达<strong>can-do(能力)</strong>。这是两者最根本的设计哲学差异。</p><table><tr><th>维度</th><th>抽象类</th><th>接口</th></tr><tr><td>语义</td><td>is-a (是什么)</td><td>can-do (能做什么)</td></tr><tr><td>实现</td><td>可含字段/构造/已实现方法</td><td>不可含字段/构造(C#8前无实现)</td></tr><tr><td>继承</td><td>单继承</td><td>可实现多个</td></tr><tr><td>访问修饰符</td><td>成员可有各种修饰符</td><td>成员默认public</td></tr><tr><td>版本演进</td><td>加非抽象成员不影响子类</td><td>加成员破坏实现者(C#8默认实现缓解)</td></tr><tr><td>实例化</td><td>不能</td><td>不能</td></tr><tr><td>构造</td><td>可以有</td><td>不能有</td></tr></table><h4>选择指南</h4><table><tr><th>场景</th><th>推荐</th><th>原因</th></tr><tr><td>对象有共同状态和行为</td><td>抽象类</td><td>可共享字段和非抽象方法</td></tr><tr><td>不同类型需共同能力</td><td>接口</td><td>可跨继承层次</td></tr><tr><td>需多继承/多实现</td><td>接口</td><td>C#不支持多继承类</td></tr><tr><td>依赖注入/单元测试</td><td>接口</td><td>便于mock和替换</td></tr></table><div class=\"highlight-box\"><strong>现代C#趋势：</strong>C#8+接口支持默认实现。但语义区别仍关键。优先用接口作依赖抽象，需共享代码时用抽象类。</div>"}
]},{id:"advanced",title:"四、CSharp进阶教学",desc:"在掌握面向对象基础之后，本章深入C#高级特性。从泛型集合到底层数据结构，从委托事件到Lambda表达式，从多线程到反射特性，涵盖排序算法与协变逆变等进阶主题，全面提升编程能力。",badge:"进阶",lessons:[
{id:"adv-1",title:"ArrayList",tags:["ArrayList","非泛型","集合","装箱"],content:"<h4>ArrayList（非泛型动态数组）</h4><p>位于<code>System.Collections</code>，存储object类型元素。存在装箱/拆箱和类型不安全问题，已<strong>被List&lt;T&gt;取代</strong>。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"type\">ArrayList</span> list = <span class=\"kw\">new</span> <span class=\"type\">ArrayList</span>();\nlist.<span class=\"fn\">Add</span>(<span class=\"num\">10</span>);           <span class=\"cm\">// int装箱</span>\nlist.<span class=\"fn\">Add</span>(<span class=\"str\">\"Hello\"</span>);      <span class=\"cm\">// string不装箱</span>\nlist.<span class=\"fn\">Add</span>(<span class=\"num\">3.14</span>);         <span class=\"cm\">// double装箱</span>\n\n<span class=\"kw\">int</span> n = (<span class=\"kw\">int</span>)list[<span class=\"num\">0</span>];  <span class=\"cm\">// 拆箱+强制转换</span>\n\nlist.<span class=\"fn\">Insert</span>(<span class=\"num\">1</span>, <span class=\"str\">\"插入\"</span>);\nlist.<span class=\"fn\">Remove</span>(<span class=\"str\">\"Hello\"</span>);\nlist.<span class=\"fn\">RemoveAt</span>(<span class=\"num\">0</span>);\nlist.<span class=\"fn\">Clear</span>();</pre></div><div class=\"highlight-box\"><strong>为什么避免：</strong>类型不安全(编译不检查)、值类型装箱/拆箱(性能差)、现代C#用<code>List&lt;T&gt;</code>替代。仅在与旧.NET代码交互时可能遇到。</div>"},
{id:"adv-2",title:"Stack",tags:["Stack","栈","LIFO","后进先出"],content:"<h4>Stack&lt;T&gt; 栈</h4><p><strong>后进先出(LIFO)</strong>的数据结构。最后加入的元素最先取出。Push入栈、Pop出栈、Peek查看栈顶。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"type\">Stack</span><<span class=\"kw\">int</span>> stack = <span class=\"kw\">new</span> <span class=\"type\">Stack</span><<span class=\"kw\">int</span>>();\nstack.<span class=\"fn\">Push</span>(<span class=\"num\">10</span>);\nstack.<span class=\"fn\">Push</span>(<span class=\"num\">20</span>);\nstack.<span class=\"fn\">Push</span>(<span class=\"num\">30</span>);  <span class=\"cm\">// 栈顶->30,20,10<-栈底</span>\n\n<span class=\"kw\">int</span> top = stack.<span class=\"fn\">Peek</span>();  <span class=\"cm\">// 30(不弹出)</span>\n<span class=\"kw\">int</span> v1 = stack.<span class=\"fn\">Pop</span>();   <span class=\"cm\">// 30</span>\n<span class=\"kw\">int</span> v2 = stack.<span class=\"fn\">Pop</span>();   <span class=\"cm\">// 20</span>\n\n<span class=\"cm\">// 安全操作（空栈不抛异常）</span>\n<span class=\"kw\">if</span> (stack.<span class=\"fn\">TryPop</span>(<span class=\"kw\">out</span> <span class=\"kw\">int</span> val))\n    Console.WriteLine(val);</pre></div><h4>应用场景</h4><table><tr><th>场景</th><th>说明</th></tr><tr><td>括号匹配</td><td>验证{}[]()是否配对</td></tr><tr><td>撤销/重做</td><td>记录操作历史并回退</td></tr><tr><td>函数调用</td><td>方法调用和返回底层机制</td></tr><tr><td>DFS深度优先搜索</td><td>遍历树/图结构</td></tr><tr><td>表达式求值</td><td>中缀转后缀、计算器</td></tr></table>"},
{id:"adv-3",title:"Queue",tags:["Queue","队列","FIFO","先进先出"],content:"<h4>Queue&lt;T&gt; 队列</h4><p><strong>先进先出(FIFO)</strong>的数据结构。Enqueue入队(加队尾)、Dequeue出队(移除队首)、Peek查看队首。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"type\">Queue</span><<span class=\"kw\">string</span>> queue = <span class=\"kw\">new</span> <span class=\"type\">Queue</span><<span class=\"kw\">string</span>>();\nqueue.<span class=\"fn\">Enqueue</span>(<span class=\"str\">\"任务A\"</span>);\nqueue.<span class=\"fn\">Enqueue</span>(<span class=\"str\">\"任务B\"</span>);\nqueue.<span class=\"fn\">Enqueue</span>(<span class=\"str\">\"任务C\"</span>);\n\n<span class=\"kw\">string</span> first = queue.<span class=\"fn\">Peek</span>();  <span class=\"cm\">// \"任务A\"</span>\n<span class=\"kw\">string</span> t1 = queue.<span class=\"fn\">Dequeue</span>(); <span class=\"cm\">// \"任务A\"</span>\n<span class=\"kw\">string</span> t2 = queue.<span class=\"fn\">Dequeue</span>(); <span class=\"cm\">// \"任务B\"</span>\n\n<span class=\"cm\">// 安全出队</span>\n<span class=\"kw\">if</span> (queue.<span class=\"fn\">TryDequeue</span>(<span class=\"kw\">out</span> <span class=\"kw\">string</span> item))\n    Console.WriteLine($<span class=\"str\">\"处理: {item}\"</span>);</pre></div><h4>应用场景</h4><table><tr><th>场景</th><th>说明</th></tr><tr><td>消息队列</td><td>异步任务处理</td></tr><tr><td>BFS广度优先搜索</td><td>按层级遍历树/图</td></tr><tr><td>打印队列</td><td>按提交顺序打印</td></tr><tr><td>生产者-消费者</td><td>线程安全缓冲</td></tr><tr><td>网络请求队列</td><td>按序处理HTTP请求</td></tr></table>"},
{id:"adv-4",title:"Hashtable",tags:["Hashtable","哈希表","键值对","Dictionary"],content:"<h4>Hashtable（非泛型哈希表）</h4><p>键值对集合，通过哈希算法实现O(1)查找。键和值均为object，存在装箱/类型安全问题。已被<strong>Dictionary&lt;K,V&gt;取代</strong>。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"type\">Hashtable</span> ht = <span class=\"kw\">new</span> <span class=\"type\">Hashtable</span>();\nht.<span class=\"fn\">Add</span>(<span class=\"str\">\"name\"</span>, <span class=\"str\">\"张三\"</span>);\nht[<span class=\"str\">\"age\"</span>] = <span class=\"num\">25</span>;        <span class=\"cm\">// 索引器赋值（值类型装箱）</span>\n\n<span class=\"kw\">string</span> name = (<span class=\"kw\">string</span>)ht[<span class=\"str\">\"name\"</span>]; <span class=\"cm\">// 强制转换!</span>\n<span class=\"kw\">int</span> age = (<span class=\"kw\">int</span>)ht[<span class=\"str\">\"age\"</span>];       <span class=\"cm\">// 拆箱!</span>\n\n<span class=\"kw\">foreach</span> (<span class=\"type\">DictionaryEntry</span> e <span class=\"kw\">in</span> ht)\n    Console.WriteLine($<span class=\"str\">\"{e.Key}:{e.Value}\"</span>);</pre></div><div class=\"highlight-box\"><strong>替代方案：</strong>使用<code>Dictionary&lt;string, object&gt;</code>或更好的强类型<code>Dictionary&lt;string, int&gt;</code>。泛型字典提供编译时类型安全和更好的性能。</div>"},
{id:"adv-5",title:"泛型",tags:["泛型","Generic","List<T>","类型安全"],content:"<h4>什么是泛型(Generic)</h4><p>泛型允许定义<strong>类型参数化的</strong>类、方法、接口和委托。编译时确定具体类型，避免装箱拆箱，提供类型安全和更好的性能。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"cm\">// 泛型类</span>\n<span class=\"kw\">class</span> <span class=\"type\">MyList</span><<span class=\"type\">T</span>>\n{\n    <span class=\"kw\">private</span> <span class=\"type\">T</span>[] _items = <span class=\"kw\">new</span> <span class=\"type\">T</span>[<span class=\"num\">10</span>];\n    <span class=\"kw\">private</span> <span class=\"kw\">int</span> _count = <span class=\"num\">0</span>;\n\n    <span class=\"kw\">public</span> <span class=\"kw\">void</span> <span class=\"fn\">Add</span>(<span class=\"type\">T</span> item)\n    {\n        _items[_count++] = item;\n    }\n\n    <span class=\"kw\">public</span> <span class=\"type\">T</span> <span class=\"fn\">Get</span>(<span class=\"kw\">int</span> index) => _items[index];\n}\n\n<span class=\"cm\">// 使用泛型类</span>\n<span class=\"kw\">var</span> intList = <span class=\"kw\">new</span> <span class=\"type\">MyList</span><<span class=\"kw\">int</span>>();\nintList.<span class=\"fn\">Add</span>(<span class=\"num\">42</span>);  <span class=\"cm\">// T=int，无装箱</span>\n\n<span class=\"kw\">var</span> strList = <span class=\"kw\">new</span> <span class=\"type\">MyList</span><<span class=\"kw\">string</span>>();\nstrList.<span class=\"fn\">Add</span>(<span class=\"str\">\"Hello\"</span>);  <span class=\"cm\">// T=string</span>\n\n<span class=\"cm\">// 泛型方法</span>\n<span class=\"kw\">public</span> <span class=\"kw\">static</span> <span class=\"kw\">void</span> <span class=\"fn\">Swap</span><<span class=\"type\">T</span>>(<span class=\"kw\">ref</span> <span class=\"type\">T</span> a, <span class=\"kw\">ref</span> <span class=\"type\">T</span> b)\n{ <span class=\"type\">T</span> t = a; a = b; b = t; }</pre></div><div class=\"highlight-box\"><strong>泛型的优势：</strong>类型安全（编译时检查）、消除装箱拆箱（性能提升）、代码复用（一份代码支持多种类型）。.NET中List&lt;T&gt;、Dictionary&lt;K,V&gt;等都是泛型实现。</div>"},
{id:"adv-6",title:"泛型约束",tags:["泛型约束","where","new()","class","struct"],content:"<h4>泛型约束(Constraints)</h4><p>通过<code>where</code>关键字对类型参数施加限制，确保泛型参数满足特定条件。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"cm\">// class约束：T必须是引用类型</span>\n<span class=\"kw\">class</span> <span class=\"type\">Repository</span><<span class=\"type\">T</span>> <span class=\"kw\">where</span> <span class=\"type\">T</span> : <span class=\"kw\">class</span>\n{\n    <span class=\"kw\">public</span> <span class=\"type\">T</span> <span class=\"fn\">GetById</span>(<span class=\"kw\">int</span> id) => <span class=\"kw\">null</span>;\n}\n\n<span class=\"cm\">// struct约束：T必须是值类型</span>\n<span class=\"kw\">class</span> <span class=\"type\">Nullable</span><<span class=\"type\">T</span>> <span class=\"kw\">where</span> <span class=\"type\">T</span> : <span class=\"kw\">struct</span> { }\n\n<span class=\"cm\">// new()约束：T必须有无参构造函数</span>\n<span class=\"kw\">class</span> <span class=\"type\">Factory</span><<span class=\"type\">T</span>> <span class=\"kw\">where</span> <span class=\"type\">T</span> : <span class=\"kw\">new</span>()\n{\n    <span class=\"kw\">public</span> <span class=\"type\">T</span> <span class=\"fn\">Create</span>() => <span class=\"kw\">new</span> <span class=\"type\">T</span>();\n}\n\n<span class=\"cm\">// 基类约束：T必须是指定类或其子类</span>\n<span class=\"kw\">class</span> <span class=\"type\">AnimalContainer</span><<span class=\"type\">T</span>> <span class=\"kw\">where</span> <span class=\"type\">T</span> : <span class=\"type\">Animal</span> { }\n\n<span class=\"cm\">// 接口约束：T必须实现指定接口</span>\n<span class=\"kw\">class</span> <span class=\"type\">Sorter</span><<span class=\"type\">T</span>> <span class=\"kw\">where</span> <span class=\"type\">T</span> : <span class=\"type\">IComparable</span><<span class=\"type\">T</span>> { }\n\n<span class=\"cm\">// 多约束组合（new()必须放最后）</span>\n<span class=\"kw\">class</span> <span class=\"type\">MyClass</span><<span class=\"type\">T</span>> <span class=\"kw\">where</span> <span class=\"type\">T</span> : <span class=\"kw\">class</span>, <span class=\"type\">IDisposable</span>, <span class=\"kw\">new</span>() { }</pre></div><table><tr><th>约束</th><th>含义</th></tr><tr><td>where T : class</td><td>引用类型</td></tr><tr><td>where T : struct</td><td>值类型</td></tr><tr><td>where T : new()</td><td>有无参构造</td></tr><tr><td>where T : BaseClass</td><td>指定基类</td></tr><tr><td>where T : IInterface</td><td>实现指定接口</td></tr><tr><td>where T : unmanaged</td><td>非托管类型(C#7.3+)</td></tr></table>"},
{id:"adv-7",title:"List",tags:["List<T>","泛型","集合","动态数组"],content:"<h4>List&lt;T&gt;</h4><p><strong>最常用的泛型集合</strong>。动态数组实现，支持索引访问、自动扩容、类型安全。位于<code>System.Collections.Generic</code>。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"type\">List</span><<span class=\"kw\">string</span>> names = <span class=\"kw\">new</span> <span class=\"type\">List</span><<span class=\"kw\">string</span>>();\n\n<span class=\"cm\">// 添加</span>\nnames.<span class=\"fn\">Add</span>(<span class=\"str\">\"张三\"</span>);\nnames.<span class=\"fn\">AddRange</span>(<span class=\"kw\">new</span>[] { <span class=\"str\">\"李四\"</span>, <span class=\"str\">\"王五\"</span> });\n\n<span class=\"cm\">// 访问</span>\n<span class=\"kw\">string</span> first = names[<span class=\"num\">0</span>];  <span class=\"cm\">// 索引器</span>\n<span class=\"kw\">int</span> count = names.Count;\n\n<span class=\"cm\">// 查找</span>\n<span class=\"kw\">int</span> idx = names.<span class=\"fn\">IndexOf</span>(<span class=\"str\">\"李四\"</span>);\n<span class=\"kw\">bool</span> has = names.<span class=\"fn\">Contains</span>(<span class=\"str\">\"王五\"</span>);\n<span class=\"kw\">var</span> found = names.<span class=\"fn\">Find</span>(s => s.StartsWith(<span class=\"str\">\"李\"</span>));\n\n<span class=\"cm\">// 删除</span>\nnames.<span class=\"fn\">Remove</span>(<span class=\"str\">\"张三\"</span>);\nnames.<span class=\"fn\">RemoveAt</span>(<span class=\"num\">0</span>);\nnames.<span class=\"fn\">RemoveAll</span>(s => s.Length < <span class=\"num\">2</span>);\n\n<span class=\"cm\">// 遍历</span>\nnames.<span class=\"fn\">ForEach</span>(s => Console.WriteLine(s));\n\n<span class=\"cm\">// 转换</span>\n<span class=\"kw\">string</span>[] arr = names.<span class=\"fn\">ToArray</span>();\n\n<span class=\"cm\">// 排序</span>\nnames.<span class=\"fn\">Sort</span>();  <span class=\"cm\">// 自然排序</span>\nnames.<span class=\"fn\">Sort</span>((a, b) => b.CompareTo(a));  <span class=\"cm\">// 降序</span></pre></div><div class=\"highlight-box\"><strong>内部实现：</strong>List&lt;T&gt;内部是T[]数组，默认容量为4，容量不足时扩容为原来的2倍。已知元素数量时可用<code>new List&lt;T&gt;(capacity)</code>指定初始容量避免多次扩容。</div>"},
{id:"adv-8",title:"Dictionary",tags:["Dictionary","哈希表","键值对","泛型"],content:"<h4>Dictionary&lt;TKey,TValue&gt;</h4><p>泛型键值对集合，基于哈希表实现O(1)的增删查性能。是C#中最常用的字典类型。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"cm\">// 声明</span>\n<span class=\"kw\">var</span> dict = <span class=\"kw\">new</span> <span class=\"type\">Dictionary</span><<span class=\"kw\">string</span>, <span class=\"kw\">int</span>>();\n\n<span class=\"cm\">// 添加</span>\ndict[<span class=\"str\">\"apple\"</span>] = <span class=\"num\">10</span>;           <span class=\"cm\">// 索引器（存在则覆盖）</span>\ndict.<span class=\"fn\">Add</span>(<span class=\"str\">\"banana\"</span>, <span class=\"num\">20</span>);       <span class=\"cm\">// Add（重复key会抛异常）</span>\ndict.<span class=\"fn\">TryAdd</span>(<span class=\"str\">\"cherry\"</span>, <span class=\"num\">30</span>);  <span class=\"cm\">// 安全添加</span>\n\n<span class=\"cm\">// 访问</span>\n<span class=\"kw\">int</span> val = dict[<span class=\"str\">\"apple\"</span>];      <span class=\"cm\">// key不存在抛KeyNotFoundException</span>\n<span class=\"kw\">if</span> (dict.<span class=\"fn\">TryGetValue</span>(<span class=\"str\">\"banana\"</span>, <span class=\"kw\">out</span> <span class=\"kw\">int</span> v))\n    Console.WriteLine(v);        <span class=\"cm\">// 安全访问</span>\n\n<span class=\"cm\">// 检查</span>\n<span class=\"kw\">bool</span> hasKey = dict.<span class=\"fn\">ContainsKey</span>(<span class=\"str\">\"apple\"</span>);\n<span class=\"kw\">bool</span> hasVal = dict.<span class=\"fn\">ContainsValue</span>(<span class=\"num\">20</span>);\n\n<span class=\"cm\">// 删除</span>\ndict.<span class=\"fn\">Remove</span>(<span class=\"str\">\"cherry\"</span>);\ndict.<span class=\"fn\">Clear</span>();\n\n<span class=\"cm\">// 遍历</span>\n<span class=\"kw\">foreach</span> (<span class=\"kw\">var</span> kv <span class=\"kw\">in</span> dict)\n    Console.WriteLine($<span class=\"str\">\"{kv.Key}:{kv.Value}\"</span>);\n\n<span class=\"cm\">// 获取所有键/值</span>\n<span class=\"kw\">var</span> keys = dict.Keys;\n<span class=\"kw\">var</span> values = dict.Values;</pre></div><div class=\"highlight-box\"><strong>内部原理：</strong>基于哈希桶+链表解决冲突。GetHashCode()确定桶位置，Equals()处理冲突。自定义类作Key时必须正确实现这两个方法。建议用不可变类型作Key。</div>"},
{id:"adv-9",title:"顺序存储和链式存储",tags:["顺序存储","链式存储","数组","链表","内存"],content:"<h4>顺序存储（数组）</h4><p>元素在内存中<strong>连续排列</strong>。支持O(1)随机访问，但插入删除需要移动元素O(n)。需要预分配连续内存空间。</p><h4>链式存储（链表）</h4><p>元素在内存中<strong>分散存储</strong>，通过指针连接。插入删除O(1)（已知位置），但随机访问需遍历O(n)。</p><table><tr><th>特性</th><th>顺序存储(数组)</th><th>链式存储(链表)</th></tr><tr><td>内存</td><td>连续分配</td><td>分散分配+指针</td></tr><tr><td>随机访问</td><td>O(1)</td><td>O(n)</td></tr><tr><td>插入/删除</td><td>O(n)（移动元素）</td><td>O(1)（改指针）</td></tr><tr><td>内存利用率</td><td>高（仅存数据）</td><td>低（存数据+指针）</td></tr><tr><td>缓存友好</td><td>是（局部性原理）</td><td>否（跳跃访问）</td></tr><tr><td>C#实现</td><td>Array, List&lt;T&gt;</td><td>LinkedList&lt;T&gt;</td></tr></table><div class=\"highlight-box\"><strong>选择建议：</strong>频繁随机访问->数组；频繁插入删除->链表；元素数量固定->数组；需要缓存性能->数组。.NET中List&lt;T&gt;使用顺序存储，LinkedList&lt;T&gt;使用链式存储。</div>"},
{id:"adv-10",title:"LinkedList",tags:["LinkedList","双向链表","节点","链式存储"],content:"<h4>LinkedList&lt;T&gt;</h4><p>泛型<strong>双向链表</strong>。每个节点包含数据和前后引用。插入删除O(1)（已知节点位置），但不支持索引访问。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"kw\">var</span> list = <span class=\"kw\">new</span> <span class=\"type\">LinkedList</span><<span class=\"kw\">int</span>>();\n\n<span class=\"cm\">// 添加</span>\n<span class=\"kw\">var</span> node1 = list.<span class=\"fn\">AddFirst</span>(<span class=\"num\">10</span>);    <span class=\"cm\">// 加到头部</span>\n<span class=\"kw\">var</span> node2 = list.<span class=\"fn\">AddLast</span>(<span class=\"num\">30</span>);     <span class=\"cm\">// 加到尾部</span>\nlist.<span class=\"fn\">AddAfter</span>(node1, <span class=\"num\">20</span>);           <span class=\"cm\">// 在node1后添加</span>\nlist.<span class=\"fn\">AddBefore</span>(node2, <span class=\"num\">25</span>);         <span class=\"cm\">// 在node2前添加</span>\n\n<span class=\"cm\">// 节点属性</span>\n<span class=\"kw\">var</span> first = list.First;    <span class=\"cm\">// 首节点</span>\n<span class=\"kw\">var</span> last = list.Last;      <span class=\"cm\">// 尾节点</span>\n<span class=\"kw\">int</span> val = first.Value;      <span class=\"cm\">// 10</span>\n<span class=\"kw\">var</span> next = first.Next;      <span class=\"cm\">// 下一个节点</span>\n<span class=\"kw\">var</span> prev = first.Previous;  <span class=\"cm\">// 前一个节点(null)</span>\n\n<span class=\"cm\">// 删除</span>\nlist.<span class=\"fn\">Remove</span>(node1);\nlist.<span class=\"fn\">RemoveFirst</span>();\nlist.<span class=\"fn\">RemoveLast</span>();\n\n<span class=\"cm\">// 遍历</span>\n<span class=\"kw\">foreach</span> (<span class=\"kw\">int</span> item <span class=\"kw\">in</span> list)\n    Console.WriteLine(item);</pre></div><div class=\"highlight-box\"><strong>性能特征：</strong>无法通过索引访问（没有list[0]）。插入删除O(1)。占用更多内存（节点存前后引用）。迭代不产生GC（枚举器是struct）。</div>"},{id:"adv-11",title:"泛型栈和队列",tags:["Stack<T>","Queue<T>","泛型"],content:"<h4>泛型Stack&lt;T&gt;和Queue&lt;T&gt;</h4><p>与ArrayList/Hashtable不同，Stack和Queue在.NET 2.0已经有了泛型版本。泛型版本类型安全、无装箱。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"cm\">// 泛型栈 Stack<T></span>\n<span class=\"kw\">var</span> stack = <span class=\"kw\">new</span> <span class=\"type\">Stack</span><<span class=\"kw\">int</span>>();\nstack.<span class=\"fn\">Push</span>(<span class=\"num\">1</span>); stack.<span class=\"fn\">Push</span>(<span class=\"num\">2</span>); stack.<span class=\"fn\">Push</span>(<span class=\"num\">3</span>);\n<span class=\"kw\">while</span> (stack.<span class=\"fn\">TryPop</span>(<span class=\"kw\">out</span> <span class=\"kw\">int</span> v))\n    Console.WriteLine(v);  <span class=\"cm\">// 3, 2, 1</span>\n\n<span class=\"cm\">// 泛型队列 Queue<T></span>\n<span class=\"kw\">var</span> queue = <span class=\"kw\">new</span> <span class=\"type\">Queue</span><<span class=\"kw\">string</span>>();\nqueue.<span class=\"fn\">Enqueue</span>(<span class=\"str\">\"A\"</span>); queue.<span class=\"fn\">Enqueue</span>(<span class=\"str\">\"B\"</span>); queue.<span class=\"fn\">Enqueue</span>(<span class=\"str\">\"C\"</span>);\n<span class=\"kw\">while</span> (queue.<span class=\"fn\">TryDequeue</span>(<span class=\"kw\">out</span> <span class=\"kw\">string</span> s))\n    Console.WriteLine(s);  <span class=\"cm\">// A, B, C</span></pre></div><h4>非泛型 vs 泛型</h4><table><tr><th>特性</th><th>非泛型(Stack/Queue)</th><th>泛型(Stack&lt;T&gt;/Queue&lt;T&gt;)</th></tr><tr><td>类型安全</td><td>否(object)</td><td>是(编译时)</td></tr><tr><td>装箱</td><td>值类型会装箱</td><td>无装箱</td></tr><tr><td>安全方法</td><td>无TryPeek/TryPop</td><td>有TryPeek/TryPop/TryDequeue</td></tr><tr><td>命名空间</td><td>System.Collections</td><td>System.Collections.Generic</td></tr></table>"},
{id:"adv-12",title:"委托",tags:["委托","delegate","回调","多播"],content:"<h4>什么是委托(Delegate)</h4><p>委托是<strong>类型安全的函数指针</strong>——它可以持有对一个或多个方法的引用。委托定义了方法的签名（参数和返回值），只能引用签名匹配的方法。是事件和回调的基础。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"cm\">// 声明委托类型</span>\n<span class=\"kw\">public</span> <span class=\"kw\">delegate</span> <span class=\"kw\">int</span> <span class=\"type\">MathOperation</span>(<span class=\"kw\">int</span> a, <span class=\"kw\">int</span> b);\n\n<span class=\"cm\">// 匹配委托签名的方法</span>\n<span class=\"kw\">static</span> <span class=\"kw\">int</span> <span class=\"fn\">Add</span>(<span class=\"kw\">int</span> x, <span class=\"kw\">int</span> y) => x + y;\n<span class=\"kw\">static</span> <span class=\"kw\">int</span> <span class=\"fn\">Multiply</span>(<span class=\"kw\">int</span> x, <span class=\"kw\">int</span> y) => x * y;\n\n<span class=\"cm\">// 创建委托实例（指向方法）</span>\n<span class=\"type\">MathOperation</span> op = <span class=\"kw\">new</span> <span class=\"type\">MathOperation</span>(Add);\n<span class=\"cm\">// 或者直接赋值</span>\n<span class=\"type\">MathOperation</span> op2 = Multiply;\n\n<span class=\"cm\">// 调用委托（像调用方法一样）</span>\n<span class=\"kw\">int</span> result1 = op(<span class=\"num\">3</span>, <span class=\"num\">5</span>);   <span class=\"cm\">// 8</span>\n<span class=\"kw\">int</span> result2 = op2(<span class=\"num\">3</span>, <span class=\"num\">5</span>);  <span class=\"cm\">// 15</span>\n\n<span class=\"cm\">// 多播委托：一个委托持有多个方法</span>\n<span class=\"type\">MathOperation</span> multi = Add;\nmulti += Multiply;  <span class=\"cm\">// 添加第二个方法</span>\nmulti += Add;       <span class=\"cm\">// 再添加</span>\n<span class=\"kw\">int</span> last = multi(<span class=\"num\">2</span>, <span class=\"num\">3</span>); <span class=\"cm\">// 依次调用Add(5)->Multiply(6)->Add(5)，返回最后结果5</span></pre></div><h4>内置泛型委托</h4><table><tr><th>委托类型</th><th>签名</th><th>用途</th></tr><tr><td>Action</td><td>void()</td><td>无返回值无参</td></tr><tr><td>Action&lt;T&gt;</td><td>void(T)</td><td>无返回值有参</td></tr><tr><td>Func&lt;TResult&gt;</td><td>TResult()</td><td>有返回值无参</td></tr><tr><td>Func&lt;T,TResult&gt;</td><td>TResult(T)</td><td>有返回值有参</td></tr><tr><td>Predicate&lt;T&gt;</td><td>bool(T)</td><td>返回bool的判断</td></tr></table>"},
{id:"adv-13",title:"事件",tags:["事件","event","发布订阅","EventHandler"],content:"<h4>什么是事件(Event)</h4><p>事件是基于委托的<strong>发布-订阅</strong>机制。对象(发布者)通知其他对象(订阅者)发生了某事。事件是委托的封装，外部只能+=和-=而不能直接赋值或调用。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"cm\">// 使用内置EventHandler</span>\n<span class=\"kw\">class</span> <span class=\"type\">Button</span>\n{\n    <span class=\"cm\">// 声明事件（基于EventHandler委托）</span>\n    <span class=\"kw\">public</span> <span class=\"kw\">event</span> <span class=\"type\">EventHandler</span> Click;\n\n    <span class=\"kw\">public</span> <span class=\"kw\">void</span> <span class=\"fn\">OnClick</span>()\n    {\n        <span class=\"cm\">// 触发事件（?. 保证无订阅者时安全）</span>\n        Click?.<span class=\"fn\">Invoke</span>(<span class=\"kw\">this</span>, <span class=\"type\">EventArgs</span>.Empty);\n    }\n}\n\n<span class=\"cm\">// 自定义事件参数</span>\n<span class=\"kw\">class</span> <span class=\"type\">ScoreEventArgs</span> : <span class=\"type\">EventArgs</span>\n{\n    <span class=\"kw\">public</span> <span class=\"kw\">int</span> Score { <span class=\"kw\">get</span>; }\n    <span class=\"kw\">public</span> <span class=\"fn\">ScoreEventArgs</span>(<span class=\"kw\">int</span> s) => Score = s;\n}\n\n<span class=\"kw\">class</span> <span class=\"type\">Player</span>\n{\n    <span class=\"kw\">public</span> <span class=\"kw\">event</span> <span class=\"type\">EventHandler</span><<span class=\"type\">ScoreEventArgs</span>> Scored;\n\n    <span class=\"kw\">public</span> <span class=\"kw\">void</span> <span class=\"fn\">AddScore</span>(<span class=\"kw\">int</span> points)\n    {\n        Scored?.<span class=\"fn\">Invoke</span>(<span class=\"kw\">this</span>, <span class=\"kw\">new</span> <span class=\"type\">ScoreEventArgs</span>(points));\n    }\n}\n\n<span class=\"cm\">// 订阅事件</span>\n<span class=\"kw\">var</span> btn = <span class=\"kw\">new</span> <span class=\"type\">Button</span>();\nbtn.Click += (s, e) => Console.WriteLine(<span class=\"str\">\"被点击了!\"</span>);\nbtn.<span class=\"fn\">OnClick</span>();  <span class=\"cm\">// 输出: 被点击了!</span>\n\n<span class=\"cm\">// 取消订阅</span>\nbtn.Click -= handler;  <span class=\"cm\">// 防止内存泄漏</span></pre></div><div class=\"highlight-box\"><strong>注意：</strong>事件只能在声明类内部触发(Invoke)，外部只能+=和-=。长期订阅而不取消会导致内存泄漏（发布者持有订阅者引用）。总是使用<code>?.Invoke</code>安全触发事件。</div>"},
{id:"adv-14",title:"匿名函数",tags:["匿名函数","delegate","匿名方法"],content:"<h4>匿名方法(Anonymous Method)</h4><p>C# 2.0引入的匿名方法允许<strong>内联定义</strong>委托方法，无需单独声明命名方法。C# 3.0引入的Lambda表达式已基本取代匿名方法，但匿名方法语法在旧代码中仍可见。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"cm\">// 传统方式：命名方法</span>\n<span class=\"kw\">static</span> <span class=\"kw\">int</span> <span class=\"fn\">Square</span>(<span class=\"kw\">int</span> x) => x * x;\n<span class=\"type\">Func</span><<span class=\"kw\">int</span>, <span class=\"kw\">int</span>> f1 = Square;\n\n<span class=\"cm\">// 匿名方法 (C# 2.0)</span>\n<span class=\"type\">Func</span><<span class=\"kw\">int</span>, <span class=\"kw\">int</span>> f2 = <span class=\"kw\">delegate</span>(<span class=\"kw\">int</span> x) { <span class=\"kw\">return</span> x * x; };\n\n<span class=\"cm\">// Lambda表达式 (C# 3.0) —— 推荐方式</span>\n<span class=\"type\">Func</span><<span class=\"kw\">int</span>, <span class=\"kw\">int</span>> f3 = x => x * x;\n\n<span class=\"cm\">// 匿名方法可省略参数（如不需要时）</span>\n<span class=\"type\">Action</span> act = <span class=\"kw\">delegate</span> { Console.WriteLine(<span class=\"str\">\"Hello\"</span>); };\n\n<span class=\"cm\">// 闭包：匿名方法捕获外部变量</span>\n<span class=\"kw\">int</span> factor = <span class=\"num\">10</span>;\n<span class=\"type\">Func</span><<span class=\"kw\">int</span>, <span class=\"kw\">int</span>> multiply = <span class=\"kw\">delegate</span>(<span class=\"kw\">int</span> x) { <span class=\"kw\">return</span> x * factor; };\nConsole.WriteLine(multiply(<span class=\"num\">5</span>));  <span class=\"cm\">// 50</span></pre></div><div class=\"highlight-box\"><strong>演进历史：</strong>C# 1.0 只有命名方法委托 -> C# 2.0 匿名方法(delegate{}) -> C# 3.0 Lambda表达式(=>)。现代C#中始终使用Lambda表达式代替匿名方法。</div>"},
{id:"adv-15",title:"Lambda表达式",tags:["Lambda","=>","表达式树","闭包"],content:"<h4>Lambda表达式</h4><p>Lambda是<strong>简洁的匿名函数</strong>表示法，使用<code>=></code>运算符。可用于创建委托或表达式树(Expression Tree)。是LINQ的核心语法。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"cm\">// 表达式Lambda（单行）</span>\n<span class=\"type\">Func</span><<span class=\"kw\">int</span>, <span class=\"kw\">int</span>> square = x => x * x;\n<span class=\"type\">Func</span><<span class=\"kw\">int</span>, <span class=\"kw\">int</span>, <span class=\"kw\">int</span>> add = (a, b) => a + b;\n<span class=\"type\">Action</span><<span class=\"kw\">string</span>> print = s => Console.WriteLine(s);\n\n<span class=\"cm\">// 语句Lambda（多行）</span>\n<span class=\"type\">Func</span><<span class=\"kw\">int</span>, <span class=\"kw\">int</span>> factorial = n =>\n{\n    <span class=\"kw\">int</span> result = <span class=\"num\">1</span>;\n    <span class=\"kw\">for</span> (<span class=\"kw\">int</span> i = <span class=\"num\">2</span>; i <= n; i++)\n        result *= i;\n    <span class=\"kw\">return</span> result;\n};\n\n<span class=\"cm\">// 闭包：捕获外部变量</span>\n<span class=\"kw\">int</span> threshold = <span class=\"num\">100</span>;\n<span class=\"type\">Predicate</span><<span class=\"kw\">int</span>> isLarge = x => x > threshold;\n\n<span class=\"cm\">// LINQ中的Lambda</span>\n<span class=\"kw\">var</span> names = <span class=\"kw\">new</span>[] { <span class=\"str\">\"Alice\"</span>, <span class=\"str\">\"Bob\"</span>, <span class=\"str\">\"Charlie\"</span> };\n<span class=\"kw\">var</span> longNames = names.<span class=\"fn\">Where</span>(n => n.Length > <span class=\"num\">3</span>)\n                       .<span class=\"fn\">OrderBy</span>(n => n)\n                       .<span class=\"fn\">Select</span>(n => n.ToUpper());\n\n<span class=\"cm\">// 表达式树（用于EF Core等ORM）</span>\n<span class=\"type\">Expression</span><<span class=\"type\">Func</span><<span class=\"kw\">int</span>, <span class=\"kw\">bool</span>>> expr = x => x > <span class=\"num\">5</span>;\n<span class=\"cm\">// expr可以被翻译成SQL WHERE子句</span></pre></div><div class=\"highlight-box\"><strong>闭包陷阱：</strong>Lambda捕获的是变量的引用而非值。在for循环中使用Lambda需注意——所有Lambda共享同一个循环变量（C#5前）。C#5+的foreach中每次迭代获取独立变量。</div>"},
{id:"adv-16",title:"List排序",tags:["Sort","排序","IComparable","Comparison"],content:"<h4>List&lt;T&gt;排序</h4><p>List&lt;T&gt;提供多种排序方式：Sort()使用自然排序、Sort(Comparison&lt;T&gt;)使用自定义比较、以及LINQ的OrderBy。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"kw\">var</span> nums = <span class=\"kw\">new</span> <span class=\"type\">List</span><<span class=\"kw\">int</span>> { <span class=\"num\">5</span>, <span class=\"num\">2</span>, <span class=\"num\">8</span>, <span class=\"num\">1</span>, <span class=\"num\">9</span> };\n\n<span class=\"cm\">// 1. 自然排序（升序）</span>\nnums.<span class=\"fn\">Sort</span>();  <span class=\"cm\">// {1,2,5,8,9}</span>\n\n<span class=\"cm\">// 2. Lambda比较（降序）</span>\nnums.<span class=\"fn\">Sort</span>((a, b) => b.CompareTo(a));  <span class=\"cm\">// {9,8,5,2,1}</span>\n\n<span class=\"cm\">// 3. IComparer<T>（复杂排序）</span>\n<span class=\"kw\">class</span> <span class=\"type\">Person</span>\n{\n    <span class=\"kw\">public</span> <span class=\"kw\">string</span> Name;\n    <span class=\"kw\">public</span> <span class=\"kw\">int</span> Age;\n}\n<span class=\"kw\">var</span> people = <span class=\"kw\">new</span> <span class=\"type\">List</span><<span class=\"type\">Person</span>> { ... };\n\n<span class=\"cm\">// 先按Age再按Name排序</span>\npeople.<span class=\"fn\">Sort</span>((a, b) =>\n{\n    <span class=\"kw\">int</span> ageCompare = a.Age.CompareTo(b.Age);\n    <span class=\"kw\">if</span> (ageCompare != <span class=\"num\">0</span>) <span class=\"kw\">return</span> ageCompare;\n    <span class=\"kw\">return</span> a.Name.CompareTo(b.Name);\n});\n\n<span class=\"cm\">// 4. LINQ排序（不修改原列表）</span>\n<span class=\"kw\">var</span> sorted = people.<span class=\"fn\">OrderBy</span>(p => p.Age)\n                       .<span class=\"fn\">ThenBy</span>(p => p.Name)\n                       .<span class=\"fn\">ToList</span>();\n\n<span class=\"cm\">// 降序</span>\n<span class=\"kw\">var</span> desc = people.<span class=\"fn\">OrderByDescending</span>(p => p.Age).<span class=\"fn\">ToList</span>();</pre></div><div class=\"highlight-box\"><strong>性能：</strong>List&lt;T&gt;.Sort()使用快速排序(QuickSort)，平均O(n log n)。LINQ的OrderBy也使用快速排序但会创建新集合。频繁排序场景优先用Sort()修改原列表。</div>"},
{id:"adv-17",title:"协变逆变",tags:["协变","逆变","out","in","泛型"],content:"<h4>协变(Covariance)和逆变(Contravariance)</h4><p>协变(<code>out</code>)允许使用更<strong>具体</strong>的类型代替泛型参数；逆变(<code>in</code>)允许使用更<strong>通用</strong>的类型。仅在<strong>接口和委托</strong>的泛型参数上使用。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"cm\">// 协变 out：输出（返回值）</span>\n<span class=\"kw\">interface</span> <span class=\"type\">IProducer</span><<span class=\"kw\">out</span> <span class=\"type\">T</span>>\n{\n    <span class=\"type\">T</span> <span class=\"fn\">Produce</span>();  <span class=\"cm\">// T只能出现在返回值位置</span>\n}\n\n<span class=\"cm\">// 逆变 in：输入（参数）</span>\n<span class=\"kw\">interface</span> <span class=\"type\">IConsumer</span><<span class=\"kw\">in</span> <span class=\"type\">T</span>>\n{\n    <span class=\"kw\">void</span> <span class=\"fn\">Consume</span>(<span class=\"type\">T</span> item);  <span class=\"cm\">// T只能出现在参数位置</span>\n}\n\n<span class=\"cm\">// 协变示例</span>\n<span class=\"type\">IEnumerable</span><<span class=\"kw\">string</span>> strings = <span class=\"kw\">new</span> <span class=\"type\">List</span><<span class=\"kw\">string</span>>();\n<span class=\"type\">IEnumerable</span><<span class=\"kw\">object</span>> objects = strings;  <span class=\"cm\">// 协变！</span>\n<span class=\"cm\">// IEnumerable<T>定义为 IEnumerable<out T></span>\n\n<span class=\"cm\">// 逆变示例</span>\n<span class=\"type\">Action</span><<span class=\"kw\">object</span>> objAction = obj => Console.WriteLine(obj);\n<span class=\"type\">Action</span><<span class=\"kw\">string</span>> strAction = objAction;  <span class=\"cm\">// 逆变！</span>\n<span class=\"cm\">// Action<T>定义为 Action<in T></span>\n\n<span class=\"cm\">// 委托的协变逆变</span>\n<span class=\"type\">Func</span><<span class=\"kw\">object</span>> getObj = () => <span class=\"str\">\"hello\"</span>;\n<span class=\"type\">Func</span><<span class=\"kw\">string</span>> getStr = getObj;  <span class=\"cm\">// 协变(返回值)</span></pre></div><table><tr><th>关键字</th><th>方向</th><th>允许位置</th><th>示例</th></tr><tr><td>out</td><td>协变</td><td>仅返回值</td><td>IEnumerable&lt;out T&gt;</td></tr><tr><td>in</td><td>逆变</td><td>仅参数</td><td>Action&lt;in T&gt;</td></tr></table><div class=\"highlight-box\"><strong>记忆技巧：</strong>协变(out) => output => 子类转父类；逆变(in) => input => 父类转子类。只适用于接口和委托。</div>"},
{id:"adv-18",title:"多线程",tags:["多线程","Thread","Task","async","await"],content:"<h4>多线程编程演进</h4><p>C#多线程经历了Thread -> ThreadPool -> Task -> async/await的演进。现代C#推荐使用<strong>Task + async/await</strong>模式。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"cm\">// 1. Thread（底层，一般不直接用）</span>\n<span class=\"kw\">var</span> thread = <span class=\"kw\">new</span> <span class=\"type\">Thread</span>(() =>\n{\n    Console.WriteLine(<span class=\"str\">\"工作线程\"</span>);\n});\nthread.<span class=\"fn\">Start</span>();\nthread.<span class=\"fn\">Join</span>();  <span class=\"cm\">// 等待线程结束</span>\n\n<span class=\"cm\">// 2. Task（推荐）</span>\n<span class=\"type\">Task</span> task = <span class=\"type\">Task</span>.<span class=\"fn\">Run</span>(() =>\n{\n    <span class=\"cm\">// 计算密集型工作</span>\n    Console.WriteLine(<span class=\"str\">\"任务执行中\"</span>);\n});\n<span class=\"kw\">await</span> task;  <span class=\"cm\">// 不阻塞等待</span>\n\n<span class=\"cm\">// 3. async/await（最简单）</span>\n<span class=\"kw\">async</span> <span class=\"type\">Task</span><<span class=\"kw\">string</span>> <span class=\"fn\">DownloadAsync</span>(<span class=\"kw\">string</span> url)\n{\n    <span class=\"kw\">using</span> <span class=\"kw\">var</span> client = <span class=\"kw\">new</span> <span class=\"type\">HttpClient</span>();\n    <span class=\"kw\">return</span> <span class=\"kw\">await</span> client.<span class=\"fn\">GetStringAsync</span>(url);\n}\n\n<span class=\"cm\">// 并行执行多个任务</span>\n<span class=\"kw\">var</span> tasks = <span class=\"kw\">new</span>[]\n{\n    <span class=\"fn\">DownloadAsync</span>(<span class=\"str\">\"url1\"</span>),\n    <span class=\"fn\">DownloadAsync</span>(<span class=\"str\">\"url2\"</span>),\n    <span class=\"fn\">DownloadAsync</span>(<span class=\"str\">\"url3\"</span>)\n};\n<span class=\"kw\">var</span> results = <span class=\"kw\">await</span> <span class=\"type\">Task</span>.<span class=\"fn\">WhenAll</span>(tasks);\n\n<span class=\"cm\">// Task常用API</span>\n<span class=\"type\">Task</span>.<span class=\"fn\">Delay</span>(<span class=\"num\">1000</span>);            <span class=\"cm\">// 延迟1秒</span>\n<span class=\"type\">Task</span>.<span class=\"fn\">WhenAll</span>(t1, t2, t3);    <span class=\"cm\">// 全部完成</span>\n<span class=\"type\">Task</span>.<span class=\"fn\">WhenAny</span>(t1, t2, t3);    <span class=\"cm\">// 任一完成</span>\n<span class=\"type\">Task</span>.<span class=\"fn\">FromResult</span>(<span class=\"num\">42</span>);        <span class=\"cm\">// 已完成任务</span></pre></div><div class=\"highlight-box\"><strong>核心原则：</strong>async/await不创建新线程，它让当前线程在等待IO时释放去做其他事。计算密集用Task.Run，IO密集用async/await。async void只能用于事件处理器。</div>"},
{id:"adv-19",title:"预处理器指令",tags:["预处理器","#if","#define","#region"],content:"<h4>预处理器指令</h4><p>以<code>#</code>开头的特殊指令，在<strong>编译前</strong>处理。用于条件编译、代码折叠、诊断等。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"cm\">// 条件编译</span>\n<span class=\"kw\">#define</span> DEBUG  <span class=\"cm\">// 定义符号（必须在文件最顶部）</span>\n\n<span class=\"kw\">#if</span> DEBUG\n    Console.WriteLine(<span class=\"str\">\"调试模式\"</span>);\n<span class=\"kw\">#elif</span> RELEASE\n    Console.WriteLine(<span class=\"str\">\"发布模式\"</span>);\n<span class=\"kw\">#else</span>\n    Console.WriteLine(<span class=\"str\">\"其他模式\"</span>);\n<span class=\"kw\">#endif</span>\n\n<span class=\"cm\">// 代码折叠</span>\n<span class=\"kw\">#region</span> 数据库操作\n<span class=\"kw\">public</span> <span class=\"kw\">void</span> <span class=\"fn\">Connect</span>() { }\n<span class=\"kw\">public</span> <span class=\"kw\">void</span> <span class=\"fn\">Query</span>() { }\n<span class=\"kw\">public</span> <span class=\"kw\">void</span> <span class=\"fn\">Disconnect</span>() { }\n<span class=\"kw\">#endregion</span>\n\n<span class=\"cm\">// 警告/错误控制</span>\n<span class=\"kw\">#warning</span> 这段代码需要重构\n<span class=\"kw\">#error</span> 此平台不支持\n\n<span class=\"cm\">// 抑制警告</span>\n<span class=\"kw\">#pragma</span> warning disable  CS0168\n<span class=\"kw\">int</span> unused;  <span class=\"cm\">// 不会产生警告</span>\n<span class=\"kw\">#pragma</span> warning restore CS0168\n\n<span class=\"cm\">// 可空上下文控制</span>\n<span class=\"kw\">#nullable</span> enable\n<span class=\"kw\">string</span>? nullable = <span class=\"kw\">null</span>;  <span class=\"cm\">// 可空引用类型</span>\n<span class=\"kw\">#nullable</span> disable</pre></div><table><tr><th>指令</th><th>用途</th></tr><tr><td>#if/#elif/#else/#endif</td><td>条件编译</td></tr><tr><td>#define/#undef</td><td>定义/取消符号</td></tr><tr><td>#region/#endregion</td><td>代码折叠</td></tr><tr><td>#warning/#error</td><td>生成警告/错误</td></tr><tr><td>#pragma warning</td><td>控制警告</td></tr><tr><td>#nullable</td><td>可空引用类型控制</td></tr><tr><td>#line</td><td>修改行号/文件名</td></tr></table>"},
{id:"adv-20",title:"反射",tags:["反射","Reflection","Type","Assembly"],content:"<h4>反射(Reflection)</h4><p>反射允许在<strong>运行时</strong>获取类型信息、创建实例、调用方法、访问字段/属性。位于<code>System.Reflection</code>命名空间。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"cm\">// 获取Type的三种方式</span>\n<span class=\"type\">Type</span> t1 = <span class=\"kw\">typeof</span>(<span class=\"kw\">string</span>);\n<span class=\"type\">Type</span> t2 = obj.GetType();\n<span class=\"type\">Type</span> t3 = <span class=\"type\">Type</span>.<span class=\"fn\">GetType</span>(<span class=\"str\">\"System.String\"</span>);\n\n<span class=\"cm\">// 获取类型信息</span>\n<span class=\"type\">Type</span> type = <span class=\"kw\">typeof</span>(<span class=\"type\">Person</span>);\nConsole.WriteLine(type.Name);       <span class=\"cm\">// Person</span>\nConsole.WriteLine(type.Namespace);   <span class=\"cm\">// 命名空间</span>\nConsole.WriteLine(type.IsClass);     <span class=\"cm\">// true</span>\n\n<span class=\"cm\">// 获取所有公共属性</span>\n<span class=\"kw\">foreach</span> (<span class=\"kw\">var</span> prop <span class=\"kw\">in</span> type.<span class=\"fn\">GetProperties</span>())\n    Console.WriteLine(prop.Name);\n\n<span class=\"cm\">// 获取所有公共方法</span>\n<span class=\"kw\">foreach</span> (<span class=\"kw\">var</span> method <span class=\"kw\">in</span> type.<span class=\"fn\">GetMethods</span>())\n    Console.WriteLine(method.Name);\n\n<span class=\"cm\">// 动态创建实例并调用方法</span>\n<span class=\"kw\">object</span> instance = <span class=\"type\">Activator</span>.<span class=\"fn\">CreateInstance</span>(type);\n<span class=\"kw\">var</span> method = type.<span class=\"fn\">GetMethod</span>(<span class=\"str\">\"SayHello\"</span>);\nmethod.<span class=\"fn\">Invoke</span>(instance, <span class=\"kw\">null</span>);\n\n<span class=\"cm\">// 动态设置属性值</span>\n<span class=\"kw\">var</span> propName = type.<span class=\"fn\">GetProperty</span>(<span class=\"str\">\"Name\"</span>);\npropName.<span class=\"fn\">SetValue</span>(instance, <span class=\"str\">\"张三\"</span>);\n\n<span class=\"cm\">// 加载程序集</span>\n<span class=\"kw\">var</span> asm = <span class=\"type\">Assembly</span>.<span class=\"fn\">LoadFrom</span>(<span class=\"str\">\"MyLibrary.dll\"</span>);\n<span class=\"kw\">var</span> types = asm.<span class=\"fn\">GetTypes</span>();</pre></div><div class=\"highlight-box\"><strong>性能注意：</strong>反射比直接调用慢很多。频繁使用时考虑缓存Type/MethodInfo对象，或用表达式树/源生成器替代。常用于ORM框架、依赖注入容器、序列化库、插件系统。</div>"},
{id:"adv-21",title:"特性",tags:["特性","Attribute","元数据","自定义特性"],content:"<h4>特性(Attribute)</h4><p>特性为代码元素(类、方法、属性等)添加<strong>声明性元数据</strong>。可以在运行时通过反射读取。以<code>[AttributeName]</code>形式使用。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"cm\">// 常用内置特性</span>\n<span class=\"kw\">[Serializable]</span>\n<span class=\"kw\">[Obsolete</span>(<span class=\"str\">\"请使用NewMethod代替\"</span>)]\n<span class=\"kw\">public</span> <span class=\"kw\">void</span> <span class=\"fn\">OldMethod</span>() { }\n\n<span class=\"kw\">[MethodImpl</span>(MethodImplOptions.AggressiveInlining)]\n<span class=\"kw\">public</span> <span class=\"kw\">int</span> <span class=\"fn\">Fast</span>() => <span class=\"num\">42</span>;\n\n<span class=\"cm\">// 自定义特性</span>\n<span class=\"kw\">[AttributeUsage</span>(AttributeTargets.Class | AttributeTargets.Method, Inherited = <span class=\"kw\">true</span>)]\n<span class=\"kw\">public</span> <span class=\"kw\">class</span> <span class=\"type\">AuthorAttribute</span> : <span class=\"type\">Attribute</span>\n{\n    <span class=\"kw\">public</span> <span class=\"kw\">string</span> Name { <span class=\"kw\">get</span>; }\n    <span class=\"kw\">public</span> <span class=\"kw\">string</span> Date { <span class=\"kw\">get</span>; <span class=\"kw\">set</span>; }\n    <span class=\"kw\">public</span> <span class=\"fn\">AuthorAttribute</span>(<span class=\"kw\">string</span> name) => Name = name;\n}\n\n<span class=\"cm\">// 使用自定义特性</span>\n<span class=\"kw\">[Author</span>(<span class=\"str\">\"张三\"</span>, Date = <span class=\"str\">\"2024-01-15\"</span>)]\n<span class=\"kw\">class</span> <span class=\"type\">MyClass</span> { }\n\n<span class=\"cm\">// 通过反射读取特性</span>\n<span class=\"kw\">var</span> attr = <span class=\"kw\">typeof</span>(<span class=\"type\">MyClass</span>).<span class=\"fn\">GetCustomAttribute</span><<span class=\"type\">AuthorAttribute</span>>();\nConsole.WriteLine(attr.Name);  <span class=\"cm\">// 张三</span></pre></div><table><tr><th>常用特性</th><th>用途</th></tr><tr><td>[Obsolete]</td><td>标记已过时的方法</td></tr><tr><td>[Serializable]</td><td>标记可序列化</td></tr><tr><td>[JsonProperty]</td><td>JSON序列化配置</td></tr><tr><td>[Required]</td><td>数据验证(必填)</td></tr><tr><td>[HttpGet]/[HttpPost]</td><td>ASP.NET路由</td></tr><tr><td>[Fact]/[Theory]</td><td>xUnit测试方法</td></tr></table>"},
{id:"adv-22",title:"迭代器",tags:["迭代器","yield","IEnumerator","foreach"],content:"<h4>迭代器(Iterator)</h4><p>使用<code>yield return</code>和<code>yield break</code>的方法称为迭代器。它自动实现IEnumerator接口，支持<strong>延迟执行</strong>——元素在需要时才生成。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"cm\">// 简单迭代器</span>\n<span class=\"kw\">public</span> <span class=\"type\">IEnumerable</span><<span class=\"kw\">int</span>> <span class=\"fn\">GetNumbers</span>()\n{\n    <span class=\"kw\">yield</span> <span class=\"kw\">return</span> <span class=\"num\">1</span>;\n    <span class=\"kw\">yield</span> <span class=\"kw\">return</span> <span class=\"num\">2</span>;\n    <span class=\"kw\">yield</span> <span class=\"kw\">return</span> <span class=\"num\">3</span>;\n}\n\n<span class=\"cm\">// 斐波那契数列（无限序列）</span>\n<span class=\"kw\">public</span> <span class=\"type\">IEnumerable</span><<span class=\"kw\">long</span>> <span class=\"fn\">Fibonacci</span>()\n{\n    <span class=\"kw\">long</span> a = <span class=\"num\">0</span>, b = <span class=\"num\">1</span>;\n    <span class=\"kw\">while</span> (<span class=\"kw\">true</span>)\n    {\n        <span class=\"kw\">yield</span> <span class=\"kw\">return</span> a;\n        <span class=\"kw\">long</span> t = a + b;\n        a = b;\n        b = t;\n    }\n}\n\n<span class=\"cm\">// 带条件的迭代器（yield break停止）</span>\n<span class=\"kw\">public</span> <span class=\"type\">IEnumerable</span><<span class=\"kw\">string</span>> <span class=\"fn\">ReadLines</span>(<span class=\"kw\">string</span> path)\n{\n    <span class=\"kw\">using</span> <span class=\"kw\">var</span> reader = <span class=\"kw\">new</span> <span class=\"type\">StreamReader</span>(path);\n    <span class=\"kw\">string</span> line;\n    <span class=\"kw\">while</span> ((line = reader.<span class=\"fn\">ReadLine</span>()) != <span class=\"kw\">null</span>)\n    {\n        <span class=\"kw\">if</span> (<span class=\"kw\">string</span>.IsNullOrWhiteSpace(line))\n            <span class=\"kw\">yield</span> <span class=\"kw\">break</span>;  <span class=\"cm\">// 停止迭代</span>\n        <span class=\"kw\">yield</span> <span class=\"kw\">return</span> line;\n    }\n}\n\n<span class=\"cm\">// 使用（延迟执行）</span>\n<span class=\"kw\">var</span> fib = <span class=\"fn\">Fibonacci</span>().<span class=\"fn\">Take</span>(<span class=\"num\">10</span>);  <span class=\"cm\">// 只取前10个</span></pre></div><div class=\"highlight-box\"><strong>延迟执行：</strong>迭代器代码在被迭代前不会执行。每次foreach获取下一个元素时才执行到下一个yield return。适合处理大数据流和无限序列。</div>"},
,{id:"adv-23",title:"特殊语法",tags:["var","??","?.","nameof","switch表达式"],content:"<h4>C#特殊语法糖</h4><p>C#提供了多种简洁优雅的语法糖来减少代码量、提高可读性。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"cm\">// var 类型推断</span>\n<span class=\"kw\">var</span> dict = <span class=\"kw\">new</span> <span class=\"type\">Dictionary</span><<span class=\"kw\">string</span>, <span class=\"kw\">int</span>>();  <span class=\"cm\">// 编译器推断类型</span>\n\n<span class=\"cm\">// ?? 空合并运算符</span>\n<span class=\"kw\">string</span> name = input ?? <span class=\"str\">\"默认值\"</span>;  <span class=\"cm\">// input为null时取默认值</span>\n\n<span class=\"cm\">// ??= 空合并赋值 (C# 8.0)</span>\nname ??= <span class=\"str\">\"默认值\"</span>;  <span class=\"cm\">// 等价于 if(name==null) name=\"默认值\"</span>\n\n<span class=\"cm\">// ?. 空条件运算符</span>\n<span class=\"kw\">int</span>? len = obj?.Name?.Length;  <span class=\"cm\">// 任一步为null则返回null</span>\nobj?.<span class=\"fn\">Method</span>();  <span class=\"cm\">// obj为null时不调用</span>\n\n<span class=\"cm\">// nameof 获取名称字符串</span>\n<span class=\"kw\">string</span> n = <span class=\"kw\">nameof</span>(obj.Name);  <span class=\"cm\">// \"Name\"——重构安全</span>\n\n<span class=\"cm\">// switch表达式 (C# 8.0)</span>\n<span class=\"kw\">string</span> grade = score <span class=\"kw\">switch</span>\n{\n    >= <span class=\"num\">90</span> => <span class=\"str\">\"A\"</span>,\n    >= <span class=\"num\">80</span> => <span class=\"str\">\"B\"</span>,\n    >= <span class=\"num\">70</span> => <span class=\"str\">\"C\"</span>,\n    >= <span class=\"num\">60</span> => <span class=\"str\">\"D\"</span>,\n    _ => <span class=\"str\">\"F\"</span>\n};\n\n<span class=\"cm\">// 范围运算符 (C# 8.0)</span>\n<span class=\"kw\">int</span>[] arr = { <span class=\"num\">1</span>, <span class=\"num\">2</span>, <span class=\"num\">3</span>, <span class=\"num\">4</span>, <span class=\"num\">5</span> };\n<span class=\"kw\">var</span> slice = arr[<span class=\"num\">1</span>..<span class=\"num\">4</span>];  <span class=\"cm\">// {2,3,4}</span>\n<span class=\"kw\">var</span> last3 = arr[^<span class=\"num\">3</span>..];  <span class=\"cm\">// 最后3个 {3,4,5}</span></pre></div><table><tr><th>语法</th><th>版本</th><th>等价写法</th></tr><tr><td>var</td><td>C#3</td><td>显式类型声明</td></tr><tr><td>??</td><td>C#2</td><td>a!=null ? a : b</td></tr><tr><td>??=</td><td>C#8</td><td>if(a==null) a=b</td></tr><tr><td>?.</td><td>C#6</td><td>if(a!=null) a.b</td></tr><tr><td>nameof</td><td>C#6</td><td>字符串字面量</td></tr><tr><td>switch表达式</td><td>C#8</td><td>switch语句</td></tr><tr><td>范围/索引 [..] [^]</td><td>C#8</td><td>Substring/Skip</td></tr></table>"},
{id:"adv-24",title:"值和引用深度理解",tags:["值类型","引用类型","内存","栈","堆"],content:"<h4>内存分配深入</h4><p>值类型和引用类型的内存分配并不简单。关键区别：值类型内容直接存储在变量位置，引用类型存储的是对象在堆上的地址。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"cm\">// 值类型：在线程栈上分配（通常）</span>\n<span class=\"kw\">int</span> x = <span class=\"num\">42</span>;  <span class=\"cm\">// x直接存储值42</span>\n<span class=\"kw\">struct</span> <span class=\"type\">Point</span> { <span class=\"kw\">public</span> <span class=\"kw\">int</span> X, Y; }\n<span class=\"type\">Point</span> p = <span class=\"kw\">new</span> <span class=\"type\">Point</span> { X = <span class=\"num\">1</span>, Y = <span class=\"num\">2</span> };\n<span class=\"cm\">// p在栈上，X和Y直接存储在p中</span>\n\n<span class=\"cm\">// 引用类型：栈存引用，堆存对象</span>\n<span class=\"kw\">class</span> <span class=\"type\">Person</span> { <span class=\"kw\">public</span> <span class=\"kw\">string</span> Name; }\n<span class=\"type\">Person</span> per = <span class=\"kw\">new</span> <span class=\"type\">Person</span> { Name = <span class=\"str\">\"张三\"</span> };\n<span class=\"cm\">// per在栈上存堆地址 -> 堆上存Person对象</span>\n\n<span class=\"cm\">// 特殊情况</span>\n<span class=\"cm\">// 1. 值类型作为类成员 -> 内联在堆上</span>\n<span class=\"kw\">class</span> <span class=\"type\">Container</span> { <span class=\"kw\">public</span> <span class=\"type\">Point</span> P; }\n<span class=\"cm\">// Point值随Container对象在堆上</span>\n\n<span class=\"cm\">// 2. 引用类型作为struct成员 -> 指针在struct中</span>\n<span class=\"kw\">struct</span> <span class=\"type\">Wrapper</span> { <span class=\"kw\">public</span> <span class=\"type\">Person</span> P; }\n<span class=\"cm\">// Person引用在Wrapper中，Person对象仍在堆上</span>\n\n<span class=\"cm\">// 3. 字符串的特殊性</span>\n<span class=\"kw\">string</span> a = <span class=\"str\">\"hello\"</span>, b = <span class=\"str\">\"hello\"</span>;\n<span class=\"kw\">bool</span> same = <span class=\"kw\">object</span>.ReferenceEquals(a, b);\n<span class=\"cm\">// true! 字符串驻留(Interning)导致同字面量引同一对象</span></pre></div><table><tr><th>类型</th><th>存储位置</th><th>赋值语义</th><th>null</th></tr><tr><td>值类型</td><td>栈/内联</td><td>复制值</td><td>不能(除非T?)</td></tr><tr><td>引用类型</td><td>堆</td><td>复制地址</td><td>可以</td></tr></table><div class=\"highlight-box\"><strong>关键理解：</strong>值类型变量直接存储数据，引用类型变量存储的是地址。这是理解所有赋值、传参、比较行为的基础。</div>"},
{id:"adv-25",title:"插入排序",tags:["插入排序","排序算法","O(n^2)"],content:"<h4>插入排序(Insertion Sort)</h4><p>将元素逐个插入到已排序部分的正确位置。类似整理扑克牌。时间复杂度O(n^2)，但在数据基本有序时接近O(n)。是稳定排序。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"kw\">static</span> <span class=\"kw\">void</span> <span class=\"fn\">InsertionSort</span>(<span class=\"kw\">int</span>[] arr)\n{\n    <span class=\"kw\">for</span> (<span class=\"kw\">int</span> i = <span class=\"num\">1</span>; i < arr.Length; i++)\n    {\n        <span class=\"kw\">int</span> key = arr[i];  <span class=\"cm\">// 待插入元素</span>\n        <span class=\"kw\">int</span> j = i - <span class=\"num\">1</span>;\n\n        <span class=\"cm\">// 将大于key的元素向后移</span>\n        <span class=\"kw\">while</span> (j >= <span class=\"num\">0</span> && arr[j] > key)\n        {\n            arr[j + <span class=\"num\">1</span>] = arr[j];\n            j--;\n        }\n        arr[j + <span class=\"num\">1</span>] = key;  <span class=\"cm\">// 插入正确位置</span>\n    }\n}\n\n<span class=\"cm\">// 测试</span>\n<span class=\"kw\">int</span>[] nums = { <span class=\"num\">64</span>, <span class=\"num\">34</span>, <span class=\"num\">25</span>, <span class=\"num\">12</span>, <span class=\"num\">22</span>, <span class=\"num\">11</span>, <span class=\"num\">90</span> };\n<span class=\"fn\">InsertionSort</span>(nums);\n<span class=\"cm\">// 结果: 11, 12, 22, 25, 34, 64, 90</span></pre></div><table><tr><th>算法分析</th><th>值</th></tr><tr><td>最好情况</td><td>O(n) —— 已排序数组</td></tr><tr><td>最坏情况</td><td>O(n^2) —— 逆序数组</td></tr><tr><td>平均情况</td><td>O(n^2)</td></tr><tr><td>空间复杂度</td><td>O(1) —— 原地排序</td></tr><tr><td>稳定性</td><td>稳定</td></tr><tr><td>适用场景</td><td>小数组、基本有序数据</td></tr></table>"},
{id:"adv-26",title:"希尔排序",tags:["希尔排序","排序算法","缩小增量"],content:"<h4>希尔排序(Shell Sort)</h4><p>插入排序的改进版——先以较大间隔(gap)分组进行插入排序，逐步缩小间隔直到gap=1。让元素快速接近最终位置，大幅减少后续移动次数。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"kw\">static</span> <span class=\"kw\">void</span> <span class=\"fn\">ShellSort</span>(<span class=\"kw\">int</span>[] arr)\n{\n    <span class=\"kw\">int</span> n = arr.Length;\n    <span class=\"cm\">// 初始gap为n/2，每次减半</span>\n    <span class=\"kw\">for</span> (<span class=\"kw\">int</span> gap = n / <span class=\"num\">2</span>; gap > <span class=\"num\">0</span>; gap /= <span class=\"num\">2</span>)\n    {\n        <span class=\"cm\">// 对每个gap分组进行插入排序</span>\n        <span class=\"kw\">for</span> (<span class=\"kw\">int</span> i = gap; i < n; i++)\n        {\n            <span class=\"kw\">int</span> temp = arr[i];\n            <span class=\"kw\">int</span> j;\n            <span class=\"kw\">for</span> (j = i; j >= gap && arr[j - gap] > temp; j -= gap)\n                arr[j] = arr[j - gap];\n            arr[j] = temp;\n        }\n    }\n}\n\n<span class=\"cm\">// 示例：gap序列 n/2, n/4, n/8, ..., 1</span>\n<span class=\"cm\">// {64,34,25,12,22,11,90}</span>\n<span class=\"cm\">// gap=3: 分组 {64,12,90},{34,22},{25,11}</span>\n<span class=\"cm\">// gap=1: 最终插入排序（此时已基本有序）</span></pre></div><table><tr><th>算法分析</th><th>值</th></tr><tr><td>时间复杂度</td><td>与gap序列有关，最好O(n log n)</td></tr><tr><td>最坏情况</td><td>O(n^2) —— 取决于gap序列</td></tr><tr><td>空间复杂度</td><td>O(1) —— 原地排序</td></tr><tr><td>稳定性</td><td>不稳定</td></tr><tr><td>关键</td><td>gap序列选择决定性能</td></tr></table>"},
{id:"adv-27",title:"归并排序",tags:["归并排序","分治","O(n log n)","稳定"],content:"<h4>归并排序(Merge Sort)</h4><p>经典<strong>分治(Divide and Conquer)</strong>算法。将数组递归拆分为两半，分别排序后合并。时间复杂度稳定O(n log n)，是稳定排序，但需要O(n)额外空间。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"kw\">static</span> <span class=\"kw\">void</span> <span class=\"fn\">MergeSort</span>(<span class=\"kw\">int</span>[] arr, <span class=\"kw\">int</span> left, <span class=\"kw\">int</span> right)\n{\n    <span class=\"kw\">if</span> (left >= right) <span class=\"kw\">return</span>;\n    <span class=\"kw\">int</span> mid = (left + right) / <span class=\"num\">2</span>;\n    <span class=\"fn\">MergeSort</span>(arr, left, mid);      <span class=\"cm\">// 排序左半</span>\n    <span class=\"fn\">MergeSort</span>(arr, mid + <span class=\"num\">1</span>, right); <span class=\"cm\">// 排序右半</span>\n    <span class=\"fn\">Merge</span>(arr, left, mid, right);   <span class=\"cm\">// 合并</span>\n}\n\n<span class=\"kw\">static</span> <span class=\"kw\">void</span> <span class=\"fn\">Merge</span>(<span class=\"kw\">int</span>[] arr, <span class=\"kw\">int</span> l, <span class=\"kw\">int</span> m, <span class=\"kw\">int</span> r)\n{\n    <span class=\"kw\">int</span> n1 = m - l + <span class=\"num\">1</span>, n2 = r - m;\n    <span class=\"kw\">int</span>[] L = <span class=\"kw\">new</span> <span class=\"kw\">int</span>[n1], R = <span class=\"kw\">new</span> <span class=\"kw\">int</span>[n2];\n    <span class=\"type\">Array</span>.Copy(arr, l, L, <span class=\"num\">0</span>, n1);\n    <span class=\"type\">Array</span>.Copy(arr, m + <span class=\"num\">1</span>, R, <span class=\"num\">0</span>, n2);\n\n    <span class=\"kw\">int</span> i = <span class=\"num\">0</span>, j = <span class=\"num\">0</span>, k = l;\n    <span class=\"kw\">while</span> (i < n1 && j < n2)\n        arr[k++] = L[i] <= R[j] ? L[i++] : R[j++];\n    <span class=\"kw\">while</span> (i < n1) arr[k++] = L[i++];\n    <span class=\"kw\">while</span> (j < n2) arr[k++] = R[j++];\n}</pre></div><table><tr><th>算法分析</th><th>值</th></tr><tr><td>时间复杂度</td><td>O(n log n) —— 所有情况</td></tr><tr><td>空间复杂度</td><td>O(n) —— 需要辅助数组</td></tr><tr><td>稳定性</td><td>稳定</td></tr><tr><td>适用场景</td><td>链表排序(原地)、外部排序</td></tr><tr><td>缺点</td><td>对数组需O(n)额外空间</td></tr></table>"},
{id:"adv-28",title:"快速排序",tags:["快速排序","分治","pivot","O(n log n)"],content:"<h4>快速排序(Quick Sort)</h4><p>实际中最快的通用排序算法。选择<strong>基准值(pivot)</strong>，将数组分为小于和大于pivot的两部分，递归排序。平均O(n log n)，最坏O(n^2)，但有多种优化可避免最坏情况。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"kw\">static</span> <span class=\"kw\">void</span> <span class=\"fn\">QuickSort</span>(<span class=\"kw\">int</span>[] arr, <span class=\"kw\">int</span> low, <span class=\"kw\">int</span> high)\n{\n    <span class=\"kw\">if</span> (low < high)\n    {\n        <span class=\"kw\">int</span> pi = <span class=\"fn\">Partition</span>(arr, low, high);\n        <span class=\"fn\">QuickSort</span>(arr, low, pi - <span class=\"num\">1</span>);\n        <span class=\"fn\">QuickSort</span>(arr, pi + <span class=\"num\">1</span>, high);\n    }\n}\n\n<span class=\"kw\">static</span> <span class=\"kw\">int</span> <span class=\"fn\">Partition</span>(<span class=\"kw\">int</span>[] arr, <span class=\"kw\">int</span> low, <span class=\"kw\">int</span> high)\n{\n    <span class=\"kw\">int</span> pivot = arr[high];  <span class=\"cm\">// 选最右元素为基准</span>\n    <span class=\"kw\">int</span> i = low - <span class=\"num\">1</span>;       <span class=\"cm\">// 小于pivot区域的边界</span>\n    <span class=\"kw\">for</span> (<span class=\"kw\">int</span> j = low; j < high; j++)\n    {\n        <span class=\"kw\">if</span> (arr[j] < pivot)\n        {\n            i++;\n            (<span class=\"kw\">int</span> t = arr[i]; arr[i] = arr[j]; arr[j] = t);\n        }\n    }\n    <span class=\"cm\">// 将pivot放到正确位置</span>\n    (<span class=\"kw\">int</span> tmp = arr[i + <span class=\"num\">1</span>]; arr[i + <span class=\"num\">1</span>] = arr[high]; arr[high] = tmp);\n    <span class=\"kw\">return</span> i + <span class=\"num\">1</span>;\n}</pre></div><table><tr><th>特性</th><th>值</th></tr><tr><td>平均</td><td>O(n log n)</td></tr><tr><td>最坏</td><td>O(n^2) —— 选到最小/最大pivot</td></tr><tr><td>空间</td><td>O(log n) —— 递归栈</td></tr><tr><td>稳定性</td><td>不稳定</td></tr><tr><td>优化</td><td>三数取中、随机pivot、小数组切换插入排序</td></tr></table><div class=\"highlight-box\"><strong>实践：</strong>List&lt;T&gt;.Sort()使用快速排序。避免最坏情况常用策略：随机选pivot或三数取中法。递归深度过大时切换为堆排序（.NET内部实现）。</div>"}
]},{id:"practice",title:"五、实践与总结",desc:"将理论知识转化为实战能力。从控制台开发到随机数应用，从多文件项目管理到完整的贪食蛇游戏，最后梳理学习路线图并推荐开发环境与工具。",badge:"实践",lessons:[
{id:"prac-1",title:"控制台相关",tags:["Console","控制台","输入输出","格式化"],content:"<h4>Console类常用API</h4><p>控制台程序的基础输入输出，位于<code>System</code>命名空间。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"cm\">// 输出</span>\nConsole.<span class=\"fn\">WriteLine</span>(<span class=\"str\">\"Hello World\"</span>);\nConsole.<span class=\"fn\">Write</span>(<span class=\"str\">\"不换行\"</span>);\nConsole.<span class=\"fn\">WriteLine</span>($<span class=\"str\">\"我是{name}，{age}岁\"</span>);  <span class=\"cm\">// 插值</span>\nConsole.<span class=\"fn\">WriteLine</span>(<span class=\"str\">\"{0} + {1} = {2}\"</span>, a, b, a+b);  <span class=\"cm\">// 格式化</span>\n\n<span class=\"cm\">// 输入</span>\n<span class=\"kw\">string</span> input = Console.<span class=\"fn\">ReadLine</span>();  <span class=\"cm\">// 读取一行</span>\n<span class=\"kw\">int</span> key = Console.<span class=\"fn\">Read</span>();           <span class=\"cm\">// 读取一个字符(ASCII)</span>\nConsoleKeyInfo ki = Console.<span class=\"fn\">ReadKey</span>();  <span class=\"cm\">// 读取按键(不显示)</span>\nConsole.<span class=\"fn\">ReadKey</span>(<span class=\"kw\">true</span>);  <span class=\"cm\">// 不显示按键</span>\n\n<span class=\"cm\">// 设置</span>\nConsole.<span class=\"fn\">Clear</span>();                    <span class=\"cm\">// 清屏</span>\nConsole.ForegroundColor = <span class=\"type\">ConsoleColor</span>.Green;\nConsole.BackgroundColor = <span class=\"type\">ConsoleColor</span>.Black;\nConsole.<span class=\"fn\">SetCursorPosition</span>(<span class=\"num\">10</span>, <span class=\"num\">5</span>);  <span class=\"cm\">// 设置光标位置</span>\nConsole.CursorVisible = <span class=\"kw\">false</span>;         <span class=\"cm\">// 隐藏光标</span>\nConsole.Title = <span class=\"str\">\"我的程序\"</span>;            <span class=\"cm\">// 设置窗口标题</span>\nConsole.<span class=\"fn\">Beep</span>(<span class=\"num\">440</span>, <span class=\"num\">500</span>);            <span class=\"cm\">// 蜂鸣(频率, 时长ms)</span></pre></div><div class=\"highlight-box\"><strong>小技巧：</strong>制作控制台游戏时，<code>SetCursorPosition</code>+隐藏光标+ConsoleColor可实现平滑动画效果。使用<code>Console.ReadKey(true)</code>读取方向键(HWND)来响应键盘操作。</div>"},
{id:"prac-2",title:"随机数",tags:["Random","随机数","种子"],content:"<h4>Random类</h4><p>C#使用<code>System.Random</code>类生成伪随机数。默认用当前时间做种子，也可手动指定种子以获得可重复的随机序列。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"kw\">var</span> rand = <span class=\"kw\">new</span> <span class=\"type\">Random</span>();\n\n<span class=\"cm\">// 基本随机方法</span>\n<span class=\"kw\">int</span> n = rand.<span class=\"fn\">Next</span>();            <span class=\"cm\">// 非负随机整数</span>\n<span class=\"kw\">int</span> n2 = rand.<span class=\"fn\">Next</span>(<span class=\"num\">100</span>);        <span class=\"cm\">// 0-99</span>\n<span class=\"kw\">int</span> n3 = rand.<span class=\"fn\">Next</span>(<span class=\"num\">10</span>, <span class=\"num\">20</span>);     <span class=\"cm\">// 10-19</span>\n<span class=\"kw\">double</span> d = rand.<span class=\"fn\">NextDouble</span>();     <span class=\"cm\">// 0.0 - 1.0</span>\n<span class=\"kw\">byte</span>[] bytes = <span class=\"kw\">new</span> <span class=\"kw\">byte</span>[<span class=\"num\">16</span>];\nrand.<span class=\"fn\">NextBytes</span>(bytes);            <span class=\"cm\">// 填充随机字节</span>\n\n<span class=\"cm\">// 指定种子（相同种子产生相同序列）</span>\n<span class=\"kw\">var</span> r1 = <span class=\"kw\">new</span> <span class=\"type\">Random</span>(<span class=\"num\">42</span>);\n<span class=\"kw\">var</span> r2 = <span class=\"kw\">new</span> <span class=\"type\">Random</span>(<span class=\"num\">42</span>);\nConsole.WriteLine(r1.<span class=\"fn\">Next</span>(<span class=\"num\">100</span>));  <span class=\"cm\">// 相同结果</span>\nConsole.WriteLine(r2.<span class=\"fn\">Next</span>(<span class=\"num\">100</span>));  <span class=\"cm\">// 相同结果</span>\n\n<span class=\"cm\">// 线程安全的Random（单例模式）</span>\n<span class=\"kw\">private</span> <span class=\"kw\">static</span> <span class=\"kw\">readonly</span> <span class=\"type\">Random</span> _rand = <span class=\"kw\">new</span>();\n<span class=\"kw\">public</span> <span class=\"kw\">static</span> <span class=\"kw\">int</span> <span class=\"fn\">GetRandom</span>(<span class=\"kw\">int</span> min, <span class=\"kw\">int</span> max)\n    => _rand.<span class=\"fn\">Next</span>(min, max);\n\n<span class=\"cm\">// 注意事项：不要在循环中反复创建Random</span>\n<span class=\"cm\">// 同一毫秒内new的Random会产生相同序列！</span></pre></div><div class=\"highlight-box\"><strong>常见陷阱：</strong>快速连续创建多个Random实例会得到相同种子（时间粒度不够）。应使用单个共享Random实例。.NET Core 2.1+提供了更安全的<code>Random.Shared</code>属性。</div>"},
{id:"prac-3",title:"多脚本文件",tags:["多文件","项目","partial","namespace"],content:"<h4>多文件项目管理</h4><p>C#项目通常包含多个.cs文件。合理组织文件结构是构建可维护项目的关键。常用策略：每类一个文件、使用文件夹对应命名空间。</p><h4>项目结构示例</h4><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"cm\">// MyGame/Program.cs —— 程序入口</span>\n<span class=\"kw\">class</span> <span class=\"type\">Program</span>\n{\n    <span class=\"kw\">static</span> <span class=\"kw\">void</span> <span class=\"fn\">Main</span>()\n    {\n        <span class=\"kw\">var</span> game = <span class=\"kw\">new</span> <span class=\"type\">SnakeGame</span>();\n        game.<span class=\"fn\">Run</span>();\n    }\n}\n\n<span class=\"cm\">// MyGame/Models/Snake.cs</span>\n<span class=\"kw\">namespace</span> <span class=\"type\">MyGame</span>.<span class=\"type\">Models</span>;\n<span class=\"kw\">class</span> <span class=\"type\">Snake</span> { <span class=\"cm\">/* 蛇的数据和行为 */</span> }\n\n<span class=\"cm\">// MyGame/Models/Food.cs</span>\n<span class=\"kw\">namespace</span> <span class=\"type\">MyGame</span>.<span class=\"type\">Models</span>;\n<span class=\"kw\">class</span> <span class=\"type\">Food</span> { <span class=\"cm\">/* 食物的数据和生成 */</span> }\n\n<span class=\"cm\">// MyGame/Core/GameEngine.cs</span>\n<span class=\"kw\">namespace</span> <span class=\"type\">MyGame</span>.<span class=\"type\">Core</span>;\n<span class=\"kw\">class</span> <span class=\"type\">GameEngine</span> { <span class=\"cm\">/* 游戏主循环和逻辑 */</span> }\n\n<span class=\"cm\">// MyGame/UI/Renderer.cs</span>\n<span class=\"kw\">namespace</span> <span class=\"type\">MyGame</span>.<span class=\"type\">UI</span>;\n<span class=\"kw\">class</span> <span class=\"type\">Renderer</span> { <span class=\"cm\">/* 控制台绘制 */</span> }</pre></div><h4>跨文件访问</h4><ul><li>同一命名空间：直接使用类名</li><li>不同命名空间：使用<code>using</code>导入</li><li>顶层语句(C#9+)：Program.cs可省略Main方法和class包裹</li></ul><div class=\"highlight-box\"><strong>最佳实践：</strong>一个文件一个类（或紧密相关的少量类）。文件夹结构反映命名空间层次。使用文件作用域命名空间(C#10+)减少缩进。分离关注点：UI、业务逻辑、数据模型各在不同的文件/文件夹中。</div>"},
{id:"prac-4",title:"贪食蛇项目",tags:["贪食蛇","控制台游戏","项目实战"],content:"<h4>控制台贪食蛇游戏</h4><p>综合运用面向对象、集合、控制台API、多线程等知识实现经典贪食蛇游戏。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">C#</span></div><pre><span class=\"cm\">// 核心架构</span>\n<span class=\"kw\">class</span> <span class=\"type\">SnakeGame</span>\n{\n    <span class=\"kw\">private</span> <span class=\"type\">LinkedList</span><(<span class=\"kw\">int</span>, <span class=\"kw\">int</span>)> _snake;  <span class=\"cm\">// 蛇身(坐标)</span>\n    <span class=\"kw\">private</span> (<span class=\"kw\">int</span> X, <span class=\"kw\">int</span> Y) _food;           <span class=\"cm\">// 食物坐标</span>\n    <span class=\"kw\">private</span> <span class=\"kw\">int</span> _dx = <span class=\"num\">1</span>, _dy = <span class=\"num\">0</span>;           <span class=\"cm\">// 当前方向</span>\n    <span class=\"kw\">private</span> <span class=\"kw\">int</span> _score = <span class=\"num\">0</span>;\n    <span class=\"kw\">private</span> <span class=\"kw\">bool</span> _isRunning = <span class=\"kw\">true</span>;\n    <span class=\"kw\">private</span> <span class=\"kw\">const</span> <span class=\"kw\">int</span> Width = <span class=\"num\">40</span>, Height = <span class=\"num\">20</span>;\n\n    <span class=\"kw\">public</span> <span class=\"kw\">void</span> <span class=\"fn\">Run</span>()\n    {\n        <span class=\"fn\">Init</span>();\n        <span class=\"kw\">while</span> (_isRunning)\n        {\n            <span class=\"fn\">HandleInput</span>();\n            <span class=\"fn\">Update</span>();\n            <span class=\"fn\">Render</span>();\n            Thread.<span class=\"fn\">Sleep</span>(<span class=\"num\">100</span>);  <span class=\"cm\">// 控制游戏速度</span>\n        }\n    }\n\n    <span class=\"kw\">private</span> <span class=\"kw\">void</span> <span class=\"fn\">HandleInput</span>()\n    {\n        <span class=\"kw\">if</span> (!Console.KeyAvailable) <span class=\"kw\">return</span>;\n        <span class=\"kw\">switch</span> (Console.<span class=\"fn\">ReadKey</span>(<span class=\"kw\">true</span>).Key)\n        {\n            <span class=\"kw\">case</span> ConsoleKey.UpArrow: <span class=\"kw\">if</span> (_dy != <span class=\"num\">1</span>) { _dx = <span class=\"num\">0</span>; _dy = -<span class=\"num\">1</span>; } <span class=\"kw\">break</span>;\n            <span class=\"kw\">case</span> ConsoleKey.DownArrow: <span class=\"kw\">if</span> (_dy != -<span class=\"num\">1</span>) { _dx = <span class=\"num\">0</span>; _dy = <span class=\"num\">1</span>; } <span class=\"kw\">break</span>;\n            <span class=\"kw\">case</span> ConsoleKey.LeftArrow: <span class=\"kw\">if</span> (_dx != <span class=\"num\">1</span>) { _dx = -<span class=\"num\">1</span>; _dy = <span class=\"num\">0</span>; } <span class=\"kw\">break</span>;\n            <span class=\"kw\">case</span> ConsoleKey.RightArrow: <span class=\"kw\">if</span> (_dx != -<span class=\"num\">1</span>) { _dx = <span class=\"num\">1</span>; _dy = <span class=\"num\">0</span>; } <span class=\"kw\">break</span>;\n        }\n    }\n}</pre></div><h4>关键技术点</h4><table><tr><th>技术</th><th>用途</th></tr><tr><td>LinkedList</td><td>存储蛇身，方便头部添加尾部删除</td></tr><tr><td>Console.SetCursorPosition</td><td>在指定位置绘制，实现无闪烁刷新</td></tr><tr><td>Console.ReadKey(true)</td><td>非阻塞读取键盘输入</td></tr><tr><td>Thread.Sleep</td><td>控制帧率/游戏速度</td></tr><tr><td>Random</td><td>随机生成食物位置</td></tr></table><div class=\"highlight-box\"><strong>实现步骤：</strong>1.搭建渲染框架(画墙) 2.实现蛇的移动 3.添加键盘控制 4.实现吃食物和增长 5.碰撞检测(墙壁和自身) 6.分数系统。逐层迭代，每步可运行验证。</div>"},
{id:"prac-5",title:"学习路线图",tags:["学习路线","进阶","体系"],content:"<h4>C#学习路线图</h4><table><tr><th>阶段</th><th>内容</th><th>预计时间</th></tr><tr><td>1. 语法基础</td><td>变量、数据类型、运算符、流程控制、数组、字符串</td><td>1-2周</td></tr><tr><td>2. 面向对象</td><td>类/对象、封装、继承、多态、接口、抽象类</td><td>2-4周</td></tr><tr><td>3. 进阶语法</td><td>泛型、委托/事件、Lambda、LINQ、异常处理</td><td>2-3周</td></tr><tr><td>4. 集合与算法</td><td>List/Dictionary、排序算法、数据结构基础</td><td>2-3周</td></tr><tr><td>5. 框架应用</td><td>ASP.NET Core、EF Core、依赖注入、Web API</td><td>4-8周</td></tr><tr><td>6. 工具与部署</td><td>Git、Docker、CI/CD、云部署(Azure/AWS)</td><td>2-4周</td></tr><tr><td>7. 进阶专题</td><td>多线程/异步、反射/特性、设计模式、性能优化</td><td>持续学习</td></tr></table><h4>推荐学习资源</h4><ul><li><strong>官方文档：</strong>docs.microsoft.com/dotnet/csharp</li><li><strong>实战项目：</strong>控制台游戏 -> Web API -> 全栈应用</li><li><strong>刷题：</strong>LeetCode, HackerRank (练习算法和C#语法)</li><li><strong>开源项目：</strong>GitHub上Star优秀的C#项目，阅读源码</li><li><strong>书籍：</strong>C# in Depth (Jon Skeet)、CLR via C# (Jeffrey Richter)</li></ul><div class=\"highlight-box\"><strong>学习建议：</strong>理论与实践结合——学了新概念立即写代码验证。从控制台小程序开始，逐步过渡到Web应用。阅读开源项目代码是提升最快的方式之一。打好基础比追求新技术更重要。</div>"},
{id:"prac-6",title:"开发环境与工具",tags:["开发环境","工具","VS","VS Code","NuGet"],content:"<h4>开发环境</h4><table><tr><th>工具</th><th>适用场景</th><th>说明</th></tr><tr><td>Visual Studio 2022</td><td>主力IDE</td><td>功能最全，适合大型项目，免费Community版</td></tr><tr><td>VS Code</td><td>轻量编辑器</td><td>跨平台，需装C#扩展，适合前端/混合项目</td></tr><tr><td>JetBrains Rider</td><td>商业IDE</td><td>跨平台，强大的重构和代码分析</td></tr><tr><td>.NET SDK</td><td>命令行</td><td>dotnet new/build/run/publish</td></tr><tr><td>NuGet</td><td>包管理</td><td>管理第三方库依赖</td></tr><tr><td>Git/GitHub</td><td>版本控制</td><td>代码管理、协作、CI/CD</td></tr></table><h4>常用dotnet CLI命令</h4><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">Terminal</span></div><pre>dotnet new console -n MyApp    <span class=\"cm\">// 创建控制台项目</span>\ndotnet new webapi -n MyApi      <span class=\"cm\">// 创建Web API项目</span>\ndotnet build                     <span class=\"cm\">// 编译项目</span>\ndotnet run                       <span class=\"cm\">// 编译并运行</span>\ndotnet test                      <span class=\"cm\">// 运行单元测试</span>\ndotnet publish -c Release        <span class=\"cm\">// 发布</span>\ndotnet add package Newtonsoft.Json <span class=\"cm\">// 安装NuGet包</span>\ndotnet --list-sdks               <span class=\"cm\">// 列出已安装的SDK版本</span></pre></div><h4>Visual Studio快捷键（推荐）</h4><table><tr><th>快捷键</th><th>功能</th></tr><tr><td>F5 / Ctrl+F5</td><td>调试运行 / 不调试运行</td></tr><tr><td>F9</td><td>设置断点</td></tr><tr><td>F10 / F11</td><td>逐过程 / 逐语句调试</td></tr><tr><td>Ctrl+K, Ctrl+D</td><td>格式化文档</td></tr><tr><td>Ctrl+R, Ctrl+R</td><td>重命名(重构)</td></tr><tr><td>Ctrl+. (Ctrl+Period)</td><td>快速操作/重构建议</td></tr><tr><td>Ctrl+K, Ctrl+C / Ctrl+U</td><td>注释/取消注释</td></tr><tr><td>prop + Tab + Tab</td><td>快速生成自动属性</td></tr><tr><td>ctor + Tab + Tab</td><td>快速生成构造函数</td></tr><tr><td>cw + Tab + Tab</td><td>快速生成Console.WriteLine</td></tr></table><div class=\"highlight-box\"><strong>环境配置建议：</strong>初学者安装Visual Studio Community 2022（勾选.NET桌面开发工作负载）。熟练后学习dotnet CLI命令行操作。使用Git管理所有代码，即使个人项目也应该版本控制。</div>"}
]}]
];
